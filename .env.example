# AI WARLORDS — M002 dev environment example.
# Copy to .env for local use. NEVER commit .env (see .gitignore).
# NOTE: M002 does not auto-load .env (zero-dependency policy).
# Export vars in your shell or prefix the command, e.g.:
#   HOST=0.0.0.0 PORT=3000 npm run dev
HOST=127.0.0.1
PORT=3000
