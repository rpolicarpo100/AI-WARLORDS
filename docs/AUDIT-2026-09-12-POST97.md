# AUDIT pós-M097 (2026-09-12) — auditoria crítica pedida pelo user

Âmbito: tudo até M097 (foco M091–M097 + integridade global).
Método: re-execução (nada por memória), desafio de claims, probe live.

## Veredictos que sobreviveram ao desafio

- Suite 1960/1960 100×4 (66 ficheiros), coverage 100×4 real,
  LINT-0, TSC-0, thresholds 100 no config (não é teatro).
- 5/5 sims sans-ms idênticos após a mudança de motor M095
  (standard preservado bit-a-bit).
- Bundle `250c35ac…` ×2 + verify PASS; match.html 576520.
- Zero TODOs/skips/only/secrets; console só onde é by design
  (AW_LOG, serve, dev); `npm audit` 0 vulns.
- Constantes OPERATIONS verificadas no código: 300/60s, 64KB,
  32 watchers, cap 50, Elo 1200/K32, seed 69, healthz keys.
- Live: static exacto + markers; API nosniff+DENY+CORS;
  metrics/healthz keys live; DRILL PASS; deploys live @ b2881ff.
- Cadeia git intacta; engines/key/render.yaml consistentes.
- Free views: perceive 0 sem crash (probe tsx); verdict ongoing.

## Defeitos encontrados e corrigidos (13, só docs — código limpo)

OPERATIONS.md (erros meus, a maioria desde M093 — escrevi de
memória em vez de verificar; mea culpa registada):

- F1. `/match/:id/stream` → `/match/:id/events?from=N`
  (a rota chama-se events; transport.ts:578).
- F2. Métricas fabricadas ("matches forged/closed, dispatches")
  → chaves reais {requests, byStatus, startedAt, uptimeMs} +
  quirk preciso M088 (self em requests, nunca em byStatus).
- F3. 500 (`oops`) → (`transport fault`) — o token `oops`
  nunca existiu no src (só em payloads de teste).
- F4. "26 fixed reasons" ambíguo → 27 call-sites / 16 strings
  distintas (M087 contou sites: 26 então + 'bad mode' M096).
- F5. Size 575818 @ M091–M093 → 576520 @ M097.
- F6. Forge `{seed?}` → `{seed?, mode?}` + close void free.
- F7. Markers + `netMode`×2.
- F8. Static headers: nosniff JÁ live (fonte desconhecida —
  default da plataforma ou click humano); pendentes só
  DENY + Referrer-Policy (item humano actualizado).

MODULE_STATUS.md: header M012/11-09 → audit/12-09; order line
e "Próximo" (M095/bloco 9) → M098 + gates Solana.

RISK_REGISTER.md: R-32 mitigação cita M094+M096 landed
(mantido FUTURE — o risco materializa-se na Fase 28).

## Observações (sem fix — registadas, não escondidas)

- O1. D-082/M087 nunca documentaram o método do "26"
  (agora explicado: call-sites). Docs de módulo congelados
  ficam como estão; esta é a correcção viva.
- O2. Janela do breach M092 formalmente UNKNOWN (logs npm
  rodados; tentativa de delimitação documentada e falhada).
- O3. HEAD / → 404 (sem handling de HEAD; impacto zero —
  health checks usam GET; nunca especificado).
- O4. Free lê 0 prompts nas views (fail-closed by design).
- O5. STATUS/OPERATIONS já eram prettier-dirty no HEAD —
  sem reflow (não churnar história por cosmética).
- O6. M096.md chegou no commit M097 (race declarada em M097.md).
- O7. Gate do audit: seed 77 morreu (OOM, 84 tsx órfãos —
  a suite/smokes largam filhos); re-run isolado verde
  1960/1960. Mitigação no OPERATIONS (pkill). 74–76
  íntegros (sumários completos, kill não imprime PASS).

## Conclusão

Código: LIMPO (nenhum defeito encontrado em src/sim/tools).
Docs vivos: CORRIGIDOS (13). Docs congelados: 1 ambiguidade
explicada (O1), resto intacto. Prod: VERDE nos dois serviços.
Lição: OPERATIONS apodrece em ~4 módulos sem revisão —
rever por bloco (já lá estava escrito; agora cumprido).
