# M033 — Unit Doctrines (VERIFIED @ 2026-09-11)

> 3.º do bloco Personalidade. Vocabulário fixo-6 + deltas
> DNA canónicos VOTADOS + rótulo opcional no record
> (âmbito VOTADO). Data-first, sem writer, sem behavior,
> sem composer (L2+, proibido na leaf por L0↛L0).

## 1. Âmbito (contrato)

- `doctrines.ts` NOVO (LEAF L0, zero imports):
  DOCTRINE_IDS (6, ordem #24, hifenizados) +
  DoctrineId + isDoctrineId + DoctrineDeltas
  (shape estrutural — mirror de DnaTraits, L0↛L0) +
  DELTA_MIN/MAX (−30/+30) + DOCTRINE_DNA_DELTAS
  (matriz 6×10 votada, frozen) + dnaDeltasOf
  (cópia fresca; desconhecido→undefined fail-soft,
  molde dnaPresetOf).
- `CommanderRecord.doctrine?` (embed votado; union
  mirror + isMirroredDoctrine; deltas NÃO espelhados
  — lookup vive em doctrines.ts).
- Composição votada p/ futuros consumidores (só
  REGISTADA): efetivo = clamp(base + deltas, 0–100),
  base = record-DNA ?? preset-personalidade ??
  neutro-50 (emperor votado).
- Sem slot novo; views/Match/validation zero churn
  (F-09 top-level); commission sem doctrine (M029).
- Selo phase1-gate: 2 pins (census + LAYERS
  `doctrines: 0` — fail-loud, M015).
- Cortes (D-027, âmbito votado): writer (init-placed);
  behavior/composer (M034+/M035+); tactic-verb weights
  (rejeitados — mais superfície de invenção); `name`,
  SKILL (ainda-cut, sem dono).

## 2. Grounding → decisão

- 5 votos explícitos do utilizador 2026-09-11
  (data-first; perfis; extend; matriz 6×10; aditivo)
  + #24 (6 nomes+epítetos) + #21 (cada Commander
  possui DOCTRINE) + #22 (influencia≠determina) +
  M032 (molde presets-votados + mirror +
  forward-placed) + M016/M027 + constraints.
- Números NUNCA inventados: matriz proposta por
  convenção explícita (neutro 0, primário ±25–30,
  ×5, clamp ±30) e APROVADA em voto dedicado
  antes do código.
- Deltas NÃO são DNA válido (conformidade provada
  por bounds-pin ±30, não por isDnaTraits).

## 3. Churn (declarado)

- `commanders.ts`: embed + union-mirror + guard
  (3.º mirror; single-line union per prettier);
  `commanders.test.ts`: battery + slot (hunks 109/
  198 HEAD-dirty intactos); zero churn resto.
- Bundle rebuildado (51079 chars, md5 idempotente ×3;
  só o mirror embarca — `siege-master` ×1, sem
  import doctrines, como prova).
- state.json/godot/sim/página intocados; sim idêntico
  (1200/500/0 avgLeft 340.0); verify-bundle SEM regen.
- Prettier: warns só em regiões HEAD-dirty (verificado
  por diff); D-027 reworded (leading-`+` vira vírgulas,
  molde D-026).
- `sim/report.json` (ruído ms) revertido, fora do commit.

## 4. Testes (18 novos → 1364/1364)

- `doctrines.test.ts` 8: vocabulário (2: lista-6 #24,
  trait-id) + deltas (4: pin escala ±30, golden matriz
  6×10 exacta, bounds ×5 ∀60, frozen) + lookup (2:
  fresh-copy, unknown→undefined).
- `commanders.test.ts` +10: embed (1+6+1+1: retro-compat,
  6 válidos, abuso fixo-6, slot round-trip + perceção)
  + cross-check mirror (1: six + battery + absent-ok).
- Bateria 1364/1364 (1346 intactos + 18, zero
  regressões); cobertura 100 global + doctrines.
- 1 `Math.abs` em teste (−0 vs +0 no `%`; prova da
  convenção ×5 se mantém); 1 cast `as unknown as`
  (molde M031).

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0 repo); verify-bundle +
  smoke + smoke:attack verdes; sim regressão limpa.
- RISKS: baixo — dados puros; residual: tuning matriz
  (#92); extensão fixa-6 quebra goldens (declarado);
  folha sem importadores.
- Commit: M033 Unit Doctrines VERIFIED (1364/1364,
  100 global). STOP.
