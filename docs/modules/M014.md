# M014 — Exploration (VERIFIED)

> Checkpoint M014 @ 2026-09-11 · 63 novos testes (649/649) · 100×4 ·
> memória = acumulação pura · D-007 escrita antes do código

## 1. Contrato

- Memória explored: `(memória + visão) → conjuntos canónicos por viewer`.
- Dados guardados: viewers PlayerId-shape + índices uint32 ascendentes únicos.
- `WorldState.explored?` opcional: exige mapa (explored⇒map), índices
  dentro dos limites.
- `markExplored` funde (união, admite viewers novos); queries fail-soft;
  tri-state `visible/explored/unexplored` (visão > memória > escuridão).
- Pós-invariante `explored-monotonic`: viewers persistem, conjuntos só
  crescem; wipe/shrink/removal = HANDLER_FAULT.

## 2. Pré-análise (decisões)

- D-007 precede a implementação (análise-antes-código, em `DECISIONS.md`).
- Guard no L0 leaf (`explored.ts`, zero imports): `world-state` (L1) só
  importa para baixo (lei de layering M009).
- Regra monótona = máximo exigível agora; anti-forge precisa de fontes
  stateful (residual L-28, M021+).
- Viewers persistem além de futura eliminação (M023 pode revisitar).
- `VisibilitySets` gémeo estrutural de `VisibilityResult` (sem aresta
  L2→L2; compatibilidade provada por teste de integração).

## 3. Implementação

- `src/engine/explored.ts` (novo, L0): `ExploredData` + `isExploredData` +
  `isCellIndex`, forma canónica ascendente-única.
- `src/engine/exploration.ts` (novo, L2): `markExplored` + queries +
  `explorationStatus`; imports `explored` + `authority` (DAG intacto).
- `world-state`: campo `explored?` + coerência explored-requires-map +
  bounds; `validation`: 5ª pós-invariante.
- `validation.test` 4→5 (composição declarada); `phase1-gate` census 16 +
  `explored: 0`/`exploration: 2` (função pretendida do tripwire).

## 4. Testes (6 categorias)

| Categoria    | Onde                    | O quê                                         |
| ------------ | ----------------------- | --------------------------------------------- |
| Unit         | guard + queries + tri   | 36 shapes; queries; 6 ramos tri-state         |
| Integração   | mark + WorldState + fog | goldens fusão/acumulação; feed fog→mark       |
| Falha        | guards + mark loud      | viewers/índices/ordem/versão; OOB; sem-mapa   |
| Segurança    | monotonic + views       | wipe/shrink/swap/removal; FAULT E2E; non-leak |
| Determinismo | run×2 + goldens         | saídas literais; inputs nunca mutados         |
| Regressão    | suite 649/649           | M001–M013 intactos (586 testes, tripwire)     |

## 5. Third-Eye (ataques)

- Viewer vazio/longo, índice negativo/float/overflow, ordem não-canónica,
  versão errada — tudo rejeitado pelo guard.
- Explored sem mapa, índice OOB, swap mesmo-tamanho ([0,4]→[0,5]) —
  apanhados pelo guard/regra.
- Wipe/shrink via handler real: HANDLER_FAULT, estado/revisão/log
  intactos (E2E, não só wrapper directo).
- Espelho leaf: `MAX_VIEWER_ID_CHARS` falha loud se divergir de
  `MAX_ID_LENGTH` (cross-check em teste).
- Residual assumido: handlers podem forjar visão sem fonte (L-28, M021+).

## 6. Gate

- format/lint/typecheck/test/build PASS; 649/649; 100/100/100/100.
- `dist/` com 64 ficheiros engine, 0 testes (contado, não derivado);
  fence: 2 novos + world-state/validation/testes/gate declarados.

## 7. Limitações declaradas

- L-28: anti-forge residual até fontes-owned stateful (M021+).
- L-27: viewers sem membership-check (M015 enforce no consumo).
- Sem produtores/handlers, sem mudanças em vistas (M015); sem semântica
  de eliminação-memória (M023 pode revisitar).
- N/A (§2 TESTING.md): rede/persistência (M062/M069+), SIMULTÂNEO/RACE
  (serial, M086), verificação manual (automatizado).
- L-09/L-12/L-13/L-15/L-16/L-17/L-18/L-19/L-20/L-21/L-22/L-23/L-24/L-25/L-26/L-27
  mantêm-se.

## 8. Próximo

M015 Views/Perception: enforcement no consumo (morte do `secrets`;
L-27 fecha); depois fontes-owned (M021) fecham L-28.
