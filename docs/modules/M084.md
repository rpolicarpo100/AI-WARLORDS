# M084 — Validation audit (D-079)

- Status: VERIFIED+LIVE (2026-09-12)
- Verdictos: timeouts node
  default OK; isSeed/from/
  malformed/URL OK; deep JSON
  non-issue (iterativo+cap);
  dots resolvem no parser
  (Map, sem fs — 200 honest,
  não 404).
- Pins: 4 (dots→/ratings 200,
  PUT 404, giant-rid
  MALFORMED exacto, deep40k
  200); suite 1931/1931
  100×4; zero src changes.
- Bundle SAME md5; API
  redeploy (testes only).
