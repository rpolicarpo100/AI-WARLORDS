# M049 — Counterfactual Query — VERIFIED

Vote: `sim-query` (2026-09-11). Decision: D-043.
First of the Counterfactual AI pair (M049–M050)
closing the Refutation → Counterfactual block
(M046–M050): "what would confidence say after
X" — one hypothetical, honestly simulated.

## What it ships

- NEW `src/engine/counterfactual.ts` (L3,
  read-only): `whatIfConfidence(record, index,
  state, hypothetical, deps)` — target?
  (fail-soft) → orderable? (else 'not
  orderable') → rule+handler wired? (else
  'unknown kind') → rule passes? (else detail
  verbatim) → live handler over a
  structuredClone fork (caller = holder, M029
  seam; verb rules shape-only) → rejected?
  (reason verbatim) → `confidenceOfOrder` on
  the OUTCOME state. No prompts/events/
  timeline; no exposed fork; no advance
  (declared cut). Deps injected (M045 mold):
  handlers + rules + confidenceFor (per-state
  factory — snapshot-bound treasury rebinds
  to the outcome; fixed rules priced the
  original funds, caught at the gate).
- Match: `whatIf` (mold confidenceOf) +
  domainHandlers/domainRules fields + private
  confidenceRules(snapshot) refactor.
- Pins: census `counterfactual.ts` + LAYERS
  `counterfactual: 3`.

## Result (frozen)

- Gate: 1634/1634 (1613 intact + 21: 14
  counterfactual + 7 match-counterfactual),
  100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 3 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `85a2132a…`): 70116 chars (+938 —
  `counterfactual` ships + Match adaptation);
  `counterfactual: not orderable` ×1 in the
  bundle; `state.json` unchanged, `match.html`
  re-embedded.

## Files

- NEW: `src/engine/counterfactual.ts`,
  `src/engine/counterfactual.test.ts`,
  `src/engine/match-counterfactual.test.ts`
- EDIT: `src/engine/match.ts`,
  `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-043),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
