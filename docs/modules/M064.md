# M064 — Replay Stepping — VERIFIED

Sem voto (2026-09-12): D-056+D-057 declared
this contract; molds decided the
micro-rules. Decision: D-058. LAST of the
Replay block (M062–M064) — the block CLOSES
here. Frame-by-frame playback over journals
(incremental O(n), live match per frame).

## What it ships

- `createReplayStepper(init, journal)` →
  {match, lance, total, step()} (step →
  outcome | null at end; trusts valid
  journals — M063 owns validity).
- Zero new files → zero pins. Stepper
  tree-shaken from the bundle (zero mockup
  readers — M063 precedent; UI wires).

## Result (frozen)

- Gate: 1828/1828 (1825 intact + 3:
  full-step + mid-frame + empty), 100×4
  global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 4 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `bdd80be5…`): 88550 chars (+0 — shaken);
  `state.json` unchanged, `match.html`
  re-embedded (same size).
- Infra: leaked vitest/tsc node workers
  ate all 2GB mid-gate (tsc OOM + hangs);
  `pkill -9 -x node` freed 1.5GB, gate
  re-verified clean.

## Files

- EDIT: `src/engine/match.ts`,
  `src/engine/match-replay.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-058),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
