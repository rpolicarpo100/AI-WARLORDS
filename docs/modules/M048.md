# M048 — Confidence Engine — VERIFIED

Vote: `reason-eval` (2026-09-11). Decision: D-042.
Late middle of the Refutation → Counterfactual
block (M046–M050): the M046 reasons become
executable — pure queries over live state.

## What it ships

- NEW `src/engine/confidence.ts` (L2,
  read-only): `confidenceOfOrder(record,
  index, state, rules)` evaluates the five
  M046 reasons against live state + composite
  score (100 − 20 per failure, floored at 0;
  canonical `failed` order). Evaluators:
  blocked (M022 passable), out-of-range
  (neighborsOf, melee-only), redundant
  (same-cell; empty/resourceless node),
  suicidal (DOCUMENTED heuristic, stance M037
  precedent — net zero OR hp within one
  enemy hit, pricing the foe's next turn),
  unaffordable (treasury truth). Every
  evaluator abstains on malformed input
  (validity belongs to the engine).
  Rules injected (warfareHandlers mold):
  unitStatsOf/buildCostOf/passable/defenseOf/
  canAfford (structural twins — L2↛L2).
- Match: `confidenceOf(id, index)` (mold
  assessmentOf; isUnitType/isBuildingId
  guards, fail-soft undefined, no treasury
  means unaffordable) + private
  buildings/terrainConfig.
- Pins: census `confidence.ts` + LAYERS
  `confidence: 2`.

## Result (frozen)

- Gate: 1613/1613 (1568 intact + 45: 33
  confidence + 12 match-confidence), 100×4
  global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 3 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `3d311857…`): 69178 chars (+3446 —
  `confidence` ships + Match adaptation);
  `confidenceOf` ×1 in the bundle;
  `state.json` unchanged, `match.html`
  re-embedded.

## Files

- NEW: `src/engine/confidence.ts`,
  `src/engine/confidence.test.ts`,
  `src/engine/match-confidence.test.ts`
- EDIT: `src/engine/match.ts`,
  `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-042),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
