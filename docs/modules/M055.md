# M055 — Directive Data — VERIFIED

Vote: `directive-data` (2026-09-12). Decision: D-049.
Opener of the Player ↔ Commander block
(M055–M058): data-first foundation (M046/M051
mold), player-to-commander half. Directives
are standing player instructions beyond the
order queue.

## What it ships

- NEW `src/engine/directives.ts` (L0, zero
  imports): `DIRECTIVE_KINDS` closed
  (autonomy, stance) + `AUTONOMY_LEVELS`
  closed (manual, assisted, autonomous) +
  stance values mirror STANCE_IDS (L0↛L0)
  - `CommanderDirectives` {autonomy?,
    stance?} (per-kind SET: setting
    overwrites; absent means unset = status
    quo) + total per-kind guards (extras
    ignored). Semantics (mechanics M056+):
    autonomy names who decides; stance names
    a player-ordered posture override.
- Mirror in `commanders.ts` (L0↛L0 law):
  `directives?` + mirror guard + copyRecord
  spread.
- Pins: census `directives.ts` + LAYERS
  `directives: 0`.

## Result (frozen)

- Gate: 1746/1746 (1730 intact + 16: 14
  directives + 2 commanders mirror), 100×4
  global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 4 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `261e0481…`): 76169 chars (+428 —
  canonical tree-shaken, zero readers; mirror
  ships); `directives` ×5 in the bundle;
  `state.json` unchanged, `match.html`
  re-embedded.

## Files

- NEW: `src/engine/directives.ts`,
  `src/engine/directives.test.ts`
- EDIT: `src/engine/commanders.ts`,
  `src/engine/commanders.test.ts`,
  `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-049),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
