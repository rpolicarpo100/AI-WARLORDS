# M063 — Export Blob — VERIFIED

Sem voto (2026-09-12): D-056 declared this
contract; molds decided the micro-rules.
Decision: D-057. SECOND of the Replay block
(M062–M064): match histories serialize to
portable JSON blobs (init + journal) and
import back strictly (export → import →
replay identical).

## What it ships

- `exportReplayBlob(init, journal)` (strips
  test seams) + `importReplayBlob(json)`
  (throws on malformed JSON / bad shape) +
  strict `isReplayBlob` (version, journal
  array, 4 envelope strings, outcome
  shapes incl recursive duplicate + 6
  error codes; init passes through —
  Match owns game-validity).
- Zero new files → zero pins. Blob fns
  tree-shaken from the bundle (zero mockup
  readers — M055 precedent; M064 wires).

## Result (frozen)

- Gate: 1825/1825 (1820 intact + 5:
  mixed round-trip + duplicate + strip +
  empty + 24-case battery), 100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 4 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `5473d5d4…`): 88550 chars (+0 — shaken;
  only minifier rename churn `xo`→`ko`);
  `state.json` unchanged, `match.html`
  re-embedded (same size).

## Files

- EDIT: `src/engine/match.ts`,
  `src/engine/match-replay.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-057),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
