# M034 — Personalidade Testing (VERIFIED @ 2026-09-11)

> Fecha o bloco Personalidade (M031–M034). Módulo PURO
> DE TESTES (âmbito VOTADO): drill de personalidade no
> sim (commanders com triples em Matches reais + 7
> invariantes) + suite transversal (journey do bloco +
> determinismo). Zero produção.

## 1. Âmbito (contrato)

- `sim/personality-drill.ts` (toolchain, fora do
  coverage — M026): Matches reais × seed; init c0/c1
  triples distintos (init-placed — writers FUTURO);
  bots commission+flips+noop (oráculo=engine; blind
  flip c999999 rejeita loud); 7 invariantes
  pós-applied (guard live; canonical agreement;
  triple-estável; minted-sem-triple; ids/nextId;
  JSON round-trip; perceção sem-leak/sem-loss);
  report próprio (determinístico exceto ms; exit 1
  se violations); script `sim:personality`.
- `src/engine/personality.test.ts` (2, molde
  military.test.ts): journey commission×2→flips×4
  (triples intactos; minted bare; events
  commander.* ×6; perceção own-only; guards
  canónicos live) + gémeos determinismo.
- Zero prod; bundle byte-idêntico; selo phase1-gate
  intacto (sem pins — sem ficheiro prod novo).
- Cortes (D-028, âmbito votado): estender playtest
  (golden 1200/500/0 intacto); executar composição
  (D-027 reserva M035+); 3.º teste rejects
  (commander-state.test cobre); thresholds (sem
  âncora — M026).

## 2. Grounding → decisão

- Voto harness 2026-09-11 + D-023
  (commission-sem-params; roster uncapped) + D-027
  (triple init-placed; composição→M035+) + M026
  (molde módulo-testes) + TESTING §1B + M027/M031
  (perceção own-only).
- 1 invariante corrigido em voo (bug do harness,
  não do engine): `active` flipa legitimamente —
  estabilidade cobre só o triple (payload dna/
  personality/doctrine).

## 3. Churn (declarado)

- `sim/*` (drill + personality-report.json novos);
  `package.json` (+1 script); `personality.test.ts`
  novo; D-028.
- personality-report.json determinístico sans-ms
  (provado ×2); report.json/balance.csv intocados
  (sim golden idêntico 1200/500/0 avgLeft 340.0).
- state.json/godot/página intocados; verify-bundle
  SEM regen; bundle nem rebuildado (md5 6235d684
  intacto — rebuild de prova idempotente).
- Prettier: `--write` só nos 2 ficheiros novos
  (inteiramente meus); D-028 limpo; dirt
  pré-existente intacto.
- `sim/report.json` (ruído ms) revertido, fora do commit.

## 4. Testes (2 novos → 1366/1366 + drill)

- Suite: journey do bloco + determinismo gémeo (2/2).
- Drill 20×60: 1200 applied, 240 rejected,
  **0 violations**; 315 commissions, 556 flips.
- Bateria 1366/1366 (1364 intactos + 2, zero
  regressões); cobertura 100 global intacta.
- 1 filter `startsWith('commander.')` (molde military
  filtra `unit.` — `match.started` lidera eventos).

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0 incl. sim);
  drill exit 0; verify-bundle + smoke + smoke:attack
  verdes sem tocar.
- RISKS: baixo — só testes; residual: bots maus
  estrategas (irrelevante — M026); drill fora do
  tsc (rede: eslint + execução).
- Commit: M034 Personalidade Testing VERIFIED
  (1366/1366, 100 global). BLOCO M031–M034 CLOSED.
