# M051 — AI Memory Data — VERIFIED

Vote: `memory-data` (2026-09-12). Decision: D-045.
Opener of the Memory block (M051–M054):
data-first foundation (M046 mold). A memory
references one emitted stream event the
commander lived through.

## What it ships

- NEW `src/engine/memories.ts` (L0, zero
  imports): `MEMORABLE_KINDS` closed
  (alphabetical, locked) — order.canceled,
  order.executed, order.overridden,
  unit.attacked, unit.slain, unit.spotted
  (all REAL emitted `type:` values, grounded
  against producers 2026-09-12) +
  `CommanderMemory` {seq uint32, revision
  uint32, kind, subject (id 64)} + total
  guards (extras ignored) + `isMemoryLog`
  (capped 8, M044 queue mold).
  Self-validating reference: seq locates the
  event, revision+kind confirm (mismatch=stale
  → M052+ fail-closed). Zero transitions,
  zero readers until M052+.
- Mirror in `commanders.ts` (L0↛L0 law):
  `memories?` (absent means empty, capped 8)
  - mirror guards + copyRecord spread-each.
- Pins: census `memories.ts` + LAYERS
  `memories: 0`.

## Result (frozen)

- Gate: 1673/1673 (1650 intact + 23: 20
  memories + 3 commanders mirror), 100×4
  global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 4 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `bd1e3324…`): 71772 chars (+569 —
  canonical tree-shaken, zero readers; mirror
  ships); `memories` ×5 in the bundle;
  `state.json` unchanged, `match.html`
  re-embedded.

## Files

- NEW: `src/engine/memories.ts`,
  `src/engine/memories.test.ts`
- EDIT: `src/engine/commanders.ts`,
  `src/engine/commanders.test.ts`,
  `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-045),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
