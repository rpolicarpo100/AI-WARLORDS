# M076 — Online close (D-070)

- Status: VERIFIED+LIVE (2026-09-12)
- Panel: botão Close → POST
  /:id/close → `table closed` +
  abort watch + limpa
  match/session (guard matchId);
  mesas fechadas somem do lobby
  (probado por curl).
- Tests: suite 1898/1898 100×4;
  base smoke clica netClose
  (no-crash); API_TEST agora faz
  o lifecycle inteiro na UI:
  lobby→forge→join→noop→attack→
  move→state→close→lobby vazio
  (raw-fetch final removido).
- Bundle untouched (91336, SAME
  md5); `match.html` re-embedded.
- FIM DO BLOCO M065–M076:
  multiplayer jogável de ponta
  a ponta (API live + page client).
