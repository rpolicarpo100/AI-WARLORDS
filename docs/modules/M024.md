# M024 — Damage Resolution (VERIFIED @ 2026-09-11)

> `net = max(0, damage − defesa)`; morte = remoção no próprio
> dispatch; facto `unit.slain` NORMAL; cura adiada sem mecânica (D-017).

## 1. Âmbito (contrato)

- `DefenseOfTerrain = (terrain: string) => number` em `warfare.ts` L2
  (predicado injectado pelo Match — precedente M022 `passable`;
  L2↛L2 impede importar terrain).
- `createAttackHandler(unitsConfig, defenseOf)`: defesa do terreno da
  célula do alvo; `net = max(0, damage − defense)` (aprovado);
  `hp = max(0, foe.hp − net)`; hp 0 → alvo removido (`filter`;
  `nextId` intacto); summary `attacked T for NET (hp A→B)[, slain]`.
  Adjacência captura a célula (`find` ≡ `!some`; lookup infalível —
  neighborsOf só devolve células reais, sem branch morto).
- `warfareHandlers(passable, unitsConfig, defenseOf)` (append,
  precedente M023); Match injecta closure do terrainConfig.
- `attackProducer` emendado (seam M023): alvo ausente no after →
  `[unit.attacked (damage = was.hp, diff genuíno), unit.slain NORMAL
  {player, unit, target}]` (cunhagem + NORMAL aprovadas).
- 9 rejects intactos (`unit/target down.` via 0hp inicial/crafted —
  isWord permite 0); dano 0 aplica (precedente M023).
- Sem regra nova (composição 5+1); sem ficheiro novo; sem bump
  ruleset (precedente M022); cenário intocado (sem ataques —
  outputs idênticos, verify-bundle verde).
- Cortes (D-017): cura mecânica (sem fonte — adiado, user aprovou);
  retaliação/armadura (M025+, sem config/âncora); sweep de 0hp (sem
  âncora — só o alvo); move intacto (M022 owns).

## 2. Grounding → decisão

- D-014 (`dano`→M024) + D-016 (remoção + seam produtor) + M011
  (defense existe sem consumidor; tooltip mentia) + M022 (molde
  predicado) + aprovação explícita do utilizador 2026-09-11
  (fórmula, `unit.slain` NORMAL, cura adiada).
- Removidos viram `unknown target.` (honesto: já não existem).

## 3. Churn (declarado)

- Assinatura warfareHandlers 2.ª vez (match.ts + testes); 3 testes
  M023 reescritos (overkill→remoção; barrage→kill+r4 `unknown
  target.`+slain ×1; produtor missing-after→slain); comentário
  terrain "M023"→"M024" (erro factual).
- 12 `createAttackHandler` + 3 `warfareHandlers` ganham stub
  `() => 0` (números M023 intactos, fixtures field).
- verify-bundle + smoke/attack verdes sem tocar (u4 em river,
  defesa 0 — números idênticos); bundle+match.html rebuildados
  (FASE B intacta, 14 refs).

## 4. Testes (6 novos → 1102/1102)

- `warfare.test.ts` +6: invalid defense (handler; warfareHandlers
  folded), spy de terreno (field→net 3), net 0 aplica, exact kill
  (remoção+nextId+resto), slain golden (attacked 12 + slain),
  montanha E2E (defesa 2 real via Match: net 2, facto damage 2).
- Cobertura 100×4 (warfare.ts + match.ts 100); selo intacto.

## 5. Gate e fecho

- Bateria 1102/1102 (1096 intactos + 6); statics verdes.
- RISKS: baixo — resolução pura + gate estrito; residual: 0hp
  inicial move (M022 owns, fora de âmbito); ids mortos → `unknown`.
- Commit: M024 Damage Resolution VERIFIED (1102/1102, 100×4). STOP.
