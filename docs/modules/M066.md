# M066 — AI Arena — Smarter Brains — VERIFIED

Voto `smarter-brains` (2026-09-12):
D-060. SECOND of the AI Arena block
(M065–M068) — the v2 brain behind the
same signature (the runner is untouched):
critical units flee, attacks focus fire,
workers seek live nodes.

## What it ships

- FLEE: hp ≤ 2 (CRITICAL, explicit —
  the snapshot carries no damage config)
  with an adjacent foe steps where the
  nearest-foe gap strictly grows (ties
  col/row); cornered units fight.
- ATTACK: focus fire (lowest hp, tie id).
- Worker SEEK: off-node workers step
  greedy toward the nearest live node
  (ties col/row); depleted-only maps
  fall back to closing in; brawl still
  beats seek, flee beats brawl.
- Provenance: a probe test printed the
  full 3×3 neighbor table first — every
  scenario cell is table-picked (probe
  deleted after; zero guessed geometry).

## Result (frozen)

- Gate: 1845/1845 (1839 intact + 6:
  focus + seek + depleted + brawl +
  flee + cornered), 100×4 global. All
  11 M065 tests survive untouched (old
  behavior = special case of v2).
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS (+attack), 4 sim goldens
  byte-identical (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `69e032b7…`): 88999 chars (+0, SAME md5
  — v2 fully shaken, prod untouched);
  `match.html` not re-embedded (identical
  bytes).

## Files

- EDIT: `src/engine/selfplay.ts`,
  `src/engine/selfplay.test.ts`,
  `docs/DECISIONS.md` (D-060),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
