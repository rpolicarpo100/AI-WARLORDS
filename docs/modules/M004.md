# M004 — World State

> STATUS: `VERIFIED` (2026-09-10)
> Checkpoint: M004 @ 2026-09-10 — official state + enforced views; 122/122; 100×4

---

## 1. Contract (§6, final)

| Campo               | Valor                                                                                                           |
| ------------------- | --------------------------------------------------------------------------------------------------------------- |
| MODULE_ID           | M004                                                                                                            |
| MODULE_NAME         | World State                                                                                                     |
| PURPOSE             | Criar o estado oficial versionado + impor por construção a separação WORLD_STATE ≠ AI_PERCEPTION ≠ CLIENT_STATE |
| DEPENDENCIES        | M003 = VERIFIED (satisfeita). Ferramentas avaliadas (TOOLS.md) informam extensibilidade                         |
| INPUTS              | Kernel M003; decisão de ferramentas (shortlist Tiled/LDtk, gate M010)                                           |
| OUTPUTS             | `world-state.ts` + `views.ts` + 2 suites (40 testes); `freezeState` exportado; `docs/TOOLS.md`; docs            |
| FILES_AFFECTED      | 6 criados + 6 docs (1 registo novo, 5 actualizados) + 1 refactor export-only                                    |
| DATABASE_CHANGES    | none                                                                                                            |
| API_CHANGES         | none (in-process; transporte em M069+)                                                                          |
| SECURITY_IMPACT     | HIGH (positivo): redacção provada, guards, freeze, no-leak scans                                                |
| PERFORMANCE_IMPACT  | negligenciável; clone+freeze por vista anotado (medir M088+)                                                    |
| TEST_REQUIREMENTS   | cumpridos: 6 categorias, 100×4 sem exclusões, zero mocks, M002+M003 verdes                                      |
| ACCEPTANCE_CRITERIA | todos PASS (ver §5) + fence (ver §3)                                                                            |
| KNOWN_RISKS         | R-12 mantém FUTURE (nota); R-54 criado (L-08 → M006)                                                            |
| ROLLBACK_STRATEGY   | `git revert` do commit M004; sem dados/migrações                                                                |
| STATUS              | VERIFIED                                                                                                        |

## 2. Module analysis (§7 — escrita ANTES da implementação)

Separar 3 vistas exige segredo demonstrável → placeholder rotulado
(precedente: harness M003). Mutações: ZERO por design (todas pertencem a
módulos de domínio). Vistas wrapped com kind (separação nominal que o
transporte/AI não conseguem confundir). Reutilizar `freezeState` do M003
(sem duplicar o primitivo). Validação single-path: `create` constrói
ATRAVÉS do guard `isWorldState` (construção e loaders nunca divergem).

## 3. Scope fence — explicitamente NÃO é este módulo

| Tema                        | Onde vive       | Posição M004                                                       |
| --------------------------- | --------------- | ------------------------------------------------------------------ |
| Tick advancement / timeline | M005            | campo `tick` existe; M004 nunca avança (testado)                   |
| Validador central           | M006            | envelope=shape (M003); regras=handlers; M006 endurece outputs+caps |
| Eventos / vitória / score   | M007/M008/M077+ | não existem                                                        |
| Mapa / terreno / recursos   | M010–M012       | schema v1 sem mapa; M010 estende via bump versionado               |
| Fog / percepção inferida    | M013/M015/M028  | redacção mecânica; `known`==visível (declarado)                    |
| Economia / militar          | M016+           | nada                                                               |
| Formato de mapa             | M010 gate       | shortlist Tiled/LDtk em TOOLS.md; zero ficheiros de mapa           |

## 4. Implementation (só M004)

- `world-state.ts`: `WORLD_SCHEMA_VERSION=1`, `WorldState`
  (schema/tick/players/secrets-placeholder), `isWorldState` (guard único,
  12 invariantes), `createWorldState` (via guard), `worldHandlers`
  (`world.noop` só — comentário fence no código).
- `views.ts`: `toWorldView` (identidade server-only), `toClientView` /
  `toAiPerception` (clone+redact+freeze, wrapped com kind).
- `authority.ts`: rename+export `deepFreeze`→`freezeState` (4 ocorrências,
  verificado por grep). JUSTIFICAÇÃO: reutilizar o primitivo provado em
  vez de duplicar. Regressão M003 re-corrida e verde.
- `docs/TOOLS.md`: 7 ferramentas verificadas com fontes; shortlist + gate.

## 5. Testing (§9–§10) + Gates

| Gate                              | Resultado                                                    |
| --------------------------------- | ------------------------------------------------------------ |
| TEST                              | 122/122 (82 + 40), 100×4 em 6 ficheiros, thresholds enforced |
| LINT / TYPECHECK / BUILD / FORMAT | PASS (0/0, strict, dist sem testes)                          |
| INSTALL                           | package.json/lock INTocados (zero novas deps)                |
| REGRESSION                        | M002+M003 intactos e verdes (incl. após refactor)            |
| MOCKS                             | ZERO                                                         |

Negativos §10: 20 envelopes de estado inválidos, inits inválidos, vistas
p/ jogadores sem segredos, mutações (throw), aliasing (clone-first),
leakage scans JSON, interleave herdado. N/A: timeout/rede/transporte
(inexistentes); race (vistas puras, sem estado partilhado).

## 6. Correction log (§16)

Zero falhas de testes. 2 ciclos typecheck (aridade `it.each` nos MEUS
testes — corrigido e simplificado) + 1 normalização prettier + retest
total. Nada escondido.

## 7. Audit — Third Eye (§18, resumo)

1. Funciona? Sim — 40 testes, asserts exactos, redacção/no-leak/freeze.
2. Simulação? Não. Zero mocks; placeholder ROTULADO.
3. Deps inventadas? Não. Export + builtins; ferramentas com fontes,
   UNKNOWNs declarados (licenças Azgaar/Watabou).
4. Vulns? Redacção adversarial; clone-antes-freeze; guards; sem rede.
5. SPOF? N/A. 6. Race? Vistas puras; kernel serial herdado.
   7–8. Manipulação/duplicação? Vistas são só saída; envelope inalterado.
6. Abuso? L-08/R-54 registados (estado unbounded → caps em M006).
   10–12. LLM/API/fallback? Sem deps. ✓
7. Observabilidade? schema version rastreável; log herdado.
8. Rollback? `git revert`.
   15–16. Complexidade? Guard hand-rolled justificado (sem nova dep);
   kinds = 3 linhas contra confusão cliente/AI.
9. Fundação? Sim; fence respeitado.

ALL: PASS.

## 8. Result

```text
STATUS: VERIFIED
```

## 9. Files changed (commit M004)

```text
+ src/engine/world-state.ts, src/engine/views.ts
+ src/engine/world-state.test.ts (30), src/engine/views.test.ts (10)
+ docs/TOOLS.md, docs/modules/M004.md
~ src/engine/authority.ts (export-only: deepFreeze→freezeState, 4 occ)
~ docs/MODULE_STATUS.md, docs/ARCHITECTURE.md, docs/RISK_REGISTER.md
~ README.md, docs/DEPENDENCIES.md
= package.json/lock INTocados · TECH_STACK/TESTING/PROJECT_AUDIT sem motivo
```

## 10. Test results (finais)

122 passed / 0 failed / 0 skipped · 100×4 · lint/typecheck/build/format PASS.

## 11. Known issues / limitações honestas

- L-08 (R-54): estado canónico unbounded por construção; handlers futuros
  que escrevam dados impõem caps (M006). Hoje: nenhum handler escreve.
- Placeholder `secrets` TEM de ser substituído/removido até M015.
- Sem mutações de mundo (por design); sem mapa (M010); sem fog (M015).

## 12. Constraints para módulos futuros (adicionam aos M003 §12)

1. Vistas cliente/AI saem SÓ de toClientView/toAiPerception — transporte
   nunca serializa WorldState directo (M069).
2. Handlers com escrita impõem caps (L-08/R-54) — M006.
3. Bump de schema (ex. M010) = migração explícita + testes (guard é o gate).
4. Não converter o placeholder `secrets` em mecânica (morrer até M015).

## 13. Next dependency

M005 — Deterministic Match (PLANNED). Pré-requisito: autorização explícita.
