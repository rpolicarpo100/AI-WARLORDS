# M067 — AI Arena — Scoring — VERIFIED

Sem voto (2026-09-12): D-061. THIRD of
the AI Arena block (M065–M068) — pure
arena scoring plus selfplay reporting
it (victory/draws deliberately
untouched: score-decided wins would
ripple M060 + the gate).

## What it ships

- `scorePlayer(state, player)` (L2,
  type-only imports): living hp +
  stockpile + city levels + building
  counts, v1 all-ones weights (no tuned
  constants without data); missing
  sections/holders score zero.
- `rankPlayers` (score desc, ties by id)
  - `scoreTable` (whole roster).
- `Match.selfplay` returns final `scores`
  (finished AND stalled paths); derived,
  never stored — replay-safe for free.
- Pins: census + LAYERS `score: 2`
  (world-state L1 forces L2, strictly
  downward).

## Result (frozen)

- Gate: 1850/1850 (1845 intact + 5:
  zero + flesh + wealth + combined +
  rank), 100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS (+attack), 4 sim goldens
  byte-identical (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `4874e587…`): 89544 chars (+545 —
  scoreTable rides the retained static);
  `state.json` unchanged, `match.html`
  re-embedded.

## Files

- NEW: `src/engine/score.ts`,
  `src/engine/score.test.ts`
- EDIT: `src/engine/match.ts`,
  `src/engine/match-replay.test.ts`,
  `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-061),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
