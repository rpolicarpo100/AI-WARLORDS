# M073 — Page client (D-067)

- Status: VERIFIED+LIVE (2026-09-12)
- Panel: Online em match.html —
  `apiUrl` (default
  `https://ai-warlords-api.onrender.com`),
  `netSide` p1|p2, Lobby / Forge /
  Join / Noop / Attack / Stop,
  log `netOut`. Erros → netLog
  (jamais throw); guards sem
  sessão (`join first`, `forge or
  pick a table first`).
- SSE via fetch+ReadableStream
  (não EventSource — corre no
  node, testável); Stop =
  AbortController.
- Tests: suite 1898/1898 100×4
  (sem engine novo); base smoke
  clica tudo c/ API inalcançável
  (no-crash); `API_TEST=1` sobe
  serve.ts real (tsx) e prova
  lobby→forge→join→noop→attack→
  SSE→stop→close (~3s, sem órfãos).
- Harness: stubs performance/
  setTimeout agora só params IIFE
  (global real — undici precisa);
  server child `detached` +
  group-kill + pipe destroy.
- Bundle untouched (91336, SAME
  md5); `match.html` re-embedded
  (page client novo).
