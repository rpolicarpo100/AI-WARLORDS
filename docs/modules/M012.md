# M012 — Resources (VERIFIED)

> Checkpoint M012 @ 2026-09-11 · 39 novos testes (525/525) · 100×4 ·
> nós como geografia · RISK_REGISTER reparado (dup committed em M011)

## 1. Contrato

- Nós de recursos colados às células: `{type, amount}` (uint32; 0 = esgotado).
- Vocabulário fechado #14 (food/wood/stone/gold, máx 4).
- Coerência dupla: detalhe ⟺ terreno `resource` (guard + loader).
- Loader: props `resourceType`+`resourceAmount` obrigatórias em tiles resource,
  proibidas noutros (strict-subset M010).
- Queries: `resourceAt`, `resourceNodes`, `totalResource`; nós sob
  `map-preserved` + map-blindness herdados.

## 2. Pré-análise (decisões)

- Nós DENTRO de `MapCell` (extensão antecipada por M010), não sidecar:
  geografia junta, `mapRule` cobre sem mexer validação.
- Vocabulário em `map.ts` (restrição leaf L0); queries em `resources.ts` (L2).
- Amounts por TIPO de tile no load (Tiled: props são por tile); células
  divergem após gathering (M017). 0 aceite (esgotado válido).
- `totalResource` soma 0 p/ tipo desconhecido (agregação, não lookup —
  lookups é que são fail-stop).
- Builder de fixtures próprio (scaffolding de teste local > dedup;
  produção com zero duplicação).

## 3. Implementação

- `src/engine/map.ts` (4 emendas): vocabulário, `MapCell.resource`,
  coerência no guard, props no loader (omit-when-absent: bytes M010 intactos).
- `src/engine/resources.ts` (novo, L2): `resourceAt`, `resourceNodes`,
  `totalResource` — só leitura.
- `phase1-gate` (census 13 + `resources: 2`). Mais nada tocado.

## 4. Testes (6 categorias)

| Categoria    | Onde                          | O quê                                    |
| ------------ | ----------------------------- | ---------------------------------------- |
| Unit         | `resources.test.ts:74,93,215` | vocabulário, coerência, queries          |
| Integração   | `resources.test.ts:133`       | loader exact-load + omit-absent          |
| Falha        | `resources.test.ts:93,133`    | 19 rejeições (guard + loader)            |
| Segurança    | `resources.test.ts:240`       | mutação→preserved, blindness ×2, started |
| Determinismo | `resources.test.ts:133`       | exact-load golden estrutural             |
| Regressão    | suite 525/525                 | M002–M011 intactos (486 testes, 0 edits) |

## 5. Third-Eye (ataques)

- Coerência: detalhe-em-field, resource-sem-detalhe, tipo `lava`, amounts
  float/negativo/overflow/texto/nulo/array/primitivo — tudo rejeitado.
- Loader: props-em-field (type-only, amount-only, ambas), props em falta,
  tipo/amount inválidos — loud com `loadMapData`.
- Mutação de amount via handler → `map-preserved`; vistas sem `map`.
- M010 green sem edits (bytes antigos intactos — omit provado).

## 6. Gate

- format/lint/typecheck/test/build PASS; 525/525; 100/100/100/100.
- `dist/` com 39 ficheiros engine, 0 testes; fence: só `map.ts` + tripwire
  além dos 2 ficheiros novos.

## 7. Limitações declaradas

- L-23: amounts por tipo de tile no load (granularidade Tiled); per-cell
  só após gathering (M017).
- Sem gathering/stockpiles/valores (M016/M017); sem conteúdo; sem mecânicas.
- N/A (§2 TESTING.md): rede/persistência (M062/M069+), SIMULTÂNEO/RACE
  (serial, M086), verificação manual (automatizado).
- L-09/L-12/L-13/L-15/L-16/L-17/L-18/L-19/L-20/L-21/L-22 mantêm-se.

## 8. Próximo

M013 Fog of War: visibilidade sobre células (WORLD→AI/CLIENT filtrado);
depois M014 (exploração) e M015 (percepção).
