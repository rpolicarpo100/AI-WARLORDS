# M069 — Multiplayer — SSE+POST Transport — VERIFIED

Voto `sse-post` (2026-09-12): D-063.
FIRST of the Multiplayer block
(M069–M076) — a real zero-dep HTTP
surface over genuine Matches (the
long-deferred "don't assume WS/REST"
decision, decided: SSE+POST).

## What it ships

- `src/server/transport.ts` (node:http):
  POST /match {seed?} (fixed 2p
  skirmish, server-side everything);
  POST /:id/join → session; POST
  /:id/dispatch (caller derived from
  the session — spoof-proof); GET
  /:id/events?from=N (SSE backlog +
  live, headers flushed); GET /:id/state.
  200 + outcome for well-formed (domain
  errors ride outcomes); 400/404/500
  for wire faults (throws → 500, per
  the engine's own M069 directive).
- 26 tests, all over real localhost
  HTTP (ephemeral port): every route,
  method-swap, cursor, session and
  body branch pinned per side; live
  fanout + disconnect survival; one
  labeled TEST MOCK (fault → 500).
- Caught live bug: unflushed SSE
  headers hung cursor-past-tip clients
  (`flushHeaders` — the test timed out
  until the fix).

## Result (frozen)

- Gate: 1876/1876 (1850 intact + 26),
  100×4 global (transport.ts 100 —
  the `from=null` side needed its own
  queryless stream).
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS (+attack, +arena),
  4 sim goldens byte-identical (ms-only
  drift restored).
- Bundle untouched (91336, SAME md5 —
  server ships nowhere near browsers);
  `match.html` not re-embedded.

## Files

- NEW: `src/server/transport.ts`,
  `src/server/transport.test.ts`
- EDIT: `docs/DECISIONS.md` (D-063),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
