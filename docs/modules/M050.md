# M050 — Counterfactual Ranking — VERIFIED

Vote: `rank-scripts` (2026-09-11). Decision: D-044.
Second of the Counterfactual AI pair (M049–M050)
closing the Refutation → Counterfactual block
(M046–M050): hypothetical SCRIPTS (multi-order
sequences) plus candidate RANKING — "which
hypothetical best serves this order".

## What it ships

- `src/engine/counterfactual.ts` (L3, read-only,
  same file — NO new LAYERS pin): internal
  `simulateScript` core (one fork, steps in
  sequence, first failure wins with `failedAt`)
  - `whatIfScript` (fail-soft guard twin of
    M049; empty script scores the untouched
    state) + `rankCandidates` (every candidate
    sims alone; best-first by outcome score,
    stable input-order ties, unapplied sink at
    score −1 with their reasons, `recommended`
    names the input index — undefined when none
    apply). `whatIfConfidence` (M049) delegates
    to the core (one step) — its 14 tests pass
    UNCHANGED. Verdicts are discriminated unions
    (applied true/false); the success outcome is
    provably present (target pre-guarded, sims
    never move queues, record rides separate —
    pinned even against a commander-wiping stub)
    via a seam-documenting `as` cast (match.ts
    `validated` precedent). No advance (declared
    cut — proven irrelevant to scores); no new
    census pin (same file, by the voted design).
- Match: `whatIfScript` + `rankCandidates`
  (mold whatIf) + private `counterfactualDeps()`
  refactor (shared by the three queries —
  vestigial snapshot param dropped at the
  gate: deps are fields + outcome factory).
- Pins: NONE new (by design — census and
  LAYERS frozen through `counterfactual: 3`).

## Result (frozen)

- Gate: 1650/1650 (1634 intact + 16: 12
  counterfactual — 7 script + 5 rank — + 4
  match-counterfactual), 100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 4 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `63a136fa…`): 71203 chars (+1087 —
  script core + ranking + Match methods);
  `rankCandidates` ×1 in the bundle;
  `state.json` unchanged, `match.html`
  re-embedded.

## Files

- EDIT: `src/engine/counterfactual.ts`,
  `src/engine/counterfactual.test.ts`,
  `src/engine/match-counterfactual.test.ts`,
  `src/engine/match.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-044),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
- No NEW files (by the voted design).
