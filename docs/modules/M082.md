# M082 — Body limits (D-077)

- Status: VERIFIED+LIVE (2026-09-12)
- Server: MAX_BODY_BYTES=64000
  pinado; readJson tripa >cap
  (off+resume+resolve); helper
  bodyError (413 vs 400 — 1
  branch p/ 5 rotas); boundary
  exato servido (> não >=).
- Tests: 3 (pin, 413 JSON
  válido 70KB, 200 exato
  64KB); suite 1926/1926
  100×4.
- Bundle SAME md5; página
  intocada; API redeploy.
