# M088 — Metrics (D-083)

- Status: VERIFIED+LIVE (2026-09-12)
- Server: contadores closure
  (requests + byStatus exacto +
  startedAt); finish listener
  ponto único, 0 branches;
  GET /metrics (+uptimeMs);
  quirk: self em requests,
  nunca em byStatus.
- Tests: 2 (lifecycle exacto
  c/ clock + lookalikes);
  mock res ganha on(); suite
  1936/1936 100×4.
- Bundle SAME md5; API redeploy.
