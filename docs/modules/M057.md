# M057 — Proposal Data — VERIFIED

Vote: `proposal-data` (2026-09-12). Decision: D-051.
Third of the Player ↔ Commander block
(M055–M058): data-first foundation (M055
mold), commander-to-player half. Proposals
are standing commander suggestions awaiting
player verdict.

## What it ships

- NEW `src/engine/proposals.ts` (L0, zero
  imports): `PROPOSAL_KINDS` closed
  (autonomy, order, stance — symmetric with
  directives + orders) + `CommanderProposal`
  {kind, order?, stance?, autonomy?}
  (per-kind payload REQUIRED; siblings
  ignored, M015; payload-only, no notes) +
  L0↛L0 mirrors (order kind + params bounds
  8/32/64; autonomy + stance values) + total
  guards.
- Mirror in `commanders.ts` (L0↛L0 law):
  `proposal?` single-slot (refutation mold,
  reuses isMirroredOrder + value consts) +
  mirror guard + copyRecord nested spread.
- Pins: census `proposals.ts` + LAYERS
  `proposals: 0`.

## Result (frozen)

- Gate: 1774/1774 (1761 intact + 13: 11
  proposals + 2 commanders mirror), 100×4
  global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 4 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `9b2b5c8e…`): 80307 chars (+574 —
  canonical tree-shaken, zero readers; mirror
  ships); `proposal` ×9 in the bundle;
  `state.json` unchanged, `match.html`
  re-embedded.

## Files

- NEW: `src/engine/proposals.ts`,
  `src/engine/proposals.test.ts`
- EDIT: `src/engine/commanders.ts`,
  `src/engine/commanders.test.ts`,
  `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-051),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
