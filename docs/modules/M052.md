# M052 — Memory Record — VERIFIED

Vote: `record-verb` + `attribute-named` (2026-09-12).
Decision: D-046. Second of the Memory block
(M051–M054): the write path — stamped order
facts persist as commander memories.

## What it ships

- NEW `src/engine/memory-record.ts` (L2):
  `memory.record` + recordParamsRule ({events
  1..32} fail-closed) + handler. Per event,
  fail-closed skips: kind unmemorable? no
  commander named? unknown commander? owned
  by another caller (ANTI-SPAM: you only
  write your own logs)? malformed? seq
  already logged (dedup)? — skip. Survivors
  append (FIFO cap 8, oldest evicted).
  Subject is the named commander itself
  (details live at the referenced seq —
  the M051 self-validating pointer). Zero
  survivors REJECT ('record: nothing
  memorable.' — rejected are free, D-022).
  Battle/sighting facts carry no commander
  — SKIPPED whole (M053 owns attribution).
- Match: registration (handlers map +
  paramRule, execution mold) + auto-record
  after every applied dispatch via DIRECT
  kernel.dispatch (bypasses finished-guard
  — the final lance records; no producers;
  no victory re-check — memories cannot end
  games). Nothing memorable skips the
  dispatch (zero revision-bloat); rejected
  tolerated; error FAULTS loud (fail-stop).
  Deterministic request ids (`memory.record
rev N` — twins identical, hygiene gate
  clean). RECORD is budget-free (first
  system transition: no promptsAvailableRule
  /spend/promptsRule — bookkeeping is not a
  player action; promptsRule exported for
  the identity filter).
- Pins: census `memory-record.ts` + LAYERS
  `memory-record: 2`.

## Result (frozen)

- Gate: 1700/1700 (1673 intact + 27: 17
  memory-record + 9 match-memory-record + 1
  match collision), 100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 4 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `66bee682…`): 74386 chars (+2614 —
  record verb + Match wiring); `memory.record`
  ×2, `nothing memorable` ×2 in the bundle;
  mockups regen stable (timeline carries
  rejected record entries — deterministic).

## Files

- NEW: `src/engine/memory-record.ts`,
  `src/engine/memory-record.test.ts`,
  `src/engine/match-memory-record.test.ts`
- EDIT: `src/engine/match.ts`,
  `src/engine/match.test.ts`,
  `src/engine/validation.ts` (promptsRule export),
  `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/*`,
  `docs/DECISIONS.md` (D-046),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
