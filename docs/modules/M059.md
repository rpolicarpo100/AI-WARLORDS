# M059 — Assisted Proposer — VERIFIED

Vote: `assisted-proposer` (2026-09-12).
Decision: D-053. FIRST of the AI Evolution
block (M059–M061): assisted/autonomous
commanders file stance proposals from new
battle lessons (deterministic templates, no
LLM). Closes the D-052 training wheels.

## What it ships

- `proposal.autofile` SYSTEM transition
  (proposal-state.ts: same write as
  propose, NO owner check, budget-free
  rail, fixed wire rule = proposeParamsRule
  reused).
- Match `autofileProposals` auto-step
  (M052 mold, after recordMemories):
  active + assisted|autonomous + slot
  empty + stance unset + NEW lesson
  (fresh battle/failure stamped this
  lance — declines stay declined) +
  divergence (recall ≠ DNA) → inner
  kernel.dispatch + hand-run proposed
  producer (the holder MUST notice).
  Fire-and-forget (documented M052
  deviation: pre-checked, 100×4 law).
- stance.ts exports STANCE_BATTLE/
  FAILURE_KINDS (behavior-identical
  refactor). Zero new files → zero pins.

## Result (frozen)

- Gate: 1800/1800 (1789 intact + 11:
  3 autofile direct + 8 auto-step live),
  100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 4 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `b7f30a7f…`): 86443 chars (+1302);
  `proposal.autofile` ×4 in the bundle;
  `state.json` unchanged, `match.html`
  re-embedded.

## Files

- EDIT: `src/engine/proposal-state.ts`,
  `src/engine/proposal-state.test.ts`,
  `src/engine/match.ts`,
  `src/engine/match-memory-record.test.ts`,
  `src/engine/stance.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-053),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
