# M071 — Multiplayer — Lobby — VERIFIED

Sem voto (2026-09-12): D-065. THIRD of
the Multiplayer block (M069–M076) —
the server becomes a game room: swept
listings, closable tables, finished
matches guarded at the door.

## What it ships

- GET /matches: sweeps every table
  (no ghosts) and lists [{matchId,
  players, online, status, revision,
  createdAt}] (status open/finished
  straight from the verdict).
- POST /:id/close: drains the body,
  ends every stream, deletes (closing
  twice 404s; the table is gone for
  join/dispatch/state after).
- Join on finished → 400 (dispatch
  still 200 + MATCH_FINISHED outcome —
  join-guarded, dispatch-carried).
- 6 tests: empty lobby, exact open
  table, the full finished line (20
  real noops → revision 20 → join
  refused → MATCH_FINISHED carried),
  sweep-before-list, lookalikes 404,
  close-ends-streams.

## Result (frozen)

- Gate: 1892/1892 (1886 intact + 6),
  100×4 global (transport.ts 100).
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS (+arena), 4 sim goldens
  byte-identical (ms-only drift restored).
- Bundle untouched (91336, SAME md5);
  `match.html` not re-embedded.

## Files

- EDIT: `src/server/transport.ts`,
  `src/server/transport.test.ts`,
  `docs/DECISIONS.md` (D-065),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
