# M020 — Economy Validation (VERIFIED @ 2026-09-11)

> Fecha a Fase 3 (Economy M016–M020). A 6.ª pós-regra: caps de
> armazenamento + conservação de recursos; gather rejeita cheio.
> L-31 enforcement LANDED (mecânica M018 → efectiva).

## 1. Âmbito (contrato)

- `createEconomyRule(buildings)` em `economy.ts` L2 (tipos
  ESTRUTURAIS — L2↛L2; assignability no seam `match.ts`): `economy-cap`
  (após: cada amount ≤ `capOf`; holders ordenados, RESOURCE_TYPES) +
  `economy-conservation` (totais piles+nós não-crescentes por tipo).
- Assume-shape (postShape-first, precedente rosterRule); caps antes
  de conservação; primeira violação vence (determinístico).
- `createWorldValidator` segue base-5; Match anexa a 6.ª (pin
  reescrito 5+1 — o rewrite declarado D-010, não regra snuck-in).
- `createGatherHandler(economy, buildings=DEFAULT)` (default
  uncap-neutro, zero churn): `taken > room` → `gather: storage full.`
  (tudo-ou-nada; over-cap inicial rejeita — fail-closed).
- FAULT fecha o resto: writer custom/bug → HANDLER_FAULT; noop desde
  over-cap FAULTa (estados inválidos não abençoados).
- Cortes (D-013): sem clamp parcial, sem bounds de queue (incompatível
  M019 + ungrounded), sem counts monótonos (capture futuro), sem
  validação em createWorldState (data-lenient M016).

## 2. Grounding → decisão

- D-011 (L-31 enforcement→M020; gap gather-sem-caps declarado) +
  D-010 (M020 audita conservação) + mestre #47 (Validar resources) +
  #48 (resource manipulation) + R-20 (resource exploits).
- Cap-check no HANDLER (D-010: camada única p/ lógica); overflow
  capOf→FAULT (escala-de-bug); Storage é só nome #16 (sem semântica
  no mestre — D-011 owns caps).

## 3. Rewrites declarados (2 pins)

- `overflow stays loud` (M017) → `ceiling rejects now`: com caps,
  gather no tecto rejeita em vez de FAULTar (coincide exacto com
  would-overflow sob default); throw do `credit` directo sobrevive.
- `composition stays at 5` → `5 base + 1 wired`: base presa em 5 +
  ponteiro p/ provas comportamentais da 6.ª (FAULT suites).

## 4. Testes (23 novos → 955/955)

- Regra directa 12: config inválida, ausência, goldens cap
  (`p1.gold 120 exceeds cap 100`), storage→150, ordem
  holders+tipos, gather-move conserva, spend passa, goldens
  conservação (`gold 3 -> 4`, `wood 4 -> 5`), ordem por-tipo,
  caps-primeiro.
- Gather-caps directo 5: builds inválida, cheio/over-cap rejeitam,
  room exacto aplica, default uncap intacto.
- E2E 6: reject via dispatch (untouched/rev 0), aplica sob cap,
  overfill→FAULT, conjure-sob-cap→FAULT (conservação pura),
  noop-desde-over-cap→FAULT, happy-path rev 3.
- Cobertura 100×4; `dist` sem testes; census/LAYERS intactos
  (sem ficheiro novo — precedente M017).

## 5. Gate e fecho

- Bateria 955/955 (932 intactos + 23); selo intacto; composição
  5+1; Fase 3 CLOSED (M016–M020 VERIFIED).
- L-31 enforcement LANDED (row histórico; CLOSED desde M018);
  L-32 segue →M021/M022 (gather sem worker).
- RISKS: baixo — validação pura, sem dados novos; residual: detalhe
  do FAULT invisível no dispatch (código só; goldens directos cobrem).
- Commit: M020 Economy Validation VERIFIED (955/955, 100×4). STOP.
