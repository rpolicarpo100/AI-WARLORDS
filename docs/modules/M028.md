# M028 — Perception (VERIFIED @ 2026-09-11)

> Nível INFERRED live: terreno lembrado p/ células explored
> sem visão. L-29 CLOSED. Vista derivada, zero churn estado.

## 1. Âmbito (contrato)

- `PerceivedMap.inferred {index→terrain}` (explored E
  não-visível → terreno; memória==actual — terreno imutável,
  grep-prova zero writes fora de testes); OOB skip-soft
  (D-006 — unlawful na construção, soft na percepção);
  disjunto de visible por construção.
- Só `views.ts` (fence M015: sem ficheiro novo, census
  intacto, sem LAYERS); builders toAiPerception/toClientView
  herdam; F-09 intacta (chave aninhada, não top-level).
- Testes: golden + 3 novos (multi-célula+disjunto, OOB soft
  crafted, UNKNOWN ausente); churn map-keys ×4 (resources
  AI+CLIENT, map AI+CLIENT) + pins `inferred: {}`.
- Cortes (D-021): intel inimiga (mantém fail-closed —
  "M028+" é intervalo; inferência era a atribuição
  explícita); memória de amounts (mutáveis, sem âncora);
  memória de spawns (D-008 sem modelo); guard de
  PerceivedState (M015 wire M069).

## 2. Grounding → decisão

- M015 (INFERRED/UNKNOWN→M028; L-29→M028) + D-008
  (posição-sem-terreno até inferência) + #10 (KNOWN≠REAL)
  + D-006 (contexto soft) + M014 (memória de índices).
- OOB: isWorldState faz bounds-check (memória OOB só via
  crafted `as never` — precedente do próprio ficheiro).

## 3. Churn (declarado)

- views.ts (+docs L-29, +loop inferred); views.test.ts
  (golden + describe inference); map.test.ts + resources.
  test.ts (keys).
- Bundle byte-idêntico (views fora do entry browser-engine);
  state.json intocado; sim idêntico (1200/500/0); página
  ignora a chave (aditivo inofensivo).
- RISK row M028 (L-29 CLOSED); M024–M027 sem rows (opção
  desses módulos — nada retroactivo).

## 4. Testes (3 novos → 1169/1169)

- Inference 3/3: multi-célula disjunta, OOB soft crafted,
  UNKNOWN ausente nos 3 campos.
- Bateria 1169/1169 (1166 intactos + 3); cobertura 100×4
  (All-files + views 100); selo intacto.

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0); verify-bundle + smoke +
  smoke:attack verdes; sim regressão limpa.
- RISKS: baixo — vista derivada, edges soft; residual:
  nenhum novo (D-006 preservado).
- Commit: M028 Perception VERIFIED (1169/1169, 100×4). STOP.
