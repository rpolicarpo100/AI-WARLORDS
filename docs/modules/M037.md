# M037 — Commander Stance (VERIFIED @ 2026-09-11)

> 3.º do bloco Strategic AI. 1.ª AI DECISION sobre o
> substrato (#7; âmbito + convenção VOTADOS): postura
> pura por commander a partir do effective DNA
> (dominante-com-margem-15). 1.º consumer do M035.
> Zero wiring (consumers M038+).

## 1. Âmbito (contrato)

- `src/engine/stance.ts` NOVO (L2 — commanders/dna
  L0 + effective-dna L1, downward): STANCE_IDS
  (fixo-5) + isStanceId + STANCE_MARGIN = 15
  (VOTADO) + stanceOf(record): dominante entre
  aggression/defense/expansion/diplomacy vence se
  max−2.º ≥ 15, senão balanced (empates →
  balanced determinístico). Pura, total.
- Selo phase1-gate: 2 pins (census + LAYERS
  `stance: 2` — fail-loud, M015).
- Zero wiring (consumers M038+); bundle byte-idêntico
  (md5 6235d684 — rebuild de prova idempotente).
- Cortes (D-031): thresholds por traço; input
  DnaTraits (não consome M035); blends; wiring já.

## 2. Grounding → decisão

- 2 votos 2026-09-11 (stance; convenção margem-15,
  molde matrizes — proposta antes do código) + #7
  (AI=DECISIONS) + M035 (substrato) + gate.
- 1 parêntese em falta (typo meu, parse fail-loud);
  margem-15 vence exatamente no 100−85 (golden).

## 3. Churn (declarado)

- `stance.ts` + `.test.ts` novos;
  `phase1-gate.test.ts` (2 pins; hunks 57/393
  intactos); D-031.
- state.json/godot/sim/página intocados; sim
  idêntico (1200/500/0); drill idêntico
  (1200/240/0/315/556); verify-bundle SEM regen.
- Prettier: `--write` teste novo; D-031 reworded
  (leading-`+` → vírgula, molde D-026); dirt
  pré-existente intacto.
- Reports `ms` revertidos, fora do commit.

## 4. Testes (11 novos → 1397/1397)

- `stance.test.ts` 11: vocabulário (3: lista-5,
  trait-id, pin margem-15) + goldens (7: aggressive
  100−85=15, defensive 90−50, expansionist e
  diplomatic construídos, bare balanced, edges
  64→balanced e 65→aggressive) + battery (1:
  6×7=42 combos isStanceId ∀ + 3 edges).
- Bateria 1397/1397 (1386 intactos + 11, zero
  regressões); cobertura 100 global + stance.

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0); verify-bundle +
  smoke + smoke:attack verdes; sims regressão limpa.
- RISKS: baixo — pura+total; residual: tuning (#92);
  folha L2 sem importadores até M038+.
- Commit: M037 Commander Stance VERIFIED (1397/1397,
  100 global). BLOCO SEGUE (M038+).
