# M058 — Proposal Verbs — VERIFIED

Sem voto (2026-09-12): D-051 declared this
contract; molds decided the micro-rules.
Decision: D-052. LAST of the Player ↔
Commander block (M055–M058) — the block
CLOSES here. The M057 pending slot becomes
playable.

## What it ships

- NEW `src/engine/proposal-state.ts` (L2,
  directive-state mold verbatim):
  `proposal.propose` {id, proposal}
  (owner-only; reject when occupied
  'pending.' — verdicts never lost) +
  `proposal.approve` {id} (order→append to
  orders, cap 8 fail-closed 'queue full.'
  — issue mold; stance/autonomy→directive
  write; slot clears) + `proposal.decline`
  {id} (slot clears, nothing executes).
  Emptied slots drop the key whole (M047
  law). Structural producers
  (proposed/approved/declined; NOT
  memorable). propose owner-dispatched
  (training wheels until M059+ AI writers;
  no auto-propose; no active check — mold).
- Match wiring: handlers merge + paramRules
  (propose rule; approve/decline REUSE
  commanderIdParamsRule) + 3 producers.
- Pins: census `proposal-state.ts` +
  LAYERS `proposal-state: 2`.

## Result (frozen)

- Gate: 1789/1789 (1774 intact + 15:
  registration + 2 rules + 3 propose +
  4 approve + 2 decline + 3 producers),
  100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 4 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `03e25381…`): 85141 chars (+4834 —
  handlers + producers ship);
  `proposal.propose` ×5 in the bundle;
  `state.json` unchanged, `match.html`
  re-embedded.

## Files

- NEW: `src/engine/proposal-state.ts`,
  `src/engine/proposal-state.test.ts`
- EDIT: `src/engine/match.ts`,
  `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-052),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
