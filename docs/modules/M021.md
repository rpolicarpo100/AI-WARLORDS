# M021 — Unit System (VERIFIED @ 2026-09-11)

> Abre a Fase 4 (Military). Unidades como indivíduos posicionados:
> dados+config+mecânica pura, sem transições (M018-shape); L-32
> repoint→M022 (gate worker-at-node precisa movimento).

## 1. Âmbito (contrato)

- `units.ts` L0 NEW: 3 tipos fechados #15 (worker/warrior/archer;
  cavalry+ NOT YET) + UnitInstance `{id, owner, type, hp, col, row}`
  - UnitsData `{nextId, units[]}` + guards (ids únicos) + `unitsOf`/
    `unitById` fail-soft com cópia (precedente cityOf).
- `warfare.ts` L2 NEW: UnitsConfig por-tipo `{cost, maxHp, damage}`
  (#83) + strict-guard + DEFAULT neutro (free/1hp/harmless, frozen)
  - `unitCostOf`/`maxHpOf`/`unitDamageOf` + `spawnUnit` puro full-hp
    (id `u${nextId}`; colisão/exaustão→throw; bounds uints-only).
- `WorldState.units?`; percepção own-only `units` (F-09 10/9).
- Census 21 (gate lista 22 c/ harness); LAYERS += `units: 0`,
  `warfare: 2`; composição 5+1; `dist/engine` 88/0.
- Cortes (D-014): sem transições/Match/factos (criação→M025
  presumível); sem regra; sem bounds on-map (M022); sem cura/dano
  (M024); sem percepção inimiga (fail-closed →M028+); sem gate
  worker (L-32→M022).

## 2. Grounding → decisão

- #15 (3 MVP + papéis; resto "Não implementar ainda") + #83 (UNIT
  COST/HP/DAMAGE) + #5 (HP/criação engine-owned) + percepção-units
  (#~332) + L-32 + M018 (shape) + D-006 (config-sem-consumidores).
- nextId determinístico (replay-safe; random-UUID banned no engine);
  custo ESTRUTURAL (L2↛L2; payCost no seam M025); maxHp≥1 strict
  (0=stillborn); owner validado via isPlayerId (L0 authority).

## 3. Implementação

- Split L0+L2 forçado (world-state L1 só importa downward):
  units.ts zero-imports (espelhos holder/uint32, teste cruza);
  warfare.ts importa authority/map/rng/units.
- 3.ª cópia local isUint32 (convenção fog/economy, forçado M009).
- Gate apanhou `randomUUID` em comentário (allowlist token-scan):
  reword p/ "random-UUID" — token nunca em sources novas.

## 4. Testes (80 novos → 1035/1035)

- `units.test.ts` 48: vocabulário, guards, unicidade, queries,
  cópias-fresh (not.toBe), espelhos 64/uint32.
- `warfare.test.ts` 32: config strict, defaults, lookups, spawn
  (goldens u0/u1, colisão, exaustão, pureza), WSext, percepção.
- `views.test.ts`: F-09 10/9 + 2 goldens com `units: []`.
- Cobertura 100×4; selo intacto; zero churn (sem dispatch novo).

## 5. Gate e fecho

- Bateria 1035/1035 (955 intactos + 80); statics verdes.
- L-32 REPOINT→M022 (registo D-014 + RISK row); L-31 segue LANDED.
- RISKS: baixo — dados puros; residual: off-map lenient até M022;
  nextId sem cross-check c/ init-placed (loud se colidir).
- Commit: M021 Unit System VERIFIED (1035/1035, 100×4). STOP.
