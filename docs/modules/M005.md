# M005 — Deterministic Match (VERIFIED)

> Checkpoint M005 @ 2026-09-10 · 92 novos testes (214/214) · 100×4 · zero mocks

## 1. Contrato

- Proveniência: MATCH_ID + SEED + RULESET + PLAYERS + INITIAL_STATE, validada e congelada.
- Avanço: `match.advance` soma +1 tick (única mutação; resto chega com os domínios).
- TIMELINE: 1 entrada por dispatch aplicado/rejeitado, com hash sha256; espelha o kernel log 1:1.
- RNG com seed (mulberry32): primitiva selada, SEM wiring a handlers.
- Determinismo: mesma seed + mesmas ops ⇒ mesmos snapshot/hash/timeline (goldens lockados).

## 2. Pré-análise (decisões)

- Tick regista-se por outcome: rejeições carregam o tick corrente (documentado, testado).
- Roster == jogadores do mundo (fail loud); espectadores usam vistas read-only (M163).
- `matchId` gerado se ausente (`crypto.randomUUID`); seed OBRIGATÓRIA, nunca defaulted.
- Ruleset `standard v1`; resto recusado (fail loud, nunca fallback silencioso).
- Timeline sem `matchId` (entradas comparáveis entre partidas); M088 decide agregação.

## 3. Implementação

- `src/engine/match.ts` — `Match` (L97), `matchHandlers` (L65), `MatchInit` (L82).
- `src/engine/rng.ts` — `SeededRng` (L14): mulberry32 + resume via `getState`.
- `src/engine/hash.ts` — `stableStringify` (L11) + `hashState` sha256 (L35).
- `src/engine/authority.ts` — `freezeState` endurecido (rejeita fn/symbol/bigint/undefined).

## 4. Testes (6 categorias, zero mocks)

| Categoria    | Onde                                                     | O quê                                      |
| ------------ | -------------------------------------------------------- | ------------------------------------------ |
| Unit         | `match.test.ts:51`, `rng.test.ts:4`, `hash.test.ts:4`    | guards, RNG bounds, serialização estável   |
| Integração   | `match.test.ts:158`                                      | advance, ticks 1-1-2, golden, ride-along   |
| Falha        | `match.test.ts:85`, `match.test.ts:399`                  | 20 ctor-bateria + detach/input             |
| Segurança    | `match.test.ts:399`, `authority.test.ts:511`             | freeze, sessions-out, 15 freezeState       |
| Determinismo | `match.test.ts:158`, `rng.test.ts:35`, `hash.test.ts:32` | run×2 igual, 100-seq, goldens lockados     |
| Regressão    | suite 214/214                                            | M002–M004 intactos (122 testes anteriores) |

## 5. Third-Eye (ataques)

- Roster≠mundo (3 direcções) → rejeitado; dup-roster → kernel `/unique/`.
- Handler extra a colidir → `/duplicate handler/` (nunca shadow de built-in).
- Rejeição/erro/dup: timeline intacta; hash inalterado em rejeição.
- Goldens generated-then-locked: RNG (seeds 1/42/7), sha256, timeline `d1e14b81…`.
- `matchHandlers()` contém EXACTAMENTE `match.advance` (fence lock contra mutações).

## 6. Gate

- format/lint/typecheck/test/build PASS; 214/214; 100/100/100/100.
- `dist/` com 21 ficheiros engine, 0 testes; packages intocados (verificado pré-commit).

## 7. Limitações declaradas

- L-09: timeline+log unbounded in-memory (M062: bound/persistência).
- L-10: seed inerte — wiring rng→handlers é M006+ (usar hoje seria cheating-by-design).
- L-11: sem lifecycle/persistência/rede (M071/M069+); Match é in-process.
- L-08 (M004) mantém-se: caps de escrita em M006.

## 8. Próximo

M006 Action Validation: caps por escrita, wiring rng→handlers, validação de domínio.
