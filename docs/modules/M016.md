# M016 — Resource Engine (VERIFIED)

> Checkpoint M016 @ 2026-09-11 · 66 novos testes (717/717) · 100×4 ·
> economia = estado + config + ops exactas · D-009 escrita antes do código

## 1. Contrato

- Stockpiles em estado: `WorldState.stockpiles?` (4-set fechado
  food/wood/stone/gold, uint32; map-independent; holders só-shape).
- Config fora do estado (#84): `EconomyConfig` 4/4 `{value, gatherYield}`
  - default neutro frozen; `value`→score futuro (L-17), `gatherYield`→M017.
- Ops puras exactas: `credit`/`debit` (overflow/overdraft loud, nunca
  saturação silenciosa), `canAfford` (boolean fail-soft), `stockpileOf`
  (zeros fail-soft, cópia fresca).
- Percepção own-only: `PerceivedState.stockpile` (próprio, zeros default;
  outros holders invisíveis fail-closed); tripwire F-09 +1 chave.
- SEM pós-invariante (M020 owns — composição presa em 5 por teste);
  SEM handlers/eventos (M017 primeiro produtor).

## 2. Pré-análise (decisões)

- D-009 precede a implementação (análise-antes-código, em `DECISIONS.md`).
- Par leaf+ops simétrico a M014 (forçamento M009: guard em L0 porque
  world-state L1 só importa L0; L0 não importa L0).
- Defaults neutros 1/1/1/1 (analogia M011: só desvios ditados pelo
  mestre; tuning em #92/playtesting).
- Chaves fixas espelham RESOURCE_TYPES; 3.º espelho id-shape; unions
  idênticas mutuamente atribuíveis; cross-checks em teste.
- Doutrina membership-no-consumo (M014/L-27): holders non-roster
  invisíveis; custos parciais (ausente = zero).

## 3. Implementação

- `stockpiles.ts` (novo, L0): dados + guard + `stockpileOf` (builder
  puro, precedente loader map.js; callers congelam).
- `economy.ts` (novo, L2): config + default + `credit`/`debit`/
  `canAfford`; imports authority/map/rng/stockpiles (DAG intacto).
- `world-state`: campo/init/guard/factory (+4 spots, padrão M010-map);
  `views`: import + campo + embed (+3 spots, L2→L0).
- Gate census 17 + `stockpiles: 0`/`economy: 2`; composition test
  INTACTO em 5 (prova de não-regra); F-09 re-locked (7/6 chaves).

## 4. Testes (6 categorias)

| Categoria    | Onde                        | O quê                                     |
| ------------ | --------------------------- | ----------------------------------------- |
| Unit         | guards + default + afford   | 31 shapes; config 4/4; neutro; blockers   |
| Integração   | ops + WorldState + perceive | goldens credit/debit; extensão; own-only  |
| Falha        | loud ×3 ops + soft queries  | data/type/amount/cost; overflow; zeros    |
| Segurança    | exactidão + non-leak        | sem saturação; imutáveis; holders fora    |
| Determinismo | run×2 + goldens             | saídas literais; frozen top+nested        |
| Regressão    | suite 717/717               | M001–M015 intactos (651 testes, tripwire) |

## 5. Third-Eye (ataques)

- Data/type/amount/cost malformados ×3 ops — tudo loud com prefixo da op.
- Overflow MAX+1 loud; overdraft por tipo loud (`insufficient <type>`);
  custo `{}` paga sempre (vacuoso, correcto).
- Holder desconhecido/dados ausentes → zeros (nunca throw, nunca leak);
  non-roster `zx` com 999 invisível ao p1.
- Scan resources refinado M016: `amount`/`resource` nunca no JSON (a
  chave legítima `stockpile.gold` cegaria o scan antigo).
- Config 3/5, chave `lava`, rates null/array, value/yield maus ×4 —
  tudo rejeitado.

## 6. Gate

- format/lint/typecheck/test/build PASS; 717/717; 100/100/100/100.
- `dist/` com 72 ficheiros engine, 0 testes (contado, não derivado);
  fence: 2 novos + world-state/views/testes/gate declarados.

## 7. Limitações declaradas

- L-30 (nova): starting grants sem dono (decidir até M019).
- L-31 (nova): sem caps de stockpile (tecto uint32; storehouses —
  decidir até M018).
- L-28 mantém-se (anti-forge → M021+); L-29 mantém-se (posição-sem-
  terreno → M028).
- Sem gathering (M017), sem custos-mecânica (M018), sem validação
  (M020), sem score (L-17), sem trade.
- N/A (§2 TESTING.md): rede/persistência (M062/M069+), SIMULTÂNEO/RACE
  (serial, M086), verificação manual (automatizado).
- L-09/L-12/L-13/L-15/L-16/L-17/L-18/L-19/L-20/L-21/L-22/L-23/L-24/L-25/L-26/L-28/L-29
  mantêm-se.

## 8. Próximo

M017 Gathering: primeiro produtor (nó→stockpile; depleção; evento
RESOURCE_GATHERED); consome `gatherYield` (seam: Match fecha sobre
config); enfrenta `map-preserved` (D-010).
