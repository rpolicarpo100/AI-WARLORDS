# M045 — Order Execution — VERIFIED

Vote: `execute-close` (2026-09-11). Decision: D-039.
Close of the Player → AI Command block (M043–M045):
per-kind executable semantics over the M044 queue
(M044's declared L2↛L2 cut, fulfilled here).

## What it ships

- NEW `src/engine/order-execution.ts` (L2,
  downward-only): `order.execute` (treasury
  precedent — pure factory `orderExecutionHandlers`
  + `createOrderExecutedProducer`, Match injects
  the live verb handlers/rules/producers; owner-only;
  pop-head + apply via live handlers; rejects leave
  the queue intact with zero new facts) + composed
  facts (`order.executed` FIRST `{player,commander,
  kind,depth}`, then the verb's own domain facts via
  the static 5-producer map, riders-behind).
- Match wiring: injected execution handlers + static
  sub-producer map + `order.executed` producer (5/5
  verbs); collision guard (extra `order.execute` →
  fail loud).
- Pins: census `order-execution.ts` + LAYERS
  `order-execution: 2`.
- NEW `sim/orders-drill.ts` (`npm run sim:orders`,
  M041 mold): 20 games × 60 steps, 7 queue-coherence
  invariants checked against live Matches every
  applied dispatch; rider-aware domain deltas.

## Result (frozen)

- Gate: 1520/1520 (1491 intact + 29: 28
  order-execution + 1 match collision-guard), 100×4
  global.
- tsc 0, eslint 0 (incl. sim), `verify:bundle` PASS,
  `smoke` PASS, 3 sim goldens byte-identical
  (ms-only drift restored).
- Orders drill: applied=1200 rejected=155
  violations=0 (issued=728 executed=57
  canceled=110); sans-ms ×2 identical.
- Bundle rebuilt deterministically (md5 ×2
  `38b6992e…`): 63055 chars (+2136 —
  `order-execution` factory+producer ships);
  `order.executed` ×1 in the bundle;
  `state.json` unchanged, `match.html` re-embedded.

## Files

- NEW: `src/engine/order-execution.ts`,
  `src/engine/order-execution.test.ts`,
  `sim/orders-drill.ts`
- EDIT: `src/engine/match.ts`,
  `src/engine/match.test.ts`,
  `src/engine/phase1-gate.test.ts`,
  `package.json` (sim:orders), `tools/engine.bundle.js`,
  `mockups/match.html`, `docs/DECISIONS.md` (D-039),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
