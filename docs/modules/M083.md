# M083 — Security headers (D-078)

- Status: VERIFIED+LIVE (2026-09-12)
- API: nosniff + DENY em
  onRequest (todas respostas,
  2 linhas, 0 branch); 1 teste
  (GET real c/ headers);
  suite 1927/1927 100×4.
- STATIC: BLOQUEADO (dashboard-
  only, docs oficiais). Click
  path (30s): Dashboard →
  ai-warlords (static) → Headers
  → Add: `/*` +
  X-Content-Type-Options=nosniff,
  X-Frame-Options=DENY,
  Referrer-Policy=same-origin.
- Bundle SAME md5; API redeploy.
