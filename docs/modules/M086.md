# M086 — Supply chain (D-081)

- Status: VERIFIED+LIVE (2026-09-12)
- Config: engines <21 (docs);
  .npmrc engine-strict (npm ci
  prova); .nvmrc "20" (float
  patch); `npm run audit`
  (moderate+); audit 0 vulns;
  majors v5/v7 diferidos (RISK);
  secrets grep limpo.
- Tests: nenhum (config
  aplicada pelo npm; suite
  1933/1933 100×4 intacta).
- Bundle SAME md5; rebuilds
  verdes = prova (static+API).
