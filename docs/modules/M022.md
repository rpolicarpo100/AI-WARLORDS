# M022 — Movement (VERIFIED @ 2026-09-11)

> 1-step hex movement + terrain passability; gather closes over
> worker presence. O loop worker completa-se: anda→colhe. L-32 CLOSED.

## 1. Âmbito (contrato)

- `MOVE_TRANSITION` (`unit.move`) em `warfare.ts` L2:
  `createMoveHandler(passable)` + `moveParamsRule` + `moveProducer`
  (`unit.moved`, LOW, `{player, unit, col, row}`) + `warfareHandlers`.
- Regras: `no units.` / `unknown unit.` / `not your unit.` /
  `no map.` / `out of bounds.` / `not adjacent.` (neighborsOf
  stagger-aware; same-cell incluído) / `impassable.`
  (move não-finito bloqueia, fail-closed).
- 1-step (sem speed stat #83; viagem = N dispatches).
- Match: seam `terrainConfig?` (default neutro M011; river bloqueia
  free) + merge + paramRule + producer; predicado em vez de espelho
  (L2↛L2 intacto; Match L4 injecta closure).
- Gather gate (economy, unitsOf L0): worker exact-cell senão
  `gather: no worker here.` (L-32 CLOSED).
- Sem regra nova (composição 5+1); sem ficheiro novo
  (census/LAYERS/dist intactos).
- Cortes (D-015): multi-step, multipliers-como-custo (sem movement
  points), stacking-gate, fog-gating, gate adjacente, worker qualquer.

## 2. Grounding → decisão

- #47 (Validar movement) + #48 (impossible movement, speed
  manipulation) + #factos (UNIT_MOVED) + #priorities (LOW) +
  M011 (move/Infinity) + L-32 + #15 (WORKER recolha).

## 3. Churn L-32 (declarado, 18 fixtures)

- `mapful`/`gatherMatch`/`rich`/`cappedMatch`/`coerced` tripulados
  (`crewed()`: workers p1 em ambas as nodes); 161/161 verdes.
- Gerador de mockups tripulado (u5 em 3,2) + `build.mjs`
  idempotente; mockups rebuildados e re-verificados.

## 4. Testes (30 novos → 1065/1065)

- `warfare.test.ts` +21: params, handler directo (8 rejects +
  golden com spy de terreno), handlers, producer, E2E (golden +
  facto LOW, river-default bloqueia, custom fordable aplica,
  ownership, pre-rule, journey 2-steps).
- `economy.test.ts` +5: gate (sem units, célula errada, warrior,
  worker inimigo, E2E untouched/rev 0).
- `match.test.ts` +4: `terrainConfig` inválido rejeita.
- Cobertura 100×4; selo intacto.

## 5. Gate e fecho

- Bateria 1065/1065 (1035 intactos + 30); statics verdes.
- L-32 CLOSED (gather exige worker); criação→M025 mantém.
- RISKS: baixo — movimento puro + gate estrito; residual: viagem
  longa = N dispatches (aceite até speed stat, se alguma vez).
- Commit: M022 Movement VERIFIED (1065/1065, 100×4). STOP.
