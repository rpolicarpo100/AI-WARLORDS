# M081 — Rate limit (D-075)

- Status: VERIFIED+LIVE (2026-09-12)
- Server: fixed-window 120/60s
  por chave (3º param, default
  v1 pinado); rateKey = XFF-last
  não-vazio senão remoteAddress;
  tudo conta; 429 {error};
  sem Retry-After (CUT).
- Tests: 4 (pin, refuse+reset
  c/ clock, XFF-distinct,
  last-hop+fallback vazio);
  suite harness limites altos
  explícitos; suite 1924/1924
  100×4.
- Bundle SAME md5; página
  intocada; API redeploy.
