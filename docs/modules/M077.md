# M077 — Match history (D-071)

- Status: VERIFIED+LIVE (2026-09-12)
- Server: close regista
  {matchId, seed, status, outcome,
  condition, scores, revision,
  createdAt, closedAt}; GET
  /results (append); cap 50 FIFO
  (2º param createTransport);
  outcome = kind ('draw'|null —
  única condition é draw, sem
  branch win incobrível).
- Tests: 5 novos (vazio, early
  close scores exatos 177/113,
  draw finished, eviction cap=2,
  lookalikes); suite 1903/1903
  100×4.
- Bundle untouched (SAME md5);
  página intocada; API redeploy.
