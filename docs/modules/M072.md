# M072 — Multiplayer — Deploy — VERIFIED

Sem voto (2026-09-12): D-066. FOURTH of
the Multiplayer block (M069–M076) —
the transport goes to production: a
real entry point, a real second Render
service, health + CORS seams.

## What it ships

- `src/server/serve.ts` (PORT, binds,
  logs URL, Invalid PORT → exit 1):
  coverage-excluded with child-process
  tests (src/dev.ts precedent, same
  compensating control, documented).
- GET / → {service, ok} (Render health);
  OPTIONS → 204; CORS `*` on every
  response (no auth, no credentials —
  the honest wildcard).
- render.yaml: ai-warlords-api (node,
  build + start dist, healthCheckPath /);
  static site untouched.
- 6 tests: 4 deploy seams over HTTP
  (health, root-404, preflight+CORS,
  tagged responses) + 2 bootstrap
  (boots/forges/SIGTERM, bad-PORT-1).
- Proved the exact prod path locally:
  `npm run build` + `node dist/...`
  serves / and forges matches.

## Result (frozen)

- Gate: 1898/1898 (1892 intact + 6),
  100×4 global (transport.ts 100).
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS (+arena), 4 sim goldens
  byte-identical (ms-only drift restored).
- Bundle untouched (91336, SAME md5);
  `match.html` not re-embedded.

## Files

- NEW: `src/server/serve.ts`,
  `src/server/serve.test.ts`
- EDIT: `src/server/transport.ts`,
  `src/server/transport.test.ts`,
  `vitest.config.ts`, `render.yaml`,
  `docs/DECISIONS.md` (D-066),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
