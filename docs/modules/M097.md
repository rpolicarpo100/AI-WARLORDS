# M097 — Free UI (D-092, fecho bloco 9: m097)

- Status: VERIFIED+LIVE (2026-09-12)
- Page: select netMode +
  Forge envia mode + log
  `forged <id> (mode)` + State
  `prompts free (∞)` em absent
  + Close `table closed
  (mode)` (NET.mode reset).
- smoke:api perna free: forge
  free → pre-join → 12 noops
  rev-by-rev past-10 → state ∞
  → close void → board sem
  history (3) (sleep+absence).
  E2E: …pulse → free.
- Suite 1960/1960 100×4;
  bundle SAME; match.html
  576520 (+388).
- FIX: M096.md fora do push
  5ec25c4 (race write/add) —
  incluído aqui; regra: nunca
  git add em paralelo com
  write.
- Free Mode CLOSED; BLOCO
  M095–M097 CLOSED.
