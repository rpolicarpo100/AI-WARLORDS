# M075 — Online state (D-069)

- Status: VERIFIED+LIVE (2026-09-12)
- Panel: botão State → GET /state
  → netOut (`state: prompts p1:N
  p2:M` + `- id owner type hp
  @col,row`/unidade); guard =
  matchId (state não exige sessão);
  shape probado por curl (jamais
  assumido); parse defensivo.
- Tests: suite 1898/1898 100×4;
  base smoke clica netState
  (no-crash); API_TEST prova
  `p1:7 p2:10` (3 prompts gastos)
  + `- u1 p1 warrior hp12 @1,1`
  (marcha visível — loop fechado).
- Bundle untouched (91336, SAME
  md5); `match.html` re-embedded.
