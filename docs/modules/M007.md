# M007 — Event System (VERIFIED)

> Checkpoint M007 @ 2026-09-10 · 29 novos testes (284/284) · 100×4 · zero mocks

## 1. Contrato

- Factos, não journal: emissão só em outcomes aplicados, só com produtores registados.
- Emissão real: `match.started` (ctor, seed/players/ruleset) + `match.advanced {tick}` (LOW).
- Restrição provada: noop/rejeições/faults/duplicados → nada (#20 anti-bloat).
- Observação nunca parte execução: falha de produtor → surrogate `system.event-fault` + continua.
- Envelope reconstruível: `{seq, tick, revision, type, priority, payload}` (#19), tudo frozen.

## 2. Pré-análise (decisões)

- Type+params lidos do pedido kernel-accepted (cast documentado — já validado pelo kernel).
- Caller de `session` (server-established); seq de `events.length`; geneses seq 1 sempre.
- Bound de types reutiliza `MAX_ID_LENGTH` (sem nova constante mágica).
- Sem subscrição/live-delivery: zero consumidores (YAGNI — M062/M028); só `getEvents`.
- Prioridades LOW–CRITICAL (#33) só registadas; consumo é decisão futura.

## 3. Implementação

- `src/engine/events.ts` — `runProducers` total (L108), `matchStartedEvent` (L57), registo (L52).
- `src/engine/match.ts` — merge append (L187), geneses (L199), emissão pós-apply (L216), `getEvents` (L260).

## 4. Testes (6 categorias, zero mocks)

| Categoria    | Onde                      | O quê                                            |
| ------------ | ------------------------- | ------------------------------------------------ |
| Unit         | `events.test.ts:58,78,86` | geneses exacta, registo-fence, `runProducers`    |
| Integração   | `events.test.ts:218,244`  | geneses E2E, advance/custom/append/surrogate     |
| Falha        | `events.test.ts:86,244`   | 3 types + prioridade + throw + null + non-array  |
| Segurança    | `events.test.ts:424`      | freeze, cópia, sessions-out; payload exótico     |
| Determinismo | `events.test.ts:244`      | run×2 eventos iguais; surrogates determinísticos |
| Regressão    | suite 284/284             | M002–M006 intactos (255 testes, 0 test-edits)    |

## 5. Third-Eye (ataques)

- Produtor maligno (throw/objecto/facto nulo/non-array/payload exótico) → surrogate, dispatch applied.
- Non-Error throw → `'non-error thrown'` (sem vazar valores arbitrários).
- Append a built-in: ordem built-ins-primeiro (observação compõe, execução não).
- Eventos partilham proveniência frozen (`toBe`); mutações lançam `TypeError`.
- Auditoria: `world.noop` ausente do registo + E2E silencioso (restrição em 2 camadas).

## 6. Gate

- format/lint/typecheck/test/build PASS; 284/284; 100/100/100/100.
- `dist/` com 36 ficheiros engine, 0 testes; só `match.ts` mexido; packages intocados.

## 7. Limitações declaradas

- L-09 estendido: events unbounded in-memory com log/timeline (M062).
- L-15: sem subscrição/delivery (M062/M028, quando houver consumidores).
- L-16: payloads `unknown` (tipar por evento com os domínios).
- L-12/L-13 mantêm-se (cap provisório; detalhe de fault engolido).

## 8. Próximo

M008 Victory Conditions: condições de fim + veredicto determinístico; sem domínios, só esqueleto real.
