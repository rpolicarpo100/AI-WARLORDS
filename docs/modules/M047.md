# M047 — AI Override — VERIFIED

Vote: `override-head` (2026-09-11). Decision: D-041.
Middle of the Refutation → Counterfactual block
(M046–M050): resolution mechanics over the M046
challenge (M044 mold).

## What it ships

- NEW `src/engine/order-override.ts` (L2,
  downward-only): `order.override` {id,kind,
  params?} (owner-only; OWN wire rule —
  L2↛L2 bars reusing issueParamsRule);
  challenge present? head-anchored (index 0)?
  fresh (head kind matches)? → substitute
  passes L0 gates (orderable + envelope) →
  REPLACES queue[0] + clears the refutation
  atomically; rejects leave queue and
  challenge intact with zero new facts.
- Structural fact `order.overridden`
  {player,commander,kind (new),over (old),
  reason} (before/after diff; override is the
  only clearer — exact; `by` audit-only).
- L0 helper `clearRefutation` (commanders.ts):
  drops the challenge key off a fresh copy
  (explicit undefined is FORBIDDEN in state —
  freezeState rejects it; readonly bars delete).
- Match wiring: handler + rule + producer.
- Pins: census `order-override.ts` + LAYERS
  `order-override: 2`.

## Result (frozen)

- Gate: 1568/1568 (1541 intact + 27: 26
  order-override + 1 commanders helper), 100×4
  global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 3 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `5273c1b5…`): 65732 chars (+2250 —
  `order-override` ships wired);
  `order.overridden` ×1 in the bundle;
  `state.json` unchanged, `match.html` re-embedded.

## Files

- NEW: `src/engine/order-override.ts`,
  `src/engine/order-override.test.ts`
- EDIT: `src/engine/commanders.ts`,
  `src/engine/commanders.test.ts`,
  `src/engine/match.ts`,
  `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-041),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
