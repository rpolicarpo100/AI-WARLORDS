# M053 — Memory Attribution + Recall — VERIFIED

Vote: `attribute-recall` + `sides-caller` (2026-09-12).
Decision: D-047. Third of the Memory block
(M051–M054): battle/sighting attribution
(closes the write path) + stream-validated
recall (opens the read path).

## What it ships

- `memories.ts`: `AMBIENT_KINDS`
  (attacked/slain/spotted) + `isAmbientKind`.
- `memory-record.ts`: `toMemory` → `attribute`
  (named resolves to one commander,
  subject-is-self, any active state; ambient
  fans out to the caller's ACTIVE commanders,
  subject-is-unit). Caller-bound: enemy
  dispatches never write your logs (the
  budget-free transition cannot become a
  memory-wipe). CUT: cross-dispatch sightings
  (enemy moves you observe) are not recorded.
- NEW `memory-recall.ts` (L2):
  `recallMemories(record, lookup, subject?)`
  → `{fresh, stale}` (seq resolves with
  agreeing revision + kind = fresh, else
  stale — the forgery signature; consumers
  take `.fresh`, `.stale` audits).
- Match: `recall(commanderId, subject?)`
  (lookup over the stream, fail-soft).
- Pins: census `memory-recall.ts` + LAYERS
  `memory-recall: 2`.

## Result (frozen)

- Gate: 1722/1722 (1700 intact + 22: 5
  memories + 6 record + 7 recall + 4 live),
  100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 4 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `8a8b737b…`): 75322 chars (+936 —
  attribution + recall + Match method);
  `recall` ×1, `fresh` ×1, `stale` ×2 in the
  bundle; `state.json` unchanged (scenario
  lacks active p1 commanders — consistent),
  `match.html` re-embedded.

## Files

- NEW: `src/engine/memory-recall.ts`,
  `src/engine/memory-recall.test.ts`
- EDIT: `src/engine/memories.ts`,
  `src/engine/memories.test.ts`,
  `src/engine/memory-record.ts`,
  `src/engine/memory-record.test.ts`,
  `src/engine/match-memory-record.test.ts`,
  `src/engine/match.ts`,
  `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-047),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
