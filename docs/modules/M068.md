# M068 — AI Arena — Arena UI — VERIFIED

Voto `arena-ui` (2026-09-12): D-062.
LAST of the AI Arena block (M065–M068)
— the block CLOSES here. The arena
becomes playable: a panel in match.html
runs real template-player self-play on
the current position and reports lances,
verdict, scores, rank and the journal.

## What it ships

- `simplePolicy` exported from
  `tools/browser-engine.ts` (first UI
  reader — unshaken; the page calls
  engine rules, never re-implements).
- Arena panel (tArena toggle, arenaRun,
  arenaOut): forks the live snapshot
  (Take-Command mold) → `Match.selfplay`
  → lances/verdict/scores/rank + first
  8 journal lances; graceful toast
  without the engine (engineOK mold).
- `smoke:arena` (`ARENA_TEST=1`): real
  bundle via Function (ATTACK_TEST
  mold, no TEST MOCK) — asserts output
  shape + determinism (back-to-back
  runs identical; playback frames
  between clicks change rev — the
  assert clicks consecutively).

## Result (frozen)

- Gate: 1850/1850 (zero new engine
  tests — the change is UI + one
  export line; UI covered by smoke
  clicks + arena E2E), 100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS (+attack, +arena:
  `lances=40 finished, p1=235 p2=125`),
  4 sim goldens byte-identical (ms-only
  drift restored).
- Bundle rebuilt deterministically
  (`2273e414…`, policy ships): 91336
  chars (+1792); `state.json` unchanged,
  `match.html` re-embedded.
- Infra: sandbox wiped node_modules
  mid-module (`npm ci` restored, 0
  vuln); smoke-match.mjs prettier
  failure is pre-existing (HEAD fails
  too — untouched).

## Files

- EDIT: `tools/browser-engine.ts`,
  `tools/engine.bundle.js`,
  `tools/smoke-match.mjs`,
  `mockups/match.html`, `package.json`,
  `docs/DECISIONS.md` (D-062),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
