# M035 — Effective DNA (VERIFIED @ 2026-09-11)

> Abre o bloco Strategic AI (M035–M042). Executa a
> regra de composição REGISTADA no D-027 (votada):
> effective = clamp(base + deltas, 0–100). Função
> total e pura (L1 sobre as 4 leaves L0). Zero wiring
> (consumers M036+).

## 1. Âmbito (contrato)

- `src/engine/effective-dna.ts` NOVO (L1 —
  strictly-downward sobre commanders/dna/doctrines/
  personalities): NEUTRAL_DNA (10×50 frozen +
  conformance = emperor votado) + effectiveDnaOf
  (base = record-dna ?? preset ?? NEUTRAL; deltas =
  doctrine ?? zero; clamp por traço). Labels
  desconhecidos fail-soft; output fresco; input
  nunca mutado.
- Selo phase1-gate: 2 pins (census + LAYERS
  `effective-dna: 1` — fail-loud, M015).
- Zero wiring (consumers M036+); bundle byte-idêntico
  (md5 6235d684 — rebuild de prova idempotente).
- Cortes (D-029): composer nas leaves (L0↛L0);
  `dnaPresetOf(emperor)!` inline (branch imensurável —
  NEUTRAL + conformance); wiring já (sem consumer);
  tactic weights (D-027).

## 2. Grounding → decisão

- Regra + schedule AMBOS votados (M033 5.º voto:
  aditivo executado por M035+; D-027) + L0↛L0 +
  phase1-gate (downward) + M029 (bare records) —
  âmbito DERIVADO, sem voto novo (voto só quando
  preciso, ordem-bloco).
- 4 goldens corrigidos em voo (presets citados de
  memória errados — matriz votada é oráculo;
  engine sempre teve razão).

## 3. Churn (declarado)

- `effective-dna.ts` + `.test.ts` novos;
  `phase1-gate.test.ts` (2 pins; hunks 57/393
  intactos); D-029.
- state.json/godot/sim/página intocados; sim
  idêntico (1200/500/0); drill idêntico
  (1200/240/0/315/556); verify-bundle SEM regen.
- Prettier: `--write` só nos 2 ficheiros novos;
  D-029 limpo; dirt pré-existente intacto.
- Reports `ms` revertidos, fora do commit.

## 4. Testes (13 novos → 1379/1379)

- `effective-dna.test.ts` 13: neutral (1:
  conformance emperor) + goldens (6: strategist/
  turtle c/ clamp-100, conqueror/blitz c/ clamp-100,
  dna-beats-preset, bottom-clamp 0 + top-100,
  turtle-only, defender-only) + precedência (3:
  bare→neutral, chain estrito, unknown fail-soft) +
  battery (1: 84 combos isDnaTraits ∀) + pureza
  (2: fresh-copy, frozen-input).
- Bateria 1379/1379 (1366 intactos + 13, zero
  regressões); cobertura 100 global + effective-dna.
- 1 spread-vs-assign em teste (props readonly —
  tsc, molde M031).

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0); verify-bundle +
  smoke + smoke:attack verdes; sims regressão limpa.
- RISKS: baixo — pura+total; residual: tuning (#92);
  folha L1 sem importadores até M036+.
- Commit: M035 Effective DNA VERIFIED (1379/1379,
  100 global). BLOCO SEGUE (M036+).
