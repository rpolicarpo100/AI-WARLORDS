# M044 — Order Queue Transitions — VERIFIED

Vote: `fifo-queue` (2026-09-11). Decision: D-038.
Middle of the Player → AI Command block (M043–M045):
mechanics over the M043 queue. Per-kind executable
semantics belong to M045 (declared L2↛L2 cut).

## What it ships

- AMEND M043: `order?` → `orders?` (absent means empty;
  a stale M043 `order` key is ignored, never rejected —
  back-compat pinned by test); cap 8
  (`MAX_ORDERS_PER_COMMANDER`, orders.ts canonical +
  commanders.ts mirror); canonical `isOrderQueue` +
  mirror; `copyRecord` deep-copies the array.
- NEW `src/engine/order-state.ts` (L2, downward-only):
  `order.issue` (owner-only; L0 envelope + cap; FIFO
  append; duplicates apply — position matters) +
  `order.cancel` (owner-only; clears all; empty →
  `applied:false`, no-op precedent); wire-shape
  pre-rules (issue `{id,kind,params?}`; cancel REUSES
  the shared commander-id rule); structural producers
  `order.issued` (queue tail-diff, `{player,commander,
kind,depth}`) + `order.canceled` (American locale,
  `{player,commander,cleared}`).
- Match wiring: handlers + pre-rules + producers.
- Pins: census `order-state.ts` + LAYERS `order-state: 2`.

## Result (frozen)

- Gate: 1491/1491 (1451 intact + 40: 35 order-state +
  4 orders + 1 commanders stale-pin), 100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS, `smoke` PASS,
  3 sim goldens byte-identical (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2): 60919 chars
  (+3481 — `order-state` ships, `orders.ts` rides along
  as its L0 import); `order.issued`/`order.canceled`
  ×1 each in the bundle; `state.json` unchanged,
  `match.html` re-embedded.

## Files

- NEW: `src/engine/order-state.ts`,
  `src/engine/order-state.test.ts`
- EDIT: `src/engine/orders.ts`, `src/engine/orders.test.ts`,
  `src/engine/commanders.ts`, `src/engine/commanders.test.ts`,
  `src/engine/match.ts`, `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-038), `docs/MODULE_STATUS.md`,
  `docs/RISK_REGISTER.md`
