# M030 — Discovery Engine (VERIFIED @ 2026-09-11)

> Pipeline canónico visibilidade→explored→factos (Fase 5 AI
> Foundation, âmbito VOTADO): sources-from-state + acumulação
> em postStep + `cell.discovered` (LOW) + `unit.spotted`
> (NORMAL, #33). L-28 CLOSED (visão stateful existe).

## 1. Âmbito (contrato)

- `fog.ts`: `VISION_RANGE = 2` fixo (votado; canoniza o 2
  ad-hoc das tools) + `sourcesOf(units)` (edge novo
  fog→units L0; vivos hp>0 + owner isPlayerId; coords
  assume-shape — guard dá words, OOB skip-soft no flood).
- `match.ts` postStep (injecção L4; L2↛L2 proíbe nível
  handler): mapless→skip (M015); senão
  computeVisibility(stepped.map, sourcesOf(stepped.units))
  + markExplored(explored ?? fresh). Observação GLOBAL
  (todos os viewers acumulam em cada dispatch — olhos
  abertos, não acção do caller); noop TAMBÉM descobre
  (PROMPTS-consistente; postStep name-blind por desenho).
- `exploration.ts` (edge novo exploration→map L0):
  `discoveredProducer` (diff explored, viewers ascend +
  índices ascend; {player, col, row, terrain} via
  map.cells[index] guarda-undefined, molde M028; LOW) +
  `spottedFacts` puro (before/after units + 2
  visibilidades + width; spotted⟺inimigo∧after-vê-pos_
  after∧¬before-vê-pos_before; {player, unit, owner,
  col, row}; NORMAL) + closure em match.ts que computa
  as visibilidades (recompute aceite, medido).
- Ride universal: discovery+sightings após
  completionProducer em TODA a transição (ordem: domain,
  completions, discoveries, sightings, extras).
- Cortes (D-024, âmbito votado): un-spot/lost-contact;
  resource.discovered (#71 FUTURO); confidence tags (#11);
  visão de cidades (units-only, sem âncora); seam de
  config (DEFAULT_FOG_CONFIG); perceive default (views↛fog
  L2→L2 — caller usa sourcesOf); seeding na génese
  (descoberta ganha-se por dispatch); sim/página (sem UI).

## 2. Grounding → decisão

- 3 votos explícitos do utilizador 2026-09-11 (motor
  completo; células+inimigos; fixo 2) + #10 (eventos
  observados; KNOWN≠REAL) + #33 (ENEMY SCOUT→NORMAL;
  LOW≈tier-movimento p/ células) + #11 (eventos SÃO
  factos) + L-28 (visão stateful — CLOSED) + M013/M014/
  M015/M028 (peças) + M022 (injecção L4) + PROMPTS
  (postStep universal + ride-all) + constraints.
- Gap honesto: explored com zero writers live; visão
  input do caller; range 2 sem âncora — âmbito votado
  antes do código (precedente M027/M029).

## 3. Churn (declarado)

- 5 re-locks exactos (sequências de eventos ganham
  discoveries; surrogate producer-index 1→3 — ride
  precede extras; sem enfraquecimentos).
- `mockups/state.json` regenerado (114038→136528 chars,
  determinístico md5 ×2) + 4 páginas re-embedded +
  `godot/data/state.json` re-synced (sem UI nova).
- Bundle rebuildado (50292 chars, md5 idempotente ×2).
- Sim idêntico (1200/500/0 avgLeft 340.0) +30–50% ms
  (1180 vs 768–916 — 3 fog-computes/dispatch, medido).
- Prettier: 6 warns em regiões HEAD-dirty não tocadas
  (verificado por diff; adições próprias limpas) —
  sem --write (precedente M029).
- `sim/report.json` (ruído ms) revertido, fora do commit.

## 4. Testes (17 novos → 1281/1281)

- `fog.test.ts` +5 (sourcesOf: absent/golden/0hp/
  owner-malformado/RANGE-pin); `exploration.test.ts` +12
  (discovered unit 3; spotted unit 5; dispatch E2E 4:
  golden 2x2 8×discovered + sightings-silentes, corridor
  6x1 stalk→double-spot→tracked→leaving, mapless-silence,
  unitless-shell).
- Bateria 1281/1281 (1264 intactos + 17); cobertura 100
  global (threshold); 2 branches cirúrgicos (cast
  afterViewers — precedente match.ts 382; teste
  roster-vanished).
- Goldens hexadecimais previstos à mão e confirmados
  (2x2 tudo-visível; corredor 6x1 contactos em r3).

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0); verify-bundle +
  smoke + smoke:attack verdes (fork fog 34, battle
  intactos); sim regressão limpa.
- RISKS: médio — postStep universal tocado (mitiga:
  suite+selo+sim+timing); residual: tuning visão 2
  (#92); sem lost-contact; growth-forgery via
  extraHandlers indistinguível (rule unchanged — L2↛L2).
- Commit: M030 Discovery Engine VERIFIED (1281/1281,
  100 global). STOP.
