# M042 — Strategy Transversal Suite — VERIFIED

Vote: `transversal-suite` (2026-09-11). Decision: D-036.
Closes the Strategic AI block (M035–M042). Pure-test module:
zero production code (M026 closing mold).

## What it pins

`src/engine/strategy-transversal.test.ts` (2 tests, in-coverage)
composes the whole M035–M040 AI arc through live Matches:

1. Journey (commission → deactivate → upgrade, one Match):
   - Commission mints bare `c2` at the roster end; counts move
     2/1 → 3/2 and the average becomes the exact mean of the
     three effective vectors (real M035 composer is the
     oracle; thirds asserted with `toBeCloseTo`).
   - Deactivating `c0` moves activity only (3/2 → 3/1): the
     average still covers the whole roster (M036 all-roster
     semantics, pinned through the live chain, not re-united).
   - The free L2→L3 upgrade emits EXACTLY two facts — no
     structural city fact rides along (producers map, M040)
     — and both mirror the live queries deep-equal
     (military / economy / counts / avg / stances); the
     commanderless p2 fact stays honest (`avgEffective`
     key omitted, cityLevel fail-soft 1).
   - Full arc order past `match.started`: commissioned →
     deactivated → ai.assessment ×2.
2. Twin determinism: two Matches replay the 3-dispatch script
   bit-identically (snapshot + events + assessmentOf /
   stancesOf / postureOf for both holders deep-equal).

## Result (frozen)

- Gate: 1417/1417 (1415 intact + 2), 100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS (bundle byte-identical,
  no regen), `smoke` PASS.
- All three sim goldens byte-identical (ms-only regen drift
  restored): playtest, personality, strategy.

## Cuts (declared, D-036)

- playtest `commander.*` candidates: the M041 strategy drill
  already dispatches the AI beats through real Matches —
  regen would be churn without an observable gap.
- 3rd test: redundant with the journey (M026 precedent).
- Gate pins: no new product surface (census excludes tests).

## Files

- NEW: `src/engine/strategy-transversal.test.ts`
- EDIT: `docs/DECISIONS.md` (D-036), `docs/MODULE_STATUS.md`,
  `docs/RISK_REGISTER.md`
