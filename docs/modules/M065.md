# M065 — AI Arena — Self-Play — VERIFIED

Voto `self-play` (2026-09-12): D-059.
FIRST of the AI Arena block (M065–M068) —
a deterministic template player plus the
runner that plays full matches with it
(no RNG, no LLM; the engine plays itself).

## What it ships

- `simplePolicy(snapshot, player, passable)`
  → attack-adjacent > gather-on-node >
  axial-greedy approach > patrol > idle
  (workers brawl; ties by id/col/row;
  passable injected — policy never reads
  map config).
- `Match.selfplay(init, policy)` →
  {match, lances, stalled}: roster order,
  fresh sessions, `selfplay N` requestIds,
  stops on finished OR 2 consecutive
  non-applied lances (no lance cap —
  spend-down proves termination).
- `terrainPassable` extracted (M022 rule,
  single owner); `SelfplayPolicy` type;
  census + `selfplay: 3` layer pins.

## Result (frozen)

- Gate: 1839/1839 (1828 intact + 11:
  7 policy + 4 runner), 100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS (+attack), 4 sim goldens
  byte-identical (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `69e032b7…`): 88999 chars (+449 — the
  static ships, methods on retained
  classes can't shake; `simplePolicy` ×0,
  shaken); `state.json` unchanged,
  `match.html` re-embedded.
- Coverage forced a 4th runner test: the
  passable closure (match.ts:1179) is only
  invoked when the policy walks — gather/
  attack lances never call it.

## Files

- NEW: `src/engine/selfplay.ts`,
  `src/engine/selfplay.test.ts`
- EDIT: `src/engine/match.ts`,
  `src/engine/match-replay.test.ts`,
  `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-059),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
