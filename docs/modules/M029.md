# M029 — Commander State (VERIFIED @ 2026-09-11)

> Fecha o lifecycle do Commander (Fase 5 AI Foundation):
> commission + activate/deactivate + eventos + wiring Match
> (âmbito VOTADO). `active` é marcador sem efeito motor;
> roster N por holder; owner-only fail-closed.

## 1. Âmbito (contrato)

- `commander-state.ts` NOVO (LAYER L2: importa
  authority/commanders/rng L0 + world-state L1 type-only,
  edges downward — molde economy): `commander.commission`
  (sem params — precedente upgrade/noParamHandlers; mint
  `c${nextId}` owner=caller active=true; bootstrap sobre
  dados ausentes; nextId no tecto word→applied:false) +
  `commander.activate`/`commander.deactivate` (params {id} —
  commanderIdParamsRule wire-shape; id desconhecido→
  applied:false; owner≠caller→applied:false fail-closed;
  já-no-estado→applied:false, precedente upgrade) +
  `commanderHandlers()` (3 transições, custo 1 prompt cada —
  custo do dispatch, não efeito).
- Produtores diff-estrutural (molde completionProducer —
  os 3 handlers são os únicos writers, diffs exactos):
  `commissionedProducer` (ids novos→`commander.commissioned`)
  + `stateFlipProducer` (flips→`commander.activated` /
  `commander.deactivated`; nascimentos sem before-entry
  saltados; ordem do array = determinismo).
- Wiring `match.ts`: merge commanderHandlers (tripwire
  duplicate-names cobre) + COMMISSION em noParamHandlers +
  paramRules ACTIVATE/DEACTIVATE + producers.set ×3
  (completionProducer boleia grátis, sem factos).
- Selo phase1-gate: 2 pins (census `commander-state.ts` +
  LAYERS `commander-state: 2` — fail-loud deliberado, M015).
- Cortes (D-023, âmbito votado): semântica mecânica de
  `active` (marcador; dentes→M043+/M035+); name/config
  (M031+ own); orders (M043+ own); seeding no Match
  (commission é acção do holder); post-rule (shape cobre
  via isWorldState, sem fluxo cross-section); sim/página/
  cenário (init-placed, sem commanders semeados).

## 2. Grounding → decisão

- 3 votos explícitos do utilizador 2026-09-11 (lifecycle
  completo; marcador sem efeito; N por holder) + M019
  (molde lifecycle: criação+transições+Match+eventos) +
  M027 (contrato dados: guard global-único, bootstrap
  fail-soft) + M028 ("M029+" é intervalo; sem
  cross-imports) + #21 (Commander possui… — resto→M031+)
  + #29 (hierarquia OBJECTIVE→TACTICAL→FUTURE) +
  constraints (sem LLM obrigatório, zero deps AI —
  TECH_STACK §20; um módulo de cada vez + STOP).
- Master não especifica estado/lifecycle de commanders
  (`active` foi escolha data-first votada em M027) —
  âmbito votado antes do código (precedente M027).

## 3. Churn (declarado)

- `match.ts`: import + const + merge + size-check +
  noParam + paramRules ×2 + producers ×3 (23 linhas);
  zero churn em commanders.ts/views/world-state/
  validation/economy (leitores existentes intactos).
- Bundle rebuildado (48631 chars, md5 idempotente ×2);
  state.json/godot/sim/página intocados; sim idêntico
  (1200/500/0 avgLeft 340.0).
- `phase1-gate.test.ts`/`DECISIONS.md` HEAD-dirty no
  prettier (sujidade pré-existente) → só adições
  cirúrgicas, sem reformat (precedente PROMPTS match.ts).
- Ferramenta: `edit_file` paralelas no MESMO ficheiro
  perdem-se (race last-write-wins) → edições no mesmo
  ficheiro SEMPRE sequenciais + grep-verify (3
  re-aplicações nesta sessão: const match.ts,
  paramRules/producers, census pin).

## 4. Testes (40 novos → 1264/1264)

- `commander-state.test.ts` 40: registo (2), params-rule
  (1+4+3), commission (8: golden/bootstrap, spend,
  sequência, roster-N, evento, no-params, exhausted,
  colisão→HANDLER_FAULT atómica), activate (7),
  deactivate (6), produtores unit (2 + 2 absent-silence),
  integração (4: own-only, marcador-não-gateia,
  noop-silêncio, factories puras + flip-cirúrgico).
- Bateria 1264/1264 (1224 intactos + 40); cobertura
  100 global (threshold) + commander-state/match 100.
- 1 regressão própria apanhada em gate (census pin
  perdido no race) + 1 formato kernel aprendido
  (throw→HANDLER_FAULT, precedente authority.test).

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0, prettier limpo nos
  ficheiros tocados que eram HEAD-clean); verify-bundle
  + smoke + smoke:attack verdes; sim regressão limpa.
- RISKS: baixo — transições novas, leitores zero;
  residual: `active` sem leitor até ordens/IA
  (M043+/M035+); roster sem cap (spam só por bytes).
- Commit: M029 Commander State VERIFIED (1264/1264,
  100 global). STOP.
