# M031 — AI DNA (VERIFIED @ 2026-09-11)

> Abre o bloco Personalidade (M031–M034). Vocabulário
> canónico dos 10 traços + guard + embed opcional no
> CommanderRecord (âmbito VOTADO). Data-first, sem
> writer, sem consumers.

## 1. Âmbito (contrato)

- `dna.ts` NOVO (LEAF L0, zero imports — molde
  commanders): `DnaTraits` (10 traços lowercase, ordem
  #22) + DNA_MIN/MAX (0/100 int, votado; eixos
  independentes, sem soma-fixa) + TRAIT_IDS +
  isDnaTraitId + isDnaTraits (10 presentes + int-range;
  extras ignorados — M015); forward-placed p/ M032+
  (precedente M014→M015; zero importadores prod).
- `CommanderRecord.dna?` (embed votado) + guard-mirror
  TOTAL em commanders.ts (interface + ids + bounds +
  isMirroredDna — L0↛L0 proíbe importar dna.ts) +
  queries hardenadas (copyRecord: dna por cópia fresca,
  sem aliasing).
- Sem slot novo (dna boleia commanders; shape cobre);
  views/Match/validation zero churn (records integrais
  fluem; F-09 top-level — não tripa).
- Selo phase1-gate: 2 pins (census `dna.ts` + LAYERS
  `dna: 0` — fail-loud deliberado, M015).
- Cortes (D-025, âmbito votado): writer (init-placed —
  M027; dono FUTURO unsettled); consumers/influência
  (M032+/M035+; #25 ≠SKILL noted); `name` (ainda-cut,
  sem dono); commission-com-dna (M029 owns, sem params).

## 2. Grounding → decisão

- 3 votos explícitos do utilizador 2026-09-11
  (data-first; extend record; 0–100) + #22 (10 params;
  influencia-nunca-determina) + #21 (cada Commander
  possui DNA) + #25 (PERSONALITY≠SKILL) + M027 (molde
  data-first) + M016 (espelhos) + M015 (leniência) +
  M028 ("M031+" é intervalo) + constraints.
- M031=DNA inferido da ordem do bloco
  (DNA→M031…Testing→M034); master sem escala/writer —
  âmbito votado antes do código (precedente M027+).

## 3. Churn (declarado)

- `commanders.ts`: embed + mirror + copyRecord;
  `commanders.test.ts`: battery + pin (hunk 107
  HEAD-dirty intacto); zero churn resto do motor.
- Bundle rebuildado (50725 chars, md5 idempotente ×2;
  só o mirror embarca — dna.ts canónico inalcançável,
  `adaptability` ×1 como prova).
- state.json/godot/sim/página intocados; sim idêntico
  (1200/500/0 avgLeft 340.0); verify-bundle passou
  SEM regen (sem commanders no trace).
- Prettier: warns só em regiões HEAD-dirty não tocadas
  (verificado por diff); ficheiros novos + hunk próprio
  limpos (precedente M029).
- `sim/report.json` (ruído ms) revertido, fora do commit.

## 4. Testes (49 novos → 1330/1330)

- `dna.test.ts` 22: vocabulário (3: lista-10, escala,
  trait-id) + guard (19: golden, fronteiras ×20,
  abuso ×9/traço, missing, extras, non-object ×6).
- `commanders.test.ts` +27: embed (14: retro-compat,
  golden, abuso ×10/traço, fresh-copy, slot round-trip
  + reject-loud) + cross-check mirror (12: válidos 21,
  battery ×10/traço, non-object) + pin percepção (1:
  own-dna flui, foe-dna fora — em commanders.test,
  precedente M027, não views.test).
- Bateria 1330/1330 (1281 intactos + 49, zero
  regressões); cobertura 100 global + dna/commanders.
- 3 casts `as unknown as` em testes (exigência tsc,
  sugestão do próprio compilador).

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0); verify-bundle +
  smoke + smoke:attack verdes; sim regressão limpa.
- RISKS: baixo — dados puros + guards; residual:
  mirror-drift (mitiga: battery 40+/traço); influência
  por definir (M032+); folha sem importadores.
- Commit: M031 AI DNA VERIFIED (1330/1330, 100
  global). STOP.
