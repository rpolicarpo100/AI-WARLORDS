# M070 — Multiplayer — Presence — VERIFIED

Voto `presence` (2026-09-12): D-064.
SECOND of the Multiplayer block
(M069–M076) — sessions gain life:
heartbeats, rosters, lazy sweeps and
live SSE presence chatter.

## What it ships

- `createTransport(now = Date.now)`
  (injectable clock): POST /:id/heartbeat
  → {online} (sorted, deduped); POST
  /:id/leave (deletes + reports rest).
- Lazy sweep atop join/dispatch/heartbeat/
  leave/events (state deliberately pure):
  idle past 30s dies (strict boundary —
  timeout = death, rejoin); dispatches
  count as activity.
- `event: presence` SSE lines (live-only,
  engine cursor untouched): join/leave/
  timeout announce; late roster = heartbeat.
- 10 tests (manual-clock server swap,
  zero M069 churn — black-box intact):
  roster sort/dedup, bad beats/goodbyes,
  join-live-never-backlog, boundary pin,
  sweep-death-rejoin, dispatch-refresh.

## Result (frozen)

- Gate: 1886/1886 (1876 intact + 10),
  100×4 global (transport.ts 100 —
  default clock + injected clock both
  hit naturally).
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS (+arena), 4 sim goldens
  byte-identical (ms-only drift restored).
- Bundle untouched (91336, SAME md5);
  `match.html` not re-embedded.

## Files

- EDIT: `src/server/transport.ts`,
  `src/server/transport.test.ts`,
  `docs/DECISIONS.md` (D-064),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
