# M006 — Action Validation (VERIFIED)

> Checkpoint M006 @ 2026-09-10 · 41 novos testes (255/255) · 100×4 · TEST MOCKs rotulados

## 1. Contrato

- Pré-regras colecionadas em ordem (sem short-circuit) + `no-params` real nos built-ins.
- Pós-invariantes impostas: shape, roster exacto, tick monotónico, cap 1MB → `HANDLER_FAULT`.
- Wiring rng→handlers (fecha L-10): stream por dispatch via `deriveSeed(seed, seq)`.
- Match detém o registo: validação fail-fast + wrap de todos os handlers; kernel intocado.

## 2. Pré-análise (decisões)

- `TransitionContext` real = `{state, caller, params}` (sem kernel/request): nome resolve-se
  no wrap; seq via closure-contador (determinística; faults consomem ordinal sem logar).
- Regra de schema descartada: subsumida pelo shape-gate em single-version (sem regras mortas).
- Kernel engole detalhe de fault (contrato M003): asserts rule-specific via invocação directa.
- Pré-domínio chega com os domínios (M018+); mecanismo provado com TEST MOCKs rotulados.

## 3. Implementação

- `src/engine/validation.ts` — `wrapWithValidation` (L115), regras (L50–L96), `RngHandler` (L103).
- `src/engine/rng.ts` — `deriveSeed` (L54): mixing determinístico, não-criptográfico, com goldens.
- `src/engine/match.ts` — registo validado + wrap + contador (L158–L172); resto byte-idêntico.

## 4. Testes (6 categorias)

| Categoria    | Onde                                                   | O quê                                         |
| ------------ | ------------------------------------------------------ | --------------------------------------------- |
| Unit         | `validation.test.ts:140,149`, `rng.test.ts:91`         | composição, no-params, `deriveSeed`           |
| Integração   | `validation.test.ts:208,302,349`                       | no-params E2E, contenção, draws run×2         |
| Falha        | `validation.test.ts:275,295,333`                       | gate, 5 corrupts directos, registo            |
| Segurança    | `validation.test.ts:302` + adversarial `as unknown as` | 5 corrupts contidos, estado intacto           |
| Determinismo | `validation.test.ts:349`, goldens `rng.test.ts:91`     | streams (seed,seq); dup não consome           |
| Regressão    | suite 255/255                                          | M002–M005 intactos (214 testes, 0 test-edits) |

## 5. Third-Eye (ataques)

- 5 handlers corruptos (shape/roster±/reorder/tick/size 1.5MB): fault contido, tudo intacto.
- Payload em built-ins → rejected (não fault); unserializable → rejected (não fault).
- Registo com valor não-função e nome vazio → rejeitados na construção.
- Bug real apanhado pelos testes: regras 1-param recebiam `before` (corrigido, L65/L88).
- Auditoria fault-path: sem dessincronização M005 (kernel não loga faults — verificado).

## 6. Gate

- format/lint/typecheck/test/build PASS; 255/255; 100/100/100/100.
- `dist/` com 32 ficheiros engine, 0 testes; `authority.ts` intocado; packages intocados.

## 7. Limitações declaradas

- L-08 CLOSED (cap 1MB/escrita); L-10 CLOSED (wiring entregue, sem consumidor ainda).
- L-12: cap provisório (M088 mede tamanhos reais).
- L-13: detalhe de fault engolido pelo kernel (contrato M003; diagnóstico via testes directos).
- L-09 (M005) mantém-se: log/timeline unbounded (M062).

## 8. Próximo

M007 Event System: canal de eventos; emissores de domínio chegam com os domínios.
