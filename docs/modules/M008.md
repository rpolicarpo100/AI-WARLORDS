# M008 — Victory Conditions (VERIFIED)

> Checkpoint M008 @ 2026-09-10 · 29 novos testes (313/313) · 100×4 · TEST conditions rotuladas

## 1. Contrato

- Veredicto lazy (função pura do estado): `ongoing | finished{win|draw, condition, tick, revision}`.
- Única condição real: time-limit sobre ticks (#46); sem score → draw (vencedor seria inventado).
- Terminalidade imposta: dispatch pós-fim → `{error, MATCH_FINISHED}`, sem tocar o kernel.
- Decisões validadas estritamente (vencedor ∈ roster); lixo = fail-stop loud (nunca inventar, #40).
- Evento `match.finished` (HIGH) exactamente-uma-vez; extras-antes-builtins em empates.

## 2. Pré-análise (decisões)

- Lazy > armazenado: sem staleness, sem mutação sob freeze; guarda prova `wasFinished=false`.
- Draw-sem-score documentado como regra Phase-1; comparação de score chega com scoring (M016+).
- Extras primeiro: específico-vence-geral (eliminação futura bate limite no mesmo tick).
- `try/finally` no dispatch: throw de condição nunca diverge log/timeline (testado).
- `maxTicks` opcional: untimed = comportamento M007 idêntico (regressão por construção).

## 3. Implementação

- `src/engine/victory.ts` — `evaluateVictory` (L81), `timeLimitCondition` (L44), validação estrita.
- `src/engine/match.ts` — guarda (L248), `getVerdict` (L324), geneses+avaliação, outcome alargada (L114).
- `src/engine/events.ts` — `matchFinishedEvent` (L74, aditivo).

## 4. Testes (6 categorias)

| Categoria    | Onde                          | O quê                                             |
| ------------ | ----------------------------- | ------------------------------------------------- |
| Unit         | `victory.test.ts:70,90,109`   | limite, registo, avaliador+localizadores          |
| Integração   | `victory.test.ts:177`         | fim-no-limite, born-finished, win/ordem TEST      |
| Falha        | `victory.test.ts:128,168,177` | 7 decisões + 6 maxTicks + throw mid-match         |
| Segurança    | `victory.test.ts:128,348`     | vencedor∉roster, freeze, frescura, bloqueio total |
| Determinismo | `victory.test.ts:177`         | veredicto+evento exactos por (tick,revision)      |
| Regressão    | suite 313/313                 | M002–M007 intactos (284 testes, 0 edits)          |

## 5. Third-Eye (ataques)

- Vencedor forjado (não-id, fora-do-roster, kind lixo, id lixo ×3) → fail-stop, tudo intacto.
- Condição que rebenta mid-match: throw + revision aplicada + log==timeline (coerência provada).
- Pós-fim: dispatches bloqueados; log/timeline/events/tick/revision congelados (assert total).
- Born-finished (estado importado tick 100): geneses duplo + bloqueio imediato.
- Veredicto partilhado ongoing (`toBe`); finished fresco por chamada (mutação inócua + frozen).

## 6. Gate

- format/lint/typecheck/test/build PASS; 313/313; 100/100/100/100.
- `dist/` com 30 ficheiros engine, 0 testes; kernel e 7 módulos intocados; packages intocados.

## 7. Limitações declaradas

- L-17: time-draw sem score (comparação chega com scoring, M016+).
- L-18: elimination por implementar (vidas/estruturas com domínios, M023+).
- L-09/L-12/L-13/L-15/L-16 mantêm-se.

## 8. Próximo

M009 Core Engine Test Suite: suite transversal do motor (Fase 1 fecha); depois M010 (mapas).
