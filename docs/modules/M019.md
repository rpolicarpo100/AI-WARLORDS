# M019 — City System (VERIFIED @ 2026-09-11)

> Lazy-cities + fila de construção + conclusão no advance; factos
> `build.started`/`build.completed`; upgrade 1→2→3 gratuito; sem grants
> (L-30 CLOSED); sem invariante nova (M020 owns).

## 1. Âmbito (contrato)

- `city.ts` NEW L0: `CitiesData` holder-map `{ level: 1|2|3, queue:
[{ type: QueueBuilding, remaining }] }`, guards, `cityOf` fail-soft
  (deep-copy; virtual idle level-1 fora do mapa).
- `economy.ts` L2: `BUILD_TRANSITION` (`city.build`, paga+enfilera),
  `UPGRADE_TRANSITION` (`city.upgrade`, gratuito, cap 3, silencioso),
  `completeConstructions` (decrementa+conclui+thread counts),
  `build.started`/`build.completed` `{ player, building }`.
- `match.ts`: `buildingsConfig?` seam + city merge + `match.advance`
  emendado (cityless early-return = identidade M005; spread condicional).
- `WorldState.cities?`; percepção own-only `city` (F-09 9/8).
- Census gate 19; LAYERS += `city: 0`; composition 5 intacta.
- Cortes (D-012): sem `city.found`, sem grants, sem queue-cap/gating,
  sem level-costs-effects, sem pop-cap, sem capture, sem post-rule nova.

## 2. Grounding → decisão

- Master: #16 (uma cidade + LEVEL 1→2→3 + modular + 6 edifícios), #20
  (BUILD_STARTED/BUILD_COMPLETED), #83 (BUILD TIME), #99 (1 CITY).
- Ungrounded fechado por D-012 (ACCEPTED pré-código): lazy-cities
  (init-placed + auto-materialize, sem found), fila `{type, remaining}`,
  upgrade gratuito →3, conclusão no advance (sem regra nova), L-30
  fecha sem-grants (nenhum módulo concede nada).

## 3. Implementação

- `QueueBuilding` espelha `BuildingId` (L0↛L0: sem import; uniões
  idênticas, mutuamente atribuíveis — tsc prova; teste cruza exacto).
- Conclusão difere `buildings` counts por holder×tipo (só a conclusão
  escreve counts — exacto sob duplicados); holders ordenados
  (precedente M014), ordem `BUILDING_IDS` dentro.
- Cidade sem dono fora do mapa: build/upgrade percebem e materializam
  sempre sobre o próprio holder; percepção alheia nunca vaza (fail-closed).
- Producer de conclusão corre no path de extras do sistema (após
  matchAdvanced, antes de extras do caller) — sem branch morto (100×4).

## 4. Testes (87 novos → 932/932)

- `city.test.ts` 47: guards, níveis, `cityOf` fail-soft, espelhos exactos.
- `economy.test.ts` +36: pre-rule, handlers directos, completeConstructions,
  producers, 12 E2E (goldens: started, conclusão+sequência, 2-tick, ordem
  multi `[p1-house,p1-wall,p2-tower]`, default zero-time, rejeições,
  upgrade-flow, init-placed, no-params, identidade cityless), WSext,
  percepção.
- `match.test.ts` +4: `buildingsConfig` inválido rejeita.
- `views.test.ts`: F-09 9/8 + 2 goldens com `city`.
- Cobertura 100×4 global; `dist/engine` 80/0 (city +4 artefactos).

## 5. Gate e fecho

- Gate: census 19, `city: 0`, selo intacto, banned-clean, composição 5.
- L-30 CLOSED (sem grants inscrito); enforcement/caps→M020; L-32 segue.
- RISKS: médio-baixo — primeira emenda VERIFIED-behaviour (advance),
  cercada por identidade-cityless + selo + goldens; residual: fila
  ilimitada e upgrade gratuito são placeholders declarados (M020+).
- Commit: M019 City System VERIFIED (932/932, 100×4). STOP.
