# M074 — Online orders (D-068)

- Status: VERIFIED+LIVE (2026-09-12)
- Panel: inputs `netUnit` (u1) /
  `netTarget` (u2) / `netTo` (1,1)
  + botão Move; Attack usa
  (unit,target) livres; Move faz
  parse `col,row` → unit.move
  {id,col,row} (shape probado por
  curl, jamais assumido).
- Hints: `unit and target first`,
  `unit first`, `dest must be
  col,row`; sem sessão → join first.
- Tests: suite 1898/1898 100×4;
  base smoke clica netMove
  (no-crash); API_TEST prova
  bogus→hint + move u1→1,1
  `applied rev 3` + SSE
  `unit.moved`; harness "digita"
  os inputs (stubs não parseiam
  `value=`).
- Bundle untouched (91336, SAME
  md5); `match.html` re-embedded.
