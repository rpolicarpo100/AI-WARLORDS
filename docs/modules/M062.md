# M062 — Replay-Verify — VERIFIED

Vote: `replay-verify` (2026-09-12).
Decision: D-056. FIRST of the Replay block
(M062–M064): the dispatch journal + redrive
rebuild a match bit-identically (outcomes,
snapshot, events, timeline, revision).
Twins prove scripts; the journal proves
history.

## What it ships

- `JournalEntry` {requestId, playerId,
  sessionPlayer, type, payload, outcome-
  verbatim} + journal push in dispatch
  finally (applied+rejected+error;
  nullish noise skips, post-finish no-ops
  never reach) + `getJournal()` +
  `Match.replay(init, journal)` static
  (fresh Match, session per entry,
  redispatch, {match, outcomes}).
- Kernel log has no payloads (grounded —
  journal is the only redrive path).
  Forgeries replay exactly (sessionPlayer).
- Zero new engine files → zero pins.

## Result (frozen)

- Gate: 1820/1820 (1815 intact + 5:
  mixed-script + empty + 2 tampers +
  spoof-error), 100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 4 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `e087bb52…`): 88550 chars (+410);
  `getJournal` ×1 in the bundle;
  `state.json` unchanged, `match.html`
  re-embedded.

## Files

- NEW: `src/engine/match-replay.test.ts`
- EDIT: `src/engine/match.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-056),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
