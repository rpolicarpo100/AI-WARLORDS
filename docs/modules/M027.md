# M027 — Commander Core (VERIFIED @ 2026-09-11)

> Abre a Fase 5 (AI Foundation). Entidade Commander data-first
> (âmbito VOTADO): roster id/owner/active + guards + queries +
> slot WorldState + percepção own-only. Sem comportamento.

## 1. Âmbito (contrato)

- `commanders.ts` NOVO (LEAF L0, zero imports — molde
  units.ts): `CommanderRecord {id, owner, active}` (strings
  holder-id, NÃO PlayerId — L0↛L0; `active` armazenado,
  semântica→M029+ — precedente M014→M015) +
  `CommandersData {schemaVersion, nextId, commanders[]}` +
  guards strictos (version exacta, nextId word, ids únicos) +
  `commandersOf`/`commanderById` fail-soft com cópia fresca
  (precedente cityOf). Bounds 64 espelhados (teste
  cross-check: stockpiles + authority).
- `WorldState.commanders?` + init + guarda (molde units;
  "Init-placed until M029+ lifecycle"); sem regra (shapeRule
  cobre via isWorldState — zero churn validadores/Match).
- Percepção own-only `commanders` em views.ts (molde units;
  inimigo fail-closed →M028+; tripwire F-09 11/10).
- Suite `commanders.test.ts` nova (41 testes).
- Cortes (D-020, âmbito votado): commission/transições/
  config/Match (sem comportamento; criação→dono lifecycle —
  precedente M018→M019); campo `name` (ungrounded; M031+
  own); orders (M043+ own); sim/página/cenário (init-placed).

## 2. Grounding → decisão

- Voto explícito do utilizador 2026-09-11 (data-first
  id/owner/active; reconheceu grounding fino) + M021 (molde
  L0 + slot + own-only) + M016 (espelhos) + M018 (data sem
  criação) + M015 (tripwire) + constraints (sem LLM
  obrigatório, zero deps AI — TECH_STACK §20).
- Selo/census re-locked (2 pins phase1-gate: ficheiro novo
  + LAYERS `commanders: 0` — fail-loud deliberado, M015).

## 3. Churn (declarado)

- views.test.ts: 2 goldens + tripwire ganham `commanders`
  (molde M021); zero churn de produção além do slot.
- Bundle rebuildado (44800 chars, idempotente md5);
  state.json intocado; sim idêntico (1200/500/0).
- node_modules limpo entre turnos (não persiste) → `npm ci`
  antes dos gates (0 vuln).

## 4. Testes (41 novos → 1166/1166)

- `commanders.test.ts` 41: record (1+3+4+3+5), data
  (1+3+3+4+1+1), queries (3+2), mirrors (2), slot
  WorldState (round-trip, reject-loud, absent), percepção
  (own-only, absent→[]).
- Bateria 1166/1166 (1125 intactos + 41); cobertura 100×4
  (All-files + commanders + world-state + views 100).

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0); verify-bundle + smoke +
  smoke:attack verdes; sim regressão limpa.
- RISKS: baixo — dados puros + guards; residual: `active`
  sem leitor até M029+; init-only até lifecycle owner.
- Commit: M027 Commander Core VERIFIED (1166/1166, 100×4). STOP.
