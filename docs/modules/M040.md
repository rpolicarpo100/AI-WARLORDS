# M040 — AI Assessment Events (VERIFIED @ 2026-09-11)

> 6.º do bloco Strategic AI. 1.º canal IA→stream
> (âmbito VOTADO): snapshots 'ai.assessment' por
> holder em cada upgrade (assessment full + stances,
> priority low). Factory L3 + registo Match.
> Mockups regen (M022 precedente, +72/-0).

## 1. Âmbito (contrato)

- `src/engine/ai-events.ts` NOVO (L3 — L2s + L0/L1,
  downward; tipos inline, molde commander-state):
  createAiAssessmentProducer(unitsConfig): por
  holder emite 'ai.assessment' low {player, military,
  economy, commanders: {count, active, avgEffective?,
  stances}} (avg OMITIDO sem commanders — freezeState
  proíbe undefined). statsOf `as UnitType` (seam
  validada M006).
- `match.ts`: regista factory em UPGRADE_TRANSITION
  (1.º domain producer; riders atrás).
- Selo phase1-gate: 2 pins (census cabeça + LAYERS
  `ai-events: 3`).
- Bundle: 56663 (+485), md5 `0911fb1e…` ×2.
- Cortes (D-034): stance-em-commission (FACTO bare);
  flips/noop/build/move (ruído); advance (não
  existe); assessment-sem-dano (2 verdades).

## 2. Grounding → decisão

- Voto ai-events + #6 (EVENT→REPLAY→ANALYTICS) +
  M007 + M030 (precedente) + norma exposição
  (factos já públicos) + página tolerante +
  raridade upgrade (49/1200; drill 0).
- 3 achados em voo: (1) payloads exigem
  JSON-exato (undefined rebenta freezeState →
  omissão); (2) verify predito 79-vs-77 ✓;
  (3) mockups INCONSISTENTE (golden s/ unitsConfig,
  live c/ scenario — latente até o producer
  expor; fix: generator passa scenarioUnitStats).

## 3. Churn (declarado)

- `ai-events.ts` + teste novos; `match.ts`
  (registo); gate (2 pins); D-034; mockups REGEN
  (state.json +72/-0 SÓ factos; 4 html rebuild
  c/ bundle novo) + generator fix (1 linha).
- Snapshots/outcomes idênticos (golden prova);
  sim idêntico (1200/500/0); drill idêntico
  (1200/240/0/315/556); M034 journey intacta.
- Prettier: `--write` teste; D-034 reworded
  (leading-`+`); dirt intacto.
- Reports `ms` revertidos, fora do commit.

## 4. Testes (5 novos → 1415/1415)

- `ai-events.test.ts` 5: goldens upgrade (2 factos
  full ambos holders, ordem players) + repeat
  2→3 + silêncio non-upgrade + defaults-config
  (dano 0) + gémeos determinismo.
- Bateria 1415/1415 (1410 intactos + 5, zero
  regressões); cobertura 100 global + ai-events.

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0 incl. generator);
  verify-bundle PASS pós-regen; smokes verdes;
  sims regressão limpa.
- RISKS: médio — 1.º evento IA + regen; residual:
  tuning (#92); latência mockups fechada.
- Commit: M040 AI Assessment Events VERIFIED
  (1415/1415, 100 global). BLOCO SEGUE (M041+).
