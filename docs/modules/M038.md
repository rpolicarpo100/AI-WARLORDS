# M038 — Match Assessment Wiring (VERIFIED @ 2026-09-11)

> 4.º do bloco Strategic AI. 1.º wiring real (âmbito
> VOTADO): Match ganha queries read-only assessmentOf/
> stancesOf sobre o substrato M035–M037. IA embarca
> no bundle pela 1.ª vez. Zero estado/eventos/schema.

## 1. Âmbito (contrato)

- `match.ts` (L4): field `unitsConfig` privado +
  assessmentOf(holder) (statsOf sobre unitsConfig;
  `as UnitType` em seam validada — precedente M029;
  lookups re-guardam) + stancesOf(holder) (ordem
  roster). `stance.ts`: CommanderStance {id, stance}.
- Selo sem pins (sem ficheiro novo; edges novas
  downward — gate prova).
- Bundle REBUILDADO c/ IA: 55899 chars (+4820),
  md5 `2b2d179a…` idempotente ×2.
- Cortes (D-032): producers por-dispatch; snapshot
  enrich (F-09); adapter c/ branch imensurável.

## 2. Grounding → decisão

- Voto wiring 2026-09-11 + #5/#6 (Match é o
  agregado) + M006 (estados validados) + M029
  (cast em seam validada) + M036/M037.
- CORREÇÃO metodológica: bundle tem 1 linha
  (minificado) — `grep -c` era vacuo (0/1); prova
  passa a ocorrências `grep -o|wc -l`. Claims
  M032/M033 continuam verdadeiros (sem path de
  import), métrica é que era fraca. M038 prova
  com ocorrências (×3/×2/×1 acima).

## 3. Churn (declarado)

- `match.ts` (+2 métodos, field, imports);
  `stance.ts` (+interface); teste novo; D-032.
- state.json/godot/sim/página intocados; sim
  idêntico (1200/500/0); drill idêntico
  (1200/240/0/315/556); verify-bundle SEM regen
  (passa sobre bundle novo).
- Prettier: `--write` teste novo; match/stance/
  D-032 limpos; dirt pré-existente intacto.
- Reports `ms` revertidos, fora do commit.

## 4. Testes (5 novos → 1402/1402)

- `match-assessment.test.ts` 5: journey (golden +
  commission atualiza counts + média /3 exacta) +
  stancesOf (goldens + ordem + unknown []) +
  unknown-holder zeros + defaults-config (dano 0,
  hp live) + pureza (fresh-copy ambos).
- Bateria 1402/1402 (1397 intactos + 5, zero
  regressões); cobertura 100 global + match/stance.

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0); verify-bundle +
  smoke + smoke:attack verdes sobre bundle novo;
  sims regressão limpa.
- RISKS: médio-baixo — read-only, mas 1.º wiring;
  residual: superfície API; bundle +4820.
- Commit: M038 Match Assessment Wiring VERIFIED
  (1402/1402, 100 global). BLOCO SEGUE (M039+).
