# M046 — AI Refutation Data — VERIFIED

Vote: `refute-data` (2026-09-11). Decision: D-040.
Open of the Refutation → Counterfactual block
(M046–M050): data-first foundation (M043 mold).
A refutation challenges one queued commander
order with an auditable reason.

## What it ships

- NEW `src/engine/refutations.ts` (L0, zero
  imports): `REFUTATION_REASONS` closed
  (alphabetical, locked) — blocked, out-of-range,
  redundant, suicidal, unaffordable — +
  `CommanderRefutation` {orderIndex uint32, kind
  (OrderKind mirror, L0↛L0), reason, by
  (holder-id 64)} + total guards (extras
  ignored). Self-validating reference: kind
  confirms index (mismatch=stale → M047+
  fail-closed). Self-vs-peer unrestricted at
  data level (M047 decides).
- Mirror in `commanders.ts` (L0↛L0 law):
  `refutation?` (absent means none) + mirror
  guard + copyRecord spread.
- Pins: census `refutations.ts` + LAYERS
  `refutations: 0`.

## Result (frozen)

- Gate: 1541/1541 (1520 intact + 21: 18
  refutations + 3 commanders mirror), 100×4
  global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 3 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `636cdfbd…`): 63482 chars (+427 —
  `refutations` ships unreferenced, zero readers);
  `orderIndex` ×1 in the bundle; `state.json`
  unchanged, `match.html` re-embedded.

## Files

- NEW: `src/engine/refutations.ts`,
  `src/engine/refutations.test.ts`
- EDIT: `src/engine/commanders.ts`,
  `src/engine/commanders.test.ts`,
  `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-040),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
