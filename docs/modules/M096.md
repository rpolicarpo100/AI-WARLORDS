# M096 — Free exclusion (D-091, voto m096: exclude-both)

- Status: VERIFIED+LIVE (2026-09-12)
- POST /match aceita mode?
  (400 'bad mode'); close free
  = early-return void (sem
  history, sem ratings — R-32);
  standard intacto.
- Tests +2 HTTP real (77/77
  transport): bad modes; free
  12 noops past-10 + close
  void (results+ratings
  byte-identical before/after).
- Suite 1960/1960 100×4;
  bundle SAME (server-only);
  lobby intocado.
