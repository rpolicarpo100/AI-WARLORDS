# M056 — Directive Set + Stance Precedence — VERIFIED

Vote: `directive-set-stance` (2026-09-12). Decision:
D-050. Second of the Player ↔ Commander block
(M055–M058): mechanics over the M055 set +
player-ordered posture wins (stance half
complete end to end).

## What it ships

- NEW `src/engine/directive-state.ts` (L2,
  order-state mold): `directive.set`
  {id,kind,value} (overwrite) + `directive.clear`
  {id,kind} (emptied sets drop the key whole —
  M047 law). Owner-only; unknown commanders
  and clearing unset kinds fail closed ('not
  set' mirrors 'cancel: no orders').
  Structural producers emit set/cleared facts
  (NOT memorable). replaceRecord local
  (L2↛L2).
- Match: merge into the commanders map +
  paramRules + producers.
- Precedence: stanceWithRecall returns
  directives.stance first (player > recall >
  DNA); stanceOf intact. Export aditivo
  isDirectiveStance (M055).
- Pins: census `directive-state.ts` + LAYERS
  `directive-state: 2`.

## Result (frozen)

- Gate: 1761/1761 (1746 intact + 15: 14
  directive-state + 1 stance), 100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 4 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `8acbf26c…`): 79733 chars (+3564 —
  set/clear verbs + producers + wiring);
  `directive.set` ×4, `directive.clear` ×5 in
  the bundle; `state.json` unchanged,
  `match.html` re-embedded.

## Files

- NEW: `src/engine/directive-state.ts`,
  `src/engine/directive-state.test.ts`
- EDIT: `src/engine/directives.ts` (export),
  `src/engine/stance.ts`,
  `src/engine/stance.test.ts`,
  `src/engine/match.ts`,
  `src/engine/phase1-gate.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-050),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
