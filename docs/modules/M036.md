# M036 — Player Assessment (VERIFIED @ 2026-09-11)

> 2.º do bloco Strategic AI. 1.º tijolo do STRATEGIC
> ENGINE (âmbito VOTADO): assessment pura por holder
> sobre factos do engine + effective DNA. Contagens,
> somas e médias exactas — zero pesos, zero wiring
> (consumers M037+).

## 1. Âmbito (contrato)

- `src/engine/assessment.ts` NOVO (L2 — getters
  L0 + effective-dna/world-state-type L1, downward):
  assessPlayer(state, holder, statsOf): military
  {units, totalHp, totalDamage} (statsOf injectado,
  molde M022; unknown-type dano 0); economy
  {stockpile, buildings, cityLevel} (stockpileOf,
  countsOf, cityOf); commanders {count, active,
  avgEffective} (média exacta; zero → undefined).
  AverageDna floats (NÃO-DNA — bounds-battery).
- Selo phase1-gate: 2 pins (census cabeça +
  LAYERS `assessment: 2` — fail-loud, M015).
- Zero wiring (consumers M037+); bundle byte-idêntico
  (md5 6235d684 — rebuild de prova idempotente).
- Cortes (D-030): import warfare (L2↛L2); scalar
  strength (pesos→FUTURO); rounding (floats exactos);
  threat/foe-modeling (FUTURO); wiring já.

## 2. Grounding → decisão

- Voto assessment 2026-09-11 + #7 (ENGINE=FACTS,
  AI=DECISIONS) + M022 (injecção) + M015
  (leniência) + getters L0 fail-soft + M035.
- 1 rewrite em voo (overload fantasma + `??`
  imensurável + cast Record — versão final sem
  casts, 100 branches à primeira).

## 3. Churn (declarado)

- `assessment.ts` + `.test.ts` novos;
  `phase1-gate.test.ts` (2 pins; hunks 57/393
  intactos); D-030.
- state.json/godot/sim/página intocados; sim
  idêntico (1200/500/0); drill idêntico
  (1200/240/0/315/556); verify-bundle SEM regen.
- Prettier: ficheiros novos limpos à primeira;
  D-030 limpo; dirt pré-existente intacto.
- Reports `ms` revertidos, fora do commit.

## 4. Testes (7 novos → 1386/1386)

- `assessment.test.ts` 7: goldens (2: holder cheio
  c/ média exacta 37.5…, holder sparse c/ zeros +
  city-1 + undefined) + edges (2: unknown-type,
  bare-state) + battery (1: 11 commanders, avg
  finito ∈ [0,100]) + pureza (2: fresh-copy,
  no-mutate).
- Bateria 1386/1386 (1379 intactos + 7, zero
  regressões); cobertura 100 global + assessment.
- 21/21 narrowed à primeira (7 + 14 gate).

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0); verify-bundle +
  smoke + smoke:attack verdes; sims regressão limpa.
- RISKS: baixo — pura; residual: tuning (#92);
  folha L2 sem importadores até M037+.
- Commit: M036 Player Assessment VERIFIED (1386/1386,
  100 global). BLOCO SEGUE (M037+).
