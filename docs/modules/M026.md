# M026 — Military Tests (VERIFIED @ 2026-09-11)

> Fecha a Fase 4 (Military M021–M026). Módulo PURO DE TESTES:
> drills militares no sim (attack/train + invariantes) + suite
> transversal (journey do arco + determinismo). Zero produção.

## 1. Âmbito (contrato)

- `sim/playtest.ts` (toolchain, fora do coverage): candidatos
  `unit.attack` (foe adjacente via neighbours bot-side) +
  `unit.train` (3 tipos × célula aleatória) — oráculo sempre o
  engine; `DRILL_UNITS` (maxHp 5/12/8 == roster vale.tmj;
  custos/dano = reuse dos números test-proven; tuning real é
  #92); invariantes militares (hp≤maxHp, ids únicos+sufixo<
  nextId, nextId monótono) + os existentes (hp≥1, bounds,
  piles, tick, JSON round-trip); evidência (applied por-tipo,
  kills/dano/treinos em GameStats+totals+balance — medição,
  sem thresholds).
- `src/engine/military.test.ts` novo (precedente phase1-gate/
  harness): journey train→attack×3→slain→train (nextId nunca
  recicla; funds exactos; cadeia de 7 factos) + determinismo
  (twin Matches: snapshot+eventos deep-iguais).
- Sem mudanças em produção (nenhuma mecânica ancorada
  pendente — pós-regra militar seria invenção); bundle
  byte-idêntico (44077 chars); cenário intocado.
- Cortes (D-019): thresholds de balance (sem âncora);
  colunas militares no CSV (report chega); 3.º teste
  (redundante c/ M025 unfunded — cortado).

## 2. Grounding → decisão

- Lacunas OBSERVÁVEIS: sim nunca despachava attack/train
  (grep); nenhuma journey compunha 3+ transições unit.*
  (máx = 2, montanha M024); config neutra (dmg 0) impedia
  kills no sim.
- M020 (molde de fecho de fase) + TESTING.md §1B
  (integration transversal) + comentário DEFAULT ("tuning
  belongs to #92/playtesting") + roster Tiled (maxHp 5/12/8).

## 3. Churn (declarado)

- `sim/*` (playtest + report.json + balance.csv regenerados
  como prova); `military.test.ts` novo; D-019.
- balance.csv determinístico (md5 estável entre corridas);
  report.json muda só em `ms` (honesto).

## 4. Testes (2 novos → 1125/1125 + sim)

- Suite: journey do arco + determinismo de batalha (2/2).
- Sim 20×60: 1200 applied (attack 161, train 141, move 381,
  build 386, gather 21, upgrade 49, advance 61), 500
  rejected, **0 violations**; 27 kills, 414 dano, 141
  treinos (avg 1.35/20.7/7.05 por jogo).
- Cobertura 100×4 intacta (zero prod); selo intacto.

## 5. Gate e fecho

- Bateria 1125/1125 (1123 intactos + 2); statics verdes
  (tsc 0, eslint 0 incl. sim); sim exit 0; verify-bundle +
  smoke + smoke:attack verdes sem tocar.
- RISKS: baixo — só testes; residual: bots maus estrategas
  (irrelevante — invariantes é que contam); DRILL_UNITS≠#92.
- Commit: M026 Military Tests VERIFIED (1125/1125, 100×4). STOP.
