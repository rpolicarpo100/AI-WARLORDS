# M054 — Recall-Stance — VERIFIED

Vote: `recall-stance` (2026-09-12). Decision: D-048.
Last of the Memory block (M051–M054): the
first recall consumer — validated experience
steers commander posture (closes the loop).

## What it ships

- `stance.ts` (same file, no new pins —
  M050 precedent): `stanceWithRecall(record,
recollection)` — fresh attacked/slain count
  battles (ours by sides-caller → momentum:
  aggression +5 each), fresh
  overridden/canceled count failures
  (self-correction → humility: defense +5
  each); both capped at two memories
  (feedback-loop bound); neutral kinds
  (executed/spotted) and stale memories never
  move posture; memoryless records read
  stanceOf exactly (margin logic extracted to
  stanceFromTraits; stanceOf delegates —
  M037 intact). Recollection is a SHAPE MIRROR
  (memory-recall.ts canonical, L2↛L2).
- Match: `stancesOf` via recall +
  stanceWithRecall (private streamLookup
  shared with recall; defined-record cast).
  DIVERGENCE documented: ai-events producer
  keeps DNA stance (ProducerInput has no
  stream); postureOf aggregation keeps DNA.
- Pins: NONE new (by design).

## Result (frozen)

- Gate: 1730/1730 (1722 intact + 8: 6
  stance + 2 live), 100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS,
  `smoke` PASS, 4 sim goldens byte-identical
  (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2
  `116cccbc…`): 75741 chars (+419 —
  stanceWithRecall + wiring); `stancesOf` ×1
  in the bundle; `state.json` unchanged,
  `match.html` re-embedded.

## Files

- EDIT: `src/engine/stance.ts`,
  `src/engine/stance.test.ts`,
  `src/engine/match.ts`,
  `src/engine/match-memory-record.test.ts`,
  `tools/engine.bundle.js`, `mockups/match.html`,
  `docs/DECISIONS.md` (D-048),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
- No NEW files (by the voted design).
