# M010 — Map System (VERIFIED)

> Checkpoint M010 @ 2026-09-11 · 132 novos testes (459/459) · 100×4 ·
> primeiro domínio do jogo · gate TOOLS cumprido (Tiled autoritativo)

## 1. Contrato

- Modelo canónico `MapData` (versionado, validado): grid hex, 9 terrenos #13,
  spawns; topologia axial pura (vizinhos, distância, lookups O(1)).
- Loader `loadMapData`: Tiled hexagonal JSON strict-subset → `MapData`;
  bytes de ficheiro = trust boundary (tudo validado, fail loud).
- Integração WorldState v1 COMPATÍVEL (`map?`, sem bump, sem churn).
- Integridade: `map-preserved` fail-closed (O(1)); `match.started` com sumário.
- AI/CLIENT map-blind por construção; WORLD carrega.

## 2. Pré-análise (decisões)

- HEX axial: 6-adjacência uniforme (movimento/range/AI sem ambiguidade
  diagonal); master omisso ⇒ decisão de engenharia documentada; Tiled único
  shortlist com hex ⇒ confirma-se como autoritativo.
- Storage offset denso + stagger (coerência verificável, O(1)); axial derivada.
- Stagger y/odd|even = odd-r/even-r VERIFICADO nas docs Tiled 1.12.2
  (staggeraxis/index, tilesets, GIDs, flip flags — tudo ancorado).
- `TerrainId` fechado (M010 = "o que contém"); VALORES = M011.
- Spawns via layer `spawns` + props (sem pixel-math frágil).
- Sem bump: `map?` opcional ⇒ M005/M009 goldens intactos, zero churn.
- `redactFor` explícito ⇒ blindness automática, `views.ts` intocado.
- Sem ficheiros de mapa (conteúdo = M011+, com semântica); engine sem `fs`
  (loader recebe JSON já parseado).
- Flip-bits GID mascarados (presentacionais, documentado); base64/chunks/
  offsets/tilesets-externos/layers-extra rejeitados loud.

## 3. Implementação

- `src/engine/map.ts` (novo, LEAF L0): topologia (L78–104), `isMapData`
  (L136), lookups (L214–246), `loadMapData` (L306).
- Emendas declaradas: `world-state.ts` (`map?` + guard, antecipado p/ M004),
  `validation.ts` (`mapRule` O(1) por referência), `events.ts` (param opcional),
  `match.ts` (sumário no `started`).
- Ficheiros de teste alheios: `phase1-gate` (census 11 + `map: 0` — função
  pretendida do tripwire), `validation.test.ts` (3→4 invariantes — mudança
  de composição declarada). Mais nada tocado.

## 4. Testes (6 categorias)

| Categoria    | Onde                           | O quê                                           |
| ------------ | ------------------------------ | ----------------------------------------------- |
| Unit         | `map.test.ts:32,45,72,185,293` | ids, axial, guard, lookups, vizinhos            |
| Integração   | `map.test.ts:342,589,643`      | loader exact-load, WorldState, `started`        |
| Falha        | `map.test.ts:185,342`          | ~70 rejeições (guard + loader batteries)        |
| Segurança    | `map.test.ts:611,674`          | mutação ×3, blindness ×3                        |
| Determinismo | `map.test.ts:342`              | exact-load 2×2 (golden estrutural)              |
| Regressão    | suite 459/459                  | M002–M009 intactos (2 edits justificados acima) |

## 5. Third-Eye (ataques)

- Docs hostis: gid float/negativo/overflow, abaixo-firstgid, fora-tileset,
  buracos no terreno, layers/células/tiles duplicados, props sem nome/duplas.
- Formatos rejeitados: ortogonal, stagger-x, infinito, chunked, base64,
  offsets, dim-drift, tileset externo, segundo tileset, 17 spawns.
- Mutação do mapa via handlers (swap terreno/spawn/id) → `map-preserved`.
- Congelamento de vistas: WORLD=`toBe` com mapa; AI/CLIENT sem chave `map`.
- Selo/suites anteriores verdes sem edits em testes alheios além dos 2 de §3 (bytes mapless inalterados).

## 6. Gate

- format/lint/typecheck/test/build PASS; 459/459; 100/100/100/100
  (1 self-check defensivo inalcançável com `v8 ignore` + motivo, §3 TESTING).
- `dist/` com 44 ficheiros engine, 0 testes; fence: authority/hash/rng/
  harness/views/victory + packages intocados.

## 7. Limitações declaradas

- L-20: mapeamento stagger↔visual Tiled verificado nas docs, mas sem ficheiro
  real no repo — validação visual com o 1º conteúdo (M011+).
- L-21: zero ficheiros de mapa; geometria pixel ignorada (renderer futuro).
- N/A (§2 TESTING.md): SIMULTÂNEO/RACE (serial, M086), rede/persistência
  (M062/M069+), verificação manual (automatizado).
- L-09/L-12/L-13/L-15/L-16/L-17/L-18/L-19 mantêm-se.

## 8. Próximo

M011 Terrain: semântica dos 9 `TerrainId` (modificadores configuráveis #17);
depois M012 (recursos). Conteúdo do 1º mapa com semântica.
