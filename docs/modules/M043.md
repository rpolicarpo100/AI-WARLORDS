# M043 — Commander Orders Data — VERIFIED

Votes: `orders-data` + `engine-verbs` (2026-09-11). Decision: D-037.
Opens the Player → AI Command block (M043–M045). Pure data-first:
model + validation + storage, zero transitions/events/readers.

## What it ships

- NEW `src/engine/orders.ts` (LEAF L0, zero imports): `ORDER_IDS`
  (5 orderables — city.build, economy.gather, unit.attack,
  unit.move, unit.train — existing transition names,
  alphabetical, locked by test); `OrderKind` union;
  `CommanderOrder` (`{kind, params?}` — params a flat
  kind-agnostic record: strings ≤64 chars, finite numbers,
  booleans; ≤8 keys, keys 1–32 chars); total guards
  `isOrderKind` / `isOrderParams` / `isCommanderOrder`
  (extras ignored, M015).
- `commanders.ts` mirrors (M031–M033 mold, L0↛L0 tripwire):
  `CommanderOrderKind` / `CommanderOrderParams` /
  `CommanderOrder` mirror types, `MIRRORED_ORDER_IDS` +
  mirrored bounds, `order?` field on `CommanderRecord`,
  guard clause, `copyRecord` deep-copies order params
  (incl. the dna+order combined branch). Schema stays 1
  (optional-additive precedent).
- Pins: census `orders.ts` + LAYERS `orders: 0`.

## Grounding notes

- Flat params cover all five orderable wire-shapes (move,
  attack, train, build, gather — read, not assumed).
- No order vocabulary existed anywhere in the repo (grep
  zero; master prompt unavailable) — the `engine-verbs`
  vote grounds kinds in dispatched transition constants,
  pinned by test (`ORDER_IDS` ≡ the five constants).
- CUT (D-037): commander.* (lifecycle meta), city.upgrade
  (player progression), world.noop (= absence of order).

## Result (frozen)

- Gate: 1451/1451 (1417 intact + 34: 21 orders + 13
  commanders incl. mirror cross-check), 100×4 global.
- tsc 0, eslint 0, `verify:bundle` PASS, `smoke` PASS,
  3 sim goldens byte-identical (ms-only drift restored).
- Bundle rebuilt deterministically (md5 ×2): 57438 chars
  (+775, mirror code only — `orders.ts` correctly
  tree-shaken out: zero importers); `state.json` unchanged,
  `match.html` re-embedded.

## Files

- NEW: `src/engine/orders.ts`, `src/engine/orders.test.ts`
- EDIT: `src/engine/commanders.ts`,
  `src/engine/commanders.test.ts`,
  `src/engine/phase1-gate.test.ts`, `tools/engine.bundle.js`,
  `mockups/match.html`, `docs/DECISIONS.md` (D-037),
  `docs/MODULE_STATUS.md`, `docs/RISK_REGISTER.md`
