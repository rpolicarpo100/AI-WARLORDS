# M023 — Combat (VERIFIED @ 2026-09-11)

> `unit.attack` adjacente com dano #83 e chão 0; facto `unit.attacked`
> NORMAL com dano genuíno (diff hp). Morte/remoção ficam em M024 (D-014).

## 1. Âmbito (contrato)

- `ATTACK_TRANSITION` (`unit.attack`) em `warfare.ts` L2:
  `createAttackHandler(unitsConfig)` + `attackParamsRule` +
  `attackProducer` (`unit.attacked`, NORMAL, `{player, unit, target,
damage}`) + `warfareHandlers(passable, unitsConfig)` (assinatura
  estendida — emenda declarada).
- Regras: `no units.` / `unknown unit.` / `not your unit.` /
  `unit down.` / `unknown target.` / `not an enemy.` (self incluído) /
  `target down.` / `no map.` / `out of range.` (neighborsOf;
  same-cell excluído, precedente M022).
- Dano subtraído com chão 0 (isWord força; 0hp persiste; M024 remove);
  dano 0 aplica (default neutro M021 = harmless).
- Match: seam `unitsConfig?` (default neutro M021) + merge + paramRule
  - producer; predicado move intacto.
- Sem regra nova (composição 5+1); sem ficheiro novo; sem bump
  ruleset (precedente M022); cenário intocado (state.json idêntico).
- Cortes (D-016): morte/remoção (M024), alcance >1 (sem âncora;
  arqueiro por diferenciar), retaliação/armadura (M024+), cura.

## 2. Grounding → decisão

- #83 (damage sem consumidor) + #5 (HP engine-owned) + D-014
  (dano/remoção→M024) + M022 (molde + neighborsOf + same-cell) +
  aprovação explícita do utilizador 2026-09-11 (chão 0, só-adjacente,
  facto NORMAL — cunhagem `unit.attack/attacked` por analogia, sem
  vocabulário de combate no repo).
- Dano do produtor = diff hp before/after (genuíno, não config).

## 3. Churn (declarado)

- `warfareHandlers(passable)` → `(passable, unitsConfig)` (match.ts +
  1 teste `warfareHandlers` reescrito); 0 fixtures (sem dispatch novo
  no cenário); bundle+match.html rebuildados (código novo, outputs
  idênticos — verify-bundle verde).
- Bug de teste apanhado pelo próprio gate: same-cell ≠ in-range
  (precedente move); teste corrigido, implementação intacta.

## 4. Testes (31 novos → 1096/1096)

- `warfare.test.ts` +27: params (4), handler directo (14: config +
  9 rejects + 4 goldens), producer (4), E2E (5: golden+facto,
  harmless, range, pré-regra, barrage 0hp).
- `match.test.ts` +4: `unitsConfig` inválido rejeita.
- Cobertura 100×4 (warfare.ts + match.ts 100); selo intacto.

## 5. Gate e fecho

- Bateria 1096/1096 (1065 intactos + 31); statics verdes.
- 0hp persiste mas não age (`unit/target down.`); remoção→M024 mantém.
- RISKS: baixo — combate puro + gate estrito; residual: produtor
  assume alvo presente no after (M024 emenda se remover).
- Commit: M023 Combat VERIFIED (1096/1096, 100×4). STOP.
