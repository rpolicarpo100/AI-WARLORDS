# M018 — Buildings (VERIFIED)

> Checkpoint M018 @ 2026-09-11 · 80 novos testes (845/845) · 100×4 ·
> dados+config+mecânica de edifícios · D-011 escrita antes do código

## 1. Contrato

- 6 ids fechados (#16: town-center, house, storage, barracks, wall,
  tower); counts por holder (`BuildingsData`, guard L0, `countsOf`
  fail-soft zeros, cópia fresca).
- `BuildingsConfig`: por-tipo `{cost, buildTime}` + `caps {base,
perStorage}`; default neutro (custos {}, time 0, uncap); frozen.
- Mecânica: `costOf` (completa parciais, cópia fresca), `buildTimeOf`,
  `payCost` (débito multi-recurso atómico, first-short determinístico),
  `addBuilding` (count+1, overflow loud), `capOf` (derivado).
- `WorldState.buildings?` (opcional, map-independent) + percepção
  own-only (zeros default; outros holders fail-closed).
- L-31 DECIDIDA: caps derivados (base + storages×per), default uncap
  (base=MAX/per=0, status quo preservado), enforcement → M020.
- SEM transições (BUILD_STARTED/COMPLETED → M019); SEM regra nova
  (M020 owns — composição presa em 5); SEM cidade/níveis (M019);
  SEM pop-cap (população é HUD, ungrounded).

## 2. Pré-análise (decisões)

- D-011 precede a implementação (análise-antes-código, em `DECISIONS.md`).
- Counts, não instâncias: id/posição/hp ungrounded — counts chegam
  para caps+custos; M019 estende (construção/cidade).
- Caps DERIVADOS, não guardados: single-source (sem sync-bugs);
  L-31 "storehouses" = fonte da derivação.
- Defaults NEUTROS (doutrina M011/M016): mecânica existe, valores
  por afinar (tuning → playtesting); uncap-by-default nunca quebra
  gather (mesmo após M020 com config default).
- M009 força split L0 + dup holder-mirror (L0↛L0); data-lenient /
  config-strict (padrão M016: extras em dados passam, em config
  chumbam — typos de custo seriam recursos grátis).
- `payCost` atómico (check-all antes de construir; sem estados
  parciais) + first-short em ordem food→gold (determinístico).

## 3. Implementação

- `buildings.ts` (novo, L0, zero imports): ids + `BUILDING_IDS` +
  dados + guards + `countsOf` (espelho exacto `stockpiles.ts`).
- `economy.ts` (L2, extensão M018): config + default + `isCost`/
  guards + `costOf`/`buildTimeOf`/`payCost`/`addBuilding`/`capOf`;
  import buildings (DAG intacto, LAYERS +`buildings: 0`).
- `world-state`: import+campo+init+guard+factory (5 spots, padrão
  stockpiles); `views`: import+campo+embed (3 spots, L2→L0).
- Gate census 17→18; F-09 7/6→8/7; 2 goldens perceive +`buildings`.

## 4. Testes (9 categorias, +80)

- `buildings.test.ts` (41, novo): ids 7 + vocabulário 1 (lock #16
  em ordem) + counts 11 (6 posições pinned) + data 17 + countsOf
  3 + espelhos 2 (holder-cap vs stockpiles; chaves vs ids).
- `isBuildingsConfig` (13): custom 7/7 + default; não-objecto 3;
  parcial/oversized; chaves unknown (length + key-check); custos
  maus (key/valores/null); buildTime 4; caps 3.
- Default (2): golden neutro exacto + deep-frozen (3 níveis).
- `costOf`/`buildTimeOf` (5): goldens custom+default; inválidos
  loud; cópia fresca (mutação não aliasa config).
- `payCost` (5): golden multi + frozen + input intacto; parcial/
  vazio; drain exacto a zero; first-short ×4 ordem; inválidos loud.
- `addBuilding` (3): golden frozen; acumulação; inválidos+overflow.
- `capOf` (5): base-only; bónus; default uncap; absent-soft vs
  garbage-loud; config inválida + overflow.
- Fluxo custos (1): afford→pay→add compõe golden (contrato M018).
- Integração (5): WorldState extension 3 + perception carry 2.

## 5. Third-Eye (ataques)

- Config forjada (6/7 ou 8/7 chaves; key trocada c/ length
  preservado; custo c/ key/valores maus; time/caps maus) → false.
- Custo parcial malformado no pay (`{stone: -1}`, `'x'`) → throw.
- Overdraft: qualquer short → `insufficient <first>` (ordem presa).
- Overflow: count MAX+1 e cap base-MAX+1 → throw (nunca satura).
- Holder malformado (''/65ch) → guard false; unknown → zeros.
- Aliasing custo→config: mutar retorno não toca config (fresh).
- Mutar default frozen → TypeError (3 níveis presos).

## 6. Gate

- 845/845 (765 + 80), 23 ficheiros, 100/100/100/100 global E
  por-ficheiro (incl. `buildings` novo + `economy` estendido).
- `dist/engine` 76 ficheiros / 0 testes (19 módulos × 4).
- Selo intacto (campo opcional; cenário advance-only intocado).
- Composição presa em 5; gate census 18; engine 817 testes.

## 7. Limitações declaradas

- L-31 CLOSED (caps derivados; enforcement → M020; gather ignora
  caps até lá — gap declarado).
- BUILD_STARTED/COMPLETED + transições build + queue → M019.
- L-30 mantém-se (grants → M019); cidade/níveis → M019.
- Storage default bonifica 0 (mecânica provada via customs).
- Sem pop-cap (ungrounded); sem caps por-recurso (flat chega).
- L-09/L-12/L-13/L-15–L-22/L-24–L-26/L-28/L-29/L-32 mantêm-se.

## 8. Próximo

- Próximo permitido: **M019 — City System**, apenas após autorização
  explícita. STOP (sem auto-avanço).
