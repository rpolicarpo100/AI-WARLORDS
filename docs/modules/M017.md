# M017 — Gathering (VERIFIED)

> Checkpoint M017 @ 2026-09-11 · 48 novos testes (765/765) · 100×4 ·
> primeiro produtor (nó→stockpile) · D-010 escrita antes do código

## 1. Contrato

- `economy.gather` (params `{col,row}`): move `min(gatherYield,
node.amount)` do nó para o stockpile do caller; depleção in place
  (detail com amount 0 mantém-se — presença acoplada ao terreno).
- Facto `resource.gathered` (prioridade normal) via produtor; payload
  `{player, col, row, resource, amount}` calculado por diff via params.
- Match fecha sobre `economyConfig?` validado (seam M011; default
  neutro; ruleset-versioning diferido à 1.ª variante).
- `map-preserved` cede SÓ depleção (header igual + pareamento por
  posição + terrain igual + type igual + amount não-crescente).
- SEM worker/reach/ownership (L-32 → M021/M022); SEM fog-gating; SEM
  novo pós-invariante (M020 owns — composição presa em 5 por teste).

## 2. Pré-análise (decisões)

- D-010 precede a implementação (análise-antes-código, em `DECISIONS.md`).
- Guard exige length exacto + terrain⟺detail ⇒ parear por índice seria
  unprovable p/ TS; `Map.get` + checks explícitos cobre tudo sem `!`.
- Mutantes inválidos (gate-unreachable) testados por chamada DIRECTA
  da regra exportada (execução real, §3 TESTING.md — não mocks).
- Pre=wire-shape / handler=regras de jogo; cast documenta o seam
  (precedente `validated` em match.ts); tipos ESTRUTURAIS (L2→L2
  proibido até p/ types; atribuição provada na costura match.ts).
- Overflow→FAULT (tecto uint32 é escala-de-bug, L-31); taken-0→
  applied:false (yield-0 nunca no-op applied); tick intacto.

## 3. Implementação

- `economy.ts` (+6 exports): `GATHER_TRANSITION`, `GatherParams`,
  `gatherParamsRule`, `createGatherHandler`, `economyHandlers`,
  `gatherProducer`; imports +cellAt/+TransitionHandler/+WorldState.
- `match.ts` (seam): campo `economyConfig?`, validação loud, merge
  economy + size-check, `builtins`→`noParamHandlers`, mapa paramRules,
  `producers.set(GATHER_TRANSITION, [gatherProducer])`.
- `validation.ts`: `mapsDepletionOnly` exportado (puro, testável) +
  mapRule rewrite (ref O(1) + relaxação); detalhe novo.
- `resources.test.ts`: REWRITE previsto pelo M016 (mutante 500→1 era
  depleção legítima; agora aumento 501 falha + 499 passa).

## 4. Testes (7 categorias, +48)

- Pré-regra unit (2): não-objecto ×3 expects; coords ×4 expects.
- Handler directo (9): config inválida; sem-mapa; OOB ×2 shapes;
  sem-nó; esgotado; yield-0; golden; acumulação; overflow loud.
- E2E gather (5): applied+depleção+crédito+facto golden; resto +
  3.º rejeitado-registado; params malformados; default yield-1;
  perceive pós-gather own-only.
- Mutantes forja E2E (9): 7 `it.each` (terreno/type/aumento/id/
  stagger/spawns/dims, todos gate-passing provado) + arrival +
  removal → HANDLER_FAULT, intocado, revisão 0.
- Producer directo (3): params inválidos; mapa ausente; nó ausente.
- `mapsDepletionOnly` directo (15): depleção/igual; ausências;
  5 header; spawns; 6 células; posições duplicadas.
- Ctor + rewrite (5): 4 `economyConfig` inválida; resources +1.

## 5. Third-Eye (ataques)

- Forja terreno/type/aumento/id/stagger/spawns/dims/arrival/removal
  (todos gate-passing) → HANDLER_FAULT, estado intocado (E2E).
- Teleport same-type: pairing por posição fecha (totals sós não).
- Overflow stockpile: throw→fault (nunca saturação silenciosa).
- Yield-0: applied:false (nunca applied no-op); esgotado: idem.
- Params malformados: pré-regra rejeita, registado, intocado.
- Produtor incoerente: throw→`system.event-fault` (contenção M007).
- Posições duplicadas: colapso→posição nova→false (direct).

## 6. Gate

- 765/765 (717 + 48), 22 ficheiros, 100/100/100/100 global E
  por-ficheiro (incl. `economy`/`match`/`validation` estendidos).
- `dist/engine` 72 ficheiros / 0 testes (sem ficheiros novos).
- Selo intacto (cenário advance-only; gather não toca tick/seal).
- Composição presa em 5; gate census 17; engine 737 testes.

## 7. Limitações declaradas

- L-23 CLOSED (amounts por tile reais via depleção; M012→M017).
- L-32 CRIADO: gather sem worker — qualquer jogador colhe qualquer
  célula (worker+alcance+posse → M021/M022).
- Sem fog-gating (sem grounding no mestre; sem L-number).
- Ruleset-versioning da config diferido (declarado em D-010).
- Sem validação económica (M020 owns); overflow = FAULT (L-31).

## 8. Próximo

- Próximo permitido: **M018 — Buildings**, apenas após autorização
  explícita. STOP (sem auto-avanço).
