# M013 — Fog of War (VERIFIED)

> Checkpoint M013 @ 2026-09-11 · 46 novos testes (586/586) · 100×4 ·
> fog = computação pura · D-006 escrita antes do código

## 1. Contrato

- Visibilidade computada: `(mapa + fontes + config) → conjuntos por viewer`.
- Fontes guardadas: `viewer` PlayerId + `col`/`row` + `range` uint32.
- Config 9/9 validada + default: forest/mountain bloqueiam (D-006).
- Flood BFS por-range; bloqueadas visíveis sem expandir; origem expande
  sempre; fontes OOB saltadas soft; inputs malformados loud.
- Saída frozen determinística (viewers ordenados, índices ascendentes);
  queries fail-soft (`visibleCells`, `isVisibleCell`).

## 2. Pré-análise (decisões)

- D-006 precede a implementação (análise-antes-código, em `DECISIONS.md`).
- Viewers = `PlayerId` reutilizado, sem membership-check (camada pura;
  enforcement no consumo, M015 — L-27).
- Dois níveis: estrutural loud (`isVisionSource`/config), contexto soft
  (OOB skipa — posições stale não crasham).
- Origem-expande: observador vê para fora de onde está; bloqueio oclui
  o que está ALÉM (refinamento de D-006, testado).
- Índices como convenção (`row * width + col`); sem estado (M014), sem
  vistas (M015), sem fontes-owned (M021).

## 3. Implementação

- `src/engine/fog.ts` (novo, L2): `isVisionSource`, `isFogConfig` +
  `DEFAULT_FOG_CONFIG`, `computeVisibility` + `floodFrom`, queries.
- Imports L2: `map` (topologia) + `authority` (freeze/PlayerId) + `rng`
  (MAX_UINT32) — todos descendentes, DAG intacto.
- Zero emendas de produção; `phase1-gate` (census 14 + `fog: 2` —
  função pretendida do tripwire). Mais nada tocado.

## 4. Testes (6 categorias)

| Categoria    | Onde                       | O quê                                     |
| ------------ | -------------------------- | ----------------------------------------- |
| Unit         | guards + default + queries | fontes (19 shapes), config (11), queries  |
| Integração   | compute goldens            | 7/19 hex, wall, all-pass, união, OOB      |
| Falha        | guards + compute loud      | viewers/range/config/fontes inválidos     |
| Segurança    | isolamento + frozen        | contaminação ×0, chaves ordenadas, freeze |
| Determinismo | run×2 + goldens            | saídas literais + teoria hex (1+6+12)     |
| Regressão    | suite 586/586              | M001–M012 intactos (540 testes, tripwire) |

## 5. Third-Eye (ataques)

- Viewer vazio/longo/lixo, range negativo/float/overflow, config 8/10/
  lava/non-boolean — tudo rejeitado ou loud.
- Wall golden: bloqueadas visíveis, zero além; all-pass prova que a
  config é efectiva (não hardcoded).
- P1+P2 juntos vs separados: conjuntos idênticos (isolamento).
- `NaN`/ciclos herdados do `freezeState` estrito (FIX-AUDIT).

## 6. Gate

- format/lint/typecheck/test/build PASS; 586/586; 100/100/100/100.
- `dist/` com 56 ficheiros engine, 0 testes (contado, não derivado);
  fence: zero ficheiros de produção tocados além dos 2 novos + tripwire.

## 7. Limitações declaradas

- L-26: computado on-demand, sem cache (medir em M088 se preciso).
- L-27: viewers sem membership-check (M015 enforce no consumo).
- Sem memória explored (M014); sem enforcement em vistas (M015);
  sem fontes-owned (M021); montanha-muro (D-006, rever com elevation).
- N/A (§2 TESTING.md): rede/persistência (M062/M069+), SIMULTÂNEO/RACE
  (serial, M086), verificação manual (automatizado).
- L-09/L-12/L-13/L-15/L-16/L-17/L-18/L-19/L-20/L-21/L-22/L-23/L-24/L-25
  mantêm-se.

## 8. Próximo

M014 Exploration: memória explored (visited-ever) sobre a visibilidade
M013; depois M015 (percepção/enforcement, morte do `secrets`).
