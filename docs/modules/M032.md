# M032 — AI Personalities (VERIFIED @ 2026-09-11)

> 2.º do bloco Personalidade. Vocabulário fixo-5 + presets
> DNA canónicos VOTADOS + rótulo opcional no record
> (âmbito VOTADO). Data-first, sem writer, sem behavior.

## 1. Âmbito (contrato)

- `personalities.ts` NOVO (LEAF L0, zero imports):
  PERSONALITY_IDS (5, ordem #23, lowercase) +
  PersonalityId + isPersonalityId + PersonalityDna
  (shape estrutural — mirror de DnaTraits, L0↛L0) +
  PERSONALITY_DNA_PRESETS (matriz 5×10 votada,
  frozen) + dnaPresetOf (cópia fresca;
  desconhecido→undefined fail-soft, commanderById).
- `CommanderRecord.personality?` (embed votado; union
  mirror + isMirroredPersonalityId; presets NÃO
  espelhados — lookup vive em personalities.ts).
- Precedência votada p/ futuros consumidores:
  record-DNA ganha ao preset (especificidade; #22).
- Sem slot novo; views/Match/validation zero churn
  (F-09 top-level); commission sem personality (M029).
- Selo phase1-gate: 2 pins (census + LAYERS
  `personalities: 0` — fail-loud, M015).
- Cortes (D-026, âmbito votado): writer (init-placed);
  behavior/influência (M033+/M035+); `name`, SKILL
  (ainda-cut, sem dono); 6.ª personalidade ("Inicialmente"
  = extensão futura c/ rationale).

## 2. Grounding → decisão

- 5 votos explícitos do utilizador 2026-09-11
  (data-first; presets; extend; matriz 5×10; record-wins)
  + #23 (5 nomes+epítetos) + #25 (≠SKILL) + #22
  (influencia≠determina) + M031 (molde data-first +
  mirror + forward-placed) + M016/M027 + constraints.
- Números NUNCA inventados: matriz proposta por
  convenção explícita (base 50, ±30–35, ×5) e APROVADA
  em voto dedicado antes do código.

## 3. Churn (declarado)

- `commanders.ts`: embed + union-mirror + guard
  (2.º mirror; single-line union per prettier);
  `commanders.test.ts`: battery + slot (hunk 108
  HEAD-dirty intacto); zero churn resto do motor.
- Bundle rebuildado (50896 chars, md5 idempotente ×2;
  só o mirror embarca — `manipulator` ×1 como prova).
- state.json/godot/sim/página intocados; sim idêntico
  (1200/500/0 avgLeft 340.0); verify-bundle SEM regen.
- Prettier: warns só em regiões HEAD-dirty (verificado
  por diff); D-026 reworded (leading-`+` vira vírgulas
  + typo Teste-video→pino corrigido).
- `sim/report.json` (ruído ms) revertido, fora do commit.

## 4. Testes (16 novos → 1346/1346)

- `personalities.test.ts` 7: vocabulário (2: lista-5,
  trait-id) + presets (3: golden matriz 5×10 exacta,
  conformance isDnaTraits ∀5, frozen) + lookup (2:
  fresh-copy, unknown→undefined).
- `commanders.test.ts` +9: embed (1+5+1+1: retro-compat,
  5 válidos, abuso fixo-5, slot round-trip + perceção)
  + cross-check mirror (1: five + battery + absent-ok).
- Bateria 1346/1346 (1330 intactos + 16, zero
  regressões); cobertura 100 global + personalities.
- 1 cast `as unknown as` em teste (molde M031).

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0); verify-bundle +
  smoke + smoke:attack verdes; sim regressão limpa.
- RISKS: baixo — dados puros; residual: tuning matriz
  (#92); extensão "Inicialmente" quebra goldens
  (declarado); folha sem importadores.
- Commit: M032 AI Personalities VERIFIED (1346/1346,
  100 global). STOP.
