# M009 — Core Engine Test Suite (VERIFIED)

> Checkpoint M009 @ 2026-09-11 · 14 novos testes (327/327) · 100×4 · zero código de produção · **Fase 1 completa**

## 1. Contrato

- Módulo só-de-testes: prova transversal de M003–M008, não mecanismos novos.
- Equação #80 lockada end-to-end: gémeos + runs repetidos + selo golden literal.
- Inválidos Tier-1 com zero-trace observável (estado, traces, `seen`, streams).
- Reviews estáticas executáveis: higiene de determinismo + DAG arquitectural.
- Escala executada sem thresholds inventados (medir→registar, #93).

## 2. Pré-análise (decisões)

- Tier-1 (envelope/auth/dedupe/registry) vs Tier-2 (rejeição validada): só o
  Tier-1 é zero-trace — Tier-2 deixa `applied:false` por desenho (M005/M006).
- Streams por `(seed, seq)` com seq por-Match: gémeos com igual histórico de
  wrapper partilham streams; ruído Tier-1 nunca consome ordinais.
- `randomUUID` em allowlist exacta (2 imports + 2 call-sites de identidade):
  ids nunca entram nos traces do jogo (verificado por desenho + gémeos).
- Tripwires deliberadas: selo, census de 10 ficheiros, mapa de camadas —
  quebrar loud é a função, não um acidente.
- TEST handler único (`test.rngdraw`) só como consumidor de streams; resto em
  defaults reais (`match.advance`).

## 3. Implementação

- `src/engine/phase1-gate.test.ts` (576 linhas) — 6 blocos: equação (L206),
  zero-trace (L261), fronteira (L356), higiene (L422), DAG (L479), escala (L552).
- Helpers: `SCRIPT` partilhado (L75), `fingerprint` 9-campos (L117), baterias
  de ruído A/B (L136/L175), `recordedV2` auto-calibrado (L166).

## 4. Testes (6 categorias)

| Categoria    | Onde                              | O quê                                                 |
| ------------ | --------------------------------- | ----------------------------------------------------- |
| Unit         | —                                 | N/A: sem código novo para unit-testar                 |
| Integração   | `phase1-gate.test.ts:206,356,552` | gémeos deep-equal 5×, fronteira M006×M007×M008, K=500 |
| Falha        | `phase1-gate.test.ts:261,356`     | ruído Tier-1 A/B, storm no tick final, pós-fim        |
| Segurança    | `phase1-gate.test.ts:261,422`     | zero-trace+seen, scan de primitivas, allowlist ids    |
| Determinismo | `phase1-gate.test.ts:206`         | selo golden (3 hashes + veredicto literais)           |
| Regressão    | suite 327/327                     | M002–M008 intactos (313 testes, 0 edits prod)         |

## 5. Third-Eye (ataques)

- Ruídos A/B diferentes em pontos diferentes → gémeos bit-identicos (5 obs.).
- `seen` provado igual via duplicate-replay; ids-fantasma nunca gravados.
- Streams rng sob ruído: timelines (detalhes com draws) idênticas.
- Storm inválida no tick final: `match.finished` exactamente-1; pós-fim
  bloqueia 4 shapes com traces congelados.
- Futuro hostil: novo primitivo não-determinístico, novo ficheiro sem camada,
  aresta ascendente ou ciclo — tudo falha loud aqui.

## 6. Gate

- format/lint/typecheck/test/build PASS; 327/327; 100/100/100/100.
- `dist/` com 40 ficheiros engine, 0 testes; fence total: nenhum ficheiro de
  produção tocado (`git diff` vazio fora de testes+docs).
- Escala medida (indicativo, sandbox, não-gate): ~75–130ms por 1000 dispatches
  com asserts (~0.1ms/dispatch).

## 7. Limitações declaradas

- L-19: scans estáticos não detectam ofuscação deliberada (`globalThis['Ma'+…]`);
  mitigação: review humana + trips de runtime fora de âmbito (M086).
- N/A declarados (§2 TESTING.md): SIMULTÂNEO/RACE (serial in-process, M086),
  TIMEOUT/RECONNECT/DISCONNECT/DB/API/AI/REDE (sem transporte, M069+),
  verificação manual (tudo automatizado), domínios #78 inexistentes (donos futuros).
- L-09/L-12/L-13/L-15/L-16/L-17/L-18 mantêm-se.

## 8. Próximo

M010 Map System: primeiro domínio do jogo (Fase 2); gate de formato (Tiled/LDtk
em TOOLS.md). Gate #105 engine-level: cumprido — Fase 1 FECHADA.
