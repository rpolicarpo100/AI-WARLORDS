# M090 — Access log (D-085)

- Status: VERIFIED+LIVE (2026-09-12)
- Server: linha JSON {t,
  method, path, code, ms} no
  finish (ms via now());
  opt-in AW_LOG=1 (1 branch);
  serve.ts liga (Render lê
  stdout); sem IP; onRequest
  try/catch total (contrato
  never-throws + mock prova).
- Tests: 1 spy exacto
  (t/method/path/code/ms);
  suite 1939/1939 100×4.
- Bundle SAME md5; API redeploy.
