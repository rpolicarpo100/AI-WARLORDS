# M025 — Army Management (VERIFIED @ 2026-09-11)

> Treino atómico paga+gera (`unit.train`); custo #83 consumido;
> facto `unit.trained` NORMAL; treasury injectada pelo Match (D-018).

## 1. Âmbito (contrato)

- `UnitTreasury = {canAfford, pay}` em `warfare.ts` L2 (interface
  injectada pelo Match com closures economy — precedente M022;
  L2↛L2 impede warfare importar payCost; `UnitCost`≡`Cost`
  estrutural, assignable).
- `createTrainHandler(unitsConfig, treasury)`: tipo→mapa→bounds→
  `canAfford`→`pay`→spawnUnit full-hp (`u${nextId}`); rejects
  `unknown unit type.` / `no map.` / `out of bounds.` (precedente
  M022) / `cannot afford.` (precedente build); summary `trained T
  ID at C,R`. `funds ?? {v1,{}}` (precedente build — zero-custo
  sem stockpiles aplica).
- `TRAIN_TRANSITION` (`unit.train`, cunhagem aprovada) +
  `trainParamsRule` ({type string, col/row uint32}) +
  `trainProducer` (`unit.trained` NORMAL `{player, unit, type,
  col, row}` — id/unidade lidos do after, `u${before.nextId??0}`).
  NORMAL = precedente build.started (produção).
- `warfareHandlers(passable, unitsConfig, defenseOf, treasury)`
  (append, precedente M023/M024); Match injecta treasury +
  paramRule + produtor (zero branches novos no match).
- Sem regra nova (composição 5+1); sem ficheiro novo; sem bump
  ruleset (precedente M022); cenário intocado (sem treinos —
  outputs idênticos, verify-bundle verde).
- Cortes (D-018): edifício/passabilidade/adjacência (sem âncora);
  stacking-gate (M022 CUT precedente); cap/upkeep (sem âncora —
  200 da página fica CONCEPT); cura/regem (fora de âmbito).

## 2. Grounding → decisão

- D-014 (`criação`→M025; `UNIT_CREATED` presumível REAVALIADO
  para dotted-minus) + #5 (criação engine-owned) + #83 (custo
  sem consumidor) + M021 (spawnUnit puro) + M016/M017
  (payCost/canAfford; padrão createBuildHandler) + M020
  (conservação: débitos passam) + aprovação explícita do
  utilizador 2026-09-11 (verbo `train`).
- Recrutas em rio/montanha permitidos (sem âncora p/ gate —
  residual declarado, não bug).

## 3. Churn (declarado)

- Assinatura warfareHandlers 3.ª vez (match.ts + 1 teste);
  treasury stub RICH/BROKE nos testes directos (isolamento L2
  honesto — débito real provado no E2E via Match).
- verify-bundle + smoke/attack verdes sem tocar (u4hp=8 dmg=4
  idênticos); bundle+match.html rebuildados (idempotente md5;
  FASE B intacta).
- Prettier: 18 ficheiros dirty (pré-existente no HEAD — não é
  gate; eslint 0 é o gate e passa).

## 4. Testes (21 novos → 1123/1123)

- `warfare.test.ts` +21: params (it.each×3 + forma), handler
  directo ×9 (throws, 4 rejects, golden com spy de pay
  `{food:10}`, nextId, u0 fresco, zero-custo sem stockpiles),
  produtor ×5 (params, 2 missing, golden, u0 fresco), E2E ×3
  (golden food 100→90/u2/facto NORMAL, unfunded rejeita,
  pré-regra rejeita).
- Bateria 1123/1123 (1102 intactos + 21); cobertura 100×4
  (All-files + warfare.ts + match.ts 100); selo intacto.

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0); N4 cenário idêntico.
- RISKS: baixo — criação pura + gate estrito; residual: colisão
  de id (HANDLER_FAULT fail-stop, precedente spawnUnit); sem
  botão na página (visual futuro, fora do módulo).
- Commit: M025 Army Management VERIFIED (1123/1123, 100×4). STOP.
