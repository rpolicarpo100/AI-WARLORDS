# M002 — Development Environment

> STATUS: `VERIFIED` (2026-09-10)
> Checkpoint: M002 @ 2026-09-10 — INSTALL/BUILD/TEST/LINT/TYPECHECK = PASS

---

## 1. Contract (§6, final)

| Campo               | Valor                                                                                                                                                                  |
| ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MODULE_ID           | M002                                                                                                                                                                   |
| MODULE_NAME         | Development Environment                                                                                                                                                |
| PURPOSE             | Provar que o ambiente instala, compila, testa, lint e typecheck de forma reproduzível; fixar os rails de qualidade para os restantes módulos                           |
| DEPENDENCIES        | M001 = VERIFIED (satisfeita). npm registry: VERIFICADO (161 pacotes instalados)                                                                                        |
| INPUTS              | Decisão de stack delegada ao agente (necessidades + custo≈0 + fiabilidade)                                                                                             |
| OUTPUTS             | Repo git (2 commits); package.json + package-lock.json; configs tsc/vitest/eslint/prettier; scaffold dev-server + /health (SEM contrato); 28 testes; docs actualizados |
| FILES_AFFECTED      | 14 criados (código+config) + 8 docs (1 novo registo, 1 nova política, 6 actualizados)                                                                                  |
| DATABASE_CHANGES    | none                                                                                                                                                                   |
| API_CHANGES         | none — `/health` do scaffold NÃO é a API do jogo; substituível em M003/M069+                                                                                           |
| SECURITY_IMPACT     | baixo: dev-only, bind 127.0.0.1, zero prod deps, bodies de erro estáticos                                                                                              |
| PERFORMANCE_IMPACT  | negligenciável; sem optimização, sem afirmações                                                                                                                        |
| TEST_REQUIREMENTS   | cumpridos: 6 categorias + coverage 100×4 (1 exclusão documentada + controlo compensatório)                                                                             |
| ACCEPTANCE_CRITERIA | todos PASS (ver §5 Gate)                                                                                                                                               |
| KNOWN_RISKS         | R-50 MITIGATED (residual: determinismo por provar em M005/M009); R-51 ACTIVE sem impacto; R-52 ACTIVE (TS6 novo, pinned)                                               |
| ROLLBACK_STRATEGY   | `git revert` do commit M002 (ou reset ao commit M001); sem dados/migrações                                                                                             |
| STATUS              | VERIFIED                                                                                                                                                               |

## 2. Module analysis (§7, escrita ANTES de qualquer instalação)

Resumida no registo de execução; versão integral foi produzida antes do
`npm install` (contrato + análise precederam a implementação, conforme §7).
Pontos-chave: dependência única nova = npm registry (falha → BLOCKED,
não contornado); impacto zero sobre o existente (GREENFIELD); dados nenhuns;
segurança dev-only; testes por categoria com matriz unit→adversarial.

## 3. Stack decision (delegada; critérios: necessidades · custo≈0 · fiabilidade)

| Decisão                                               | Fundamento                                                                                                              |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| TypeScript 6.0.3 + Node 20 LTS, strict                | typecheck nativo (gate); determinismo viável (M005); partilha client/server futura (M069+); SDKs Solana/LLM first-class |
| Vitest 4.1.11 + coverage-v8                           | runner rápido, coverage real do motor v8                                                                                |
| ESLint 10 + typescript-eslint 8 (zero warnings) + tsc | dupla camada: estilo/bugs + tipos                                                                                       |
| Prettier 3                                            | formato canónico para 165 módulos sem discussão                                                                         |
| tsx 4                                                 | dev loop TS no Node 20 (sem TS nativo)                                                                                  |
| @types/node 22                                        | tipos `node:` sob strict                                                                                                |
| Zero production dependencies                          | custo €0, superfície mínima, sem supply-chain em runtime                                                                |
| Single package (YAGNI)                                | monorepo só quando justificado (§19)                                                                                    |
| Rejeitado: Python                                     | typecheck via 3ª parte, sem partilha client/server                                                                      |
| Rejeitado: `dotenv`, hooks, CI já                     | cada um só quando um módulo o exigir                                                                                    |

Versões exactas instaladas em `docs/DEPENDENCIES.md` (observadas, não assumidas).

## 4. Implementation (apenas M002 — sem código de jogo)

```text
package.json  scripts: build/start/dev/test(+coverage)/lint/format/typecheck/clean
tsconfig.json        typecheck strict (incl. testes)
tsconfig.build.json  emit dist/ sem testes, com declarations+sourcemaps
vitest.config.ts     thresholds 100/100/100/100 + 1 exclusão documentada
eslint.config.js     flat, recommended, max-warnings=0
.prettierrc.json / .prettierignore / .editorconfig / .nvmrc / .gitignore
.env.example         (sem auto-load por design — zero-dep; documentado)
src/health.ts        getHealth() puro, versão lida do package.json real
src/dev-server.ts    parse/format/router/runDev — tudo testável e testado
src/dev.ts           bootstrap (excluído da métrica; validado por execução real)
src/*.test.ts        28 testes: unit/integration/failure/security/adversarial/contract
```

## 5. Testing (§9–§10) + Gate M002

| Gate         | Comando                                                                        | Resultado            |
| ------------ | ------------------------------------------------------------------------------ | -------------------- |
| INSTALL      | `npm install` (161 pkgs) + `rm -rf node_modules && npm ci`                     | PASS (exit 0)        |
| BUILD        | `npm run build` → 12 artefactos dist/, 0 testes emitidos                       | PASS (exit 0)        |
| TEST         | `npm test` → 28/28, coverage 100/100/100/100 (thresholds enforced)             | PASS (exit 0)        |
| LINT         | `npm run lint` (`--max-warnings=0`)                                            | PASS (0 err, 0 warn) |
| TYPECHECK    | `npm run typecheck` (strict + noUncheckedIndexedAccess)                        | PASS (0 err)         |
| FORMAT (aux) | `npm run format`                                                               | PASS                 |
| AUDIT (aux)  | `npm audit`                                                                    | 0 vulnerabilities    |
| DEV LIVE     | `npm run dev` → UP 127.0.0.1:3000, /health 200, /nope 404                      | PASS                 |
| BUILD LIVE   | `node dist/dev.js` → UP 127.0.0.1:3001, /health 200 (bind verificado via `ss`) | PASS                 |

Negativos §10 aplicados ao scaffold: env inválido/blank, porta ocupada
(EADDRINUSE), método/path errados, target absoluto (400), bytes de lixo
(clientError → 400, servidor sobrevive), 50 concorrentes, double-close,
SIGTERM no bootstrap, exit 1 com erro claro. Timeout/DB/API/LLM: N/A.

## 6. Correction log (§16 — nada escondido)

| #   | Falha                             | Root cause                                                                                                                    | Fix                                                                        | Retest             |
| --- | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ------------------ |
| 1   | tsc: `net` sem `Address`          | @types/node 22 chama-lhe `AddressInfo` (verificado no .d.ts)                                                                  | import + assinatura corrigidos                                             | TYPECHECK PASS     |
| 2   | teste garbage-socket: timeout 25s | BUG NO TESTE: socket pausado com bytes por ler nunca emite `end`/`close` (reproduzido isolado; servidor sempre respondeu 400) | teste passa a consumir + **afirma o payload 400** (teste ficou mais forte) | 28/28 PASS         |
| 3   | TEST_EXIT=1 espúrio               | erro MEU de orquestração: corri `rm -rf node_modules` em paralelo com `npm test`                                              | retest sequencial                                                          | 28/28 PASS         |
| 4   | prettier: 9 ficheiros             | estilo (tabelas md, 2 ficheiros TS)                                                                                           | `format:write`; diff M001 verificado como cosmético (só padding)           | bateria total PASS |

Processo: edições paralelas ao MESMO ficheiro perdem writes (race
last-writer-wins) — regra nova: 1 ficheiro = edições sequenciais.

## 7. Audit — Third Eye (§18)

1. Funciona realmente? Sim: 28 testes + 2 processos live + `ss`.
2. Simulação escondida? Não. Único mock: 1 TEST MOCK marcado (branch
   defensivo inalcançável via HTTP); resto em sockets/processos reais.
3. Dependência inventada? Não. Versões observadas; providers futuros
   continuam UNKNOWN (correcto).
4. Vulnerabilidade? `npm audit` 0; dev-only; bind local; erros estáticos.
5. SPOF? N/A (scaffold dev, sem produção).
6. Race condition? Concorrência (50×) + EADDRINUSE + double-close testados.
7. Manipulação pelo cliente? N/A (sem estado de jogo); abusos → 400/404.
8. Duplicação? N/A (sem estado/transacções).
9. Abuso? Lixo de rede testado; servidor sobrevive.
10. Sem LLM? Sem LLM. ✓ 11. Sem API externa? Zero prod deps. ✓
11. Fallback? N/A. 13. Observabilidade? Logs + exit codes (M088+ para o resto).
12. Rollback? `git revert`, trivial.
13. Complexidade desnecessária? Não: 10 devDeps, 0 prod, 3 ficheiros src.
14. Mais simples? Single-file pioraria testabilidade; split actual é o
    mínimo testável.
15. Fundação primeiro? Sim — isto É a fundação; scaffold marcado substituível.

FUNCTIONAL: PASS · SECURITY: PASS · PERFORMANCE: PASS (n/a) ·
ARCHITECTURE: PASS · CODE QUALITY: PASS (lint 0 + prettier + strict)

## 8. Result

```text
STATUS: VERIFIED
```

## 9. Files changed (commit M002)

```text
+ package.json, package-lock.json
+ tsconfig.json, tsconfig.build.json, vitest.config.ts, eslint.config.js
+ .prettierrc.json, .prettierignore, .gitignore, .editorconfig, .nvmrc, .env.example
+ README.md
+ src/health.ts, src/dev-server.ts, src/dev.ts
+ src/health.test.ts, src/dev-server.test.ts, src/env-contract.test.ts
+ docs/modules/M002.md, docs/TESTING.md
~ docs/MODULE_STATUS.md, docs/TECH_STACK.md, docs/DEPENDENCIES.md
~ docs/RISK_REGISTER.md, docs/ARCHITECTURE.md (+ normalização prettier)
```

## 10. Test results (finais)

28 passed / 0 failed / 0 skipped · coverage 100/100/100/100 · lint 0/0 ·
typecheck 0 erros · build OK · npm ci OK · audit 0 vuln · 2 live checks OK.

## 11. Known issues

- K-01: sem Docker no sandbox (R-51) — sem impacto em M002.
- K-02: TypeScript 6.0.3 é major recente (R-52) — pinned por lockfile; gates verdes.
- K-03: `.env` não é auto-carregado (por design) — documentado.
- K-04: `node_modules/`, `dist/`, `coverage/` não persistem nos snapshots do
  sandbox — restaurar com `npm ci` (reprodutibilidade provada).

## 12. Next dependency

M003 — Server Authority (PLANNED). Pré-requisito: autorização explícita.
Nota: o scaffold dev-server NÃO é o servidor do jogo; M003 desenha a
autoridade sem herdar contratos do scaffold.
