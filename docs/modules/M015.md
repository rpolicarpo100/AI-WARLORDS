# M015 — Perception System (VERIFIED)

> Checkpoint M015 @ 2026-09-11 · 651/651 (+2 líquidos) · 100×4 ·
> percepção = saber compatível · D-008 escrita antes do código

## 1. Contrato

- Percepção por viewer: `(estado + visão) → PerceivedState` (mestre #10:
  KNOWN≠REAL, compatível com fog; INFERRED/UNKNOWN ficam em M028).
- Membership enforced loud no consumo (viewer∉roster ⇒ throw — L-27
  fecha); filtro por jogador existe (L-04 fecha; snapshot integral
  continua server-only por desenho).
- Mapa filtrado: visível integral (índice→terreno esparso), explored
  posição-sem-terreno, resto zero-sinal; roster público; spawns/nós
  omitidos fail-closed.
- Campo `secrets` REMOVIDO (mecanismo+morte, não só mecanismo); sem
  bump de versão (pré-M062, nada a migrar); chave legada ignorada
  (leniência documentada em teste).
- `toAiPerception`/`toClientView` → `(state, viewer, visibility = {})`;
  kinds preservados; `toWorldView` identidade intocado.

## 2. Pré-análise (decisões)

- D-008 precede a implementação (análise-antes-código, em `DECISIONS.md`).
- Percepção EM `views.ts` (sem ficheiro novo): `views→perception` seria
  aresta L2→L2; census 16 inalterado (consistente, tripwire não tripped).
- D-006 dois-níveis: estrutural loud (visibilidade malformada),
  contexto soft (OOB skipado, viewer-ausente vê nada, mapless ignora).
- Morte total vs campo inerte: inércia seria podridão canónica (cada
  snapshot/hash/teste pagaria `secrets: {}` para sempre, sem dono de
  remoção) ⇒ remoção declarada com selo cirúrgico.
- Sem terrain-memória (M014 guarda índices; L-29 → M028); sem guard de
  `PerceivedState` (derivado server-side; wire M069).

## 3. Implementação

- `src/engine/views.ts` (reescrito, L2): `PerceivedState`/`PerceivedMap`/
  `VisibilityInput` + `perceive` + builders; imports +`explored`/`map`
  (todos descendentes, DAG intacto); `redactFor` + `structuredClone` mortos.
- `src/engine/world-state.ts`: campo/init/guard/factory sem secrets
  (−4 spots); mais nada em produção tocado.
- Testes: views.test reescrito (12→19), world-state 30→25 (+leniência),
  size→roster-flood 100k (co-fire roster documentado), 4 map-blind→map
  filtrado, 2 goldens re-selados (selo M009 + timeline match).
- Selo regenerado com prova entry-level: SÓ os 5 átomos stateHash
  diferem (snapshot + 4 entries); eventos/log/veredicto/contagens
  byte-idênticos (old-vs-new dump comparados campo-a-campo).

## 4. Testes (6 categorias)

| Categoria    | Onde                           | O quê                                     |
| ------------ | ------------------------------ | ----------------------------------------- |
| Unit         | membership + validação + keys  | viewer loud; 5 shapes; F-09 (6/5 chaves)  |
| Integração   | perceive + fog-feed + builders | golden assembly; compute→perceive; envs   |
| Falha        | loud/soft                      | malformada loud; OOB/mapless/memória soft |
| Segurança    | non-leak + size                | terrain/nós scan; sem raw; roster-flood   |
| Determinismo | run×2 + goldens                | saídas literais; fonte nunca mutada       |
| Regressão    | suite 651/651                  | M001–M014 intactos (selo re-locked)       |

## 5. Third-Eye (ataques)

- Viewer fora do roster, visibilidade não-objecto/null/array,
  set não-array, índice mau — tudo loud (`/unknown viewer/`,
  `/invalid visibility/`).
- OOB 99 skipado; viewer omisso na visão; mapless com visão: zero
  crash, zero células, memória intacta.
- Serializado p1 contém 'forest' visível, NUNCA 'mountain' escondida
  nem 'gold' de nós; sem chaves raw (`cells`/`spawns`/`viewers`).
- Size-attack: roster 100k (≈1.6MB) fault HANDLER_FAULT directo+E2E,
  estado/revisão/log intactos.
- Desvio D-008 honestamente registado: `timelineHash` mudou além do
  `stateHash` (stateHash embebido por entry) — prova entry-level; fence
  cumprido em substância (5 átomos, mesma causa única).

## 6. Gate

- format/lint/typecheck/test/build PASS; 651/651; 100/100/100/100.
- `dist/` com 64 ficheiros engine, 0 testes (contado, não derivado);
  fence: 0 ficheiros novos; prod: views+world-state declarados.
- Selo substituído (M009 lock superseded, resto byte-idêntico):
  stateHash `f249d0d5…` → `2e1d8cc4…`;
  timelineHash `9528bb43…` → `bf3b98d3…` (4× stateHash embebido).
- Match golden: `d1e14b81…` → `e53bd403…` (mesma causa única).

## 7. Limitações declaradas

- L-29 (nova): explored é posição-sem-terreno até inferência (M028).
- L-28 mantém-se: anti-forge até fontes-owned stateful (M021+).
- Spawns/nós invisíveis mesmo em células visíveis (fail-closed;
  revisitar com gameplay de conteúdo, M016+).
- Roster público (sem fog de roster até módulo dono).
- N/A (§2 TESTING.md): rede/persistência (M062/M069+), SIMULTÂNEO/RACE
  (serial, M086), verificação manual (automatizado).
- L-09/L-12/L-13/L-15/L-16/L-17/L-18/L-19/L-20/L-21/L-22/L-23/L-24/L-25/L-26/L-28
  mantêm-se; L-04/L-27 CLOSED aqui.

## 8. Próximo

M016 Resource Engine: economia sobre nós (nós hoje invisíveis — §7);
depois militar (Fase 4); fontes-owned M021 fecham L-28.
