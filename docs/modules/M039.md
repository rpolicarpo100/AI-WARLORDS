# M039 — Army Posture (VERIFIED @ 2026-09-11)

> 5.º do bloco Strategic AI. 1.º consumer do M037
> (âmbito VOTADO, desempate votado no texto):
> agregação pura stances → postura do exército
> (distribuição + maioria) + query Match read-only.
> Primeiro L3 além de events.

## 1. Âmbito (contrato)

- `src/engine/posture.ts` NOVO (L3 — stance L2 +
  commanders L0, downward): postureOf(records):
  ArmyPosture {distribution, majority}: contagens
  por stanceOf; maioria argmax c/ desempate ordem
  STANCE_IDS (votado); vazio → zeros + balanced.
  Casts `as Stance` documentados (IDS fixo-5).
- `match.ts`: postureOf(holder) (query read-only,
  molde M038).
- Selo phase1-gate: 2 pins (census + LAYERS
  `posture: 3` — fail-loud, M015).
- Bundle rebuildado: 56178 chars (+279), md5
  `798c59b5…` idempotente ×2 (`postureOf` ×1).
- Cortes (D-033): L2 c/ mirror (drift); maioria
  undefined-vazio; lib sem query; pesos por rank.

## 2. Grounding → decisão

- Voto posture 2026-09-11 (desempate votado) + #7
  + M037 + gate (L2↛L2 dita L3) + M038 (query).
- Query incluída por decisão minha (reservada no
  voto): sem órfãos, uso imediato.

## 3. Churn (declarado)

- `posture.ts` + `.test.ts` novos; `match.ts`
  (+método, import); `match-assessment.test.ts`
  (+2); gate (2 pins; 57/393 intactos); D-033.
- state.json/godot/sim/página intocados; sim
  idêntico (1200/500/0); drill idêntico
  (1200/240/0/315/556); verify-bundle SEM regen.
- Prettier: `--write` teste novo; resto limpo;
  dirt pré-existente intacto.
- Reports `ms` revertidos, fora do commit.

## 4. Testes (8 novos → 1410/1410)

- `posture.test.ts` 6: goldens (4: mixed-4,
  tie-break duplo, vazio, expansionist-vence-
  balanced) + battery (1: 42 soma + maioria
  válida + determinismo) + pureza (1: fresh-copy).
- `match-assessment.test.ts` +2: journey query
  (tie→commission→maioria) + fresh-copy.
- Bateria 1410/1410 (1402 intactos + 8, zero
  regressões); cobertura 100 global + posture/match.

## 5. Gate e fecho

- Statics verdes (tsc 0, eslint 0); verify-bundle +
  smoke + smoke:attack verdes; sims regressão limpa.
- RISKS: baixo — pura+query; residual: tuning (#92).
- Commit: M039 Army Posture VERIFIED (1410/1410,
  100 global). BLOCO SEGUE (M040+).
