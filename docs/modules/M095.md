# M095 — Free endless (D-090, voto m095: endless)

- Status: VERIFIED+LIVE (2026-09-12)
- NEW src/engine/mode.ts L0
  (standard/free + guard);
  Match mode (default standard,
  public readonly, loud invalid;
  null→default, mold `??`).
- Free = sem seed + sem spend
  + sem ledger + sem pré
  (RECORD rail) + conditions
  exemptFree (WorldState
  intocado); presente respeita
  (congelado).
- Tests +19 (11 mode + 6 match
  + 2 victory): free 12 noops
  past-10 ongoing rev 12 (sem
  wrapper seria draw imediato
  — prova directa); census +
  LAYERS pins (mode: 0).
- Suite 1958/1958 100×4 (66
  files); bundle 91954
  `250c35ac…` ×2; match.html
  576132 (+314 exactos).
- Infra: OOM por 84 tsx
  órfãos (pkill; seed-75 flake,
  não código).
