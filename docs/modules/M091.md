# M091 — Status UI (D-086)

- Status: VERIFIED+LIVE (2026-09-12)
- Panel: botão Pulse →
  /healthz + /metrics → netOut
  (3 linhas); sem match
  required; erros → netLog.
- Tests: suite 1939/1939 100×4;
  base smoke clica netPulse
  (no-crash); API_TEST prova
  pulse ok + inventário exacto
  (0/0/2/2) + reqs.
- Bundle SAME md5; match.html
  re-embedded (page only).
