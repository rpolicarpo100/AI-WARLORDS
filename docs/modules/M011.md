# M011 — Terrain (VERIFIED)

> Checkpoint M011 @ 2026-09-11 · 27 novos testes (486/486) · 100×4 ·
> zero emendas de produção · dados antes de consumidores

## 1. Contrato

- Semântica jogável dos 9 `TerrainId` (#17): `{move, defense, stealth, ranged}`.
- Config completa validada (`TerrainConfig`, 9/9, sem parciais) + default frozen.
- API de consulta pura: `modifiersFor` (fail-stop), `cellModifiers`, `isPassable`.
- Config FORA do estado canónico (regras versionam à parte, #84).
- Valores todos configuráveis/tunáveis (#17, #82: balanceable).

## 2. Pré-análise (decisões)

- Só desvios ditados por #17 (forest/mountain/road/river/high-ground);
  village/city/resource/bridge/field neutros até o seu módulo falar.
- HIGH GROUND ⟺ mountain (sem campo elevation; L-22).
- `move`: ≥0 finito ou +Infinity (intransponível — semântica de pathfinding);
  bónus: finitos (NaN/±Infinity rejeitados).
- `terrain.ts` em L2 (importa `map`+`freeze`): L0→L0 violaria a regra de camadas (arestas descem sempre).
- Seam M022: Match fecha sobre config validado; ruleset versiona qual.

## 3. Implementação

- `src/engine/terrain.ts` (novo): `isTerrainConfig` (L47), default (L75),
  `modifiersFor` (L78), `cellModifiers` (L87), `isPassable` (L100).
- Zero emendas de produção; `phase1-gate` (census 12 + `terrain: 2` —
  função pretendida do tripwire). Mais nada tocado.

## 4. Testes (6 categorias)

| Categoria    | Onde                        | O quê                                            |
| ------------ | --------------------------- | ------------------------------------------------ |
| Unit         | `terrain.test.ts:36,89,110` | guard (15 shapes), default exacto, queries       |
| Integração   | `terrain.test.ts:110`       | `cellModifiers` via `MapData` real               |
| Falha        | `terrain.test.ts:36`        | configs hostis (faltar/sobrar/chave/mover)       |
| Segurança    | `terrain.test.ts:110`       | fail-stop em terreno desconhecido ×2; frozen     |
| Determinismo | —                           | N/A: puro, sem estado (directrizes #80 vacuosas) |
| Regressão    | suite 486/486               | M002–M010 intactos (459 testes, 0 edits)         |

## 5. Third-Eye (ataques)

- Configs com 8/10 terrenos, chave `lava`, `move` negativo/NaN/texto,
  `defense` NaN/Infinity, modificadores nulos/arrays — tudo rejeitado.
- Terreno desconhecido em `modifiersFor`/`isPassable` → throw loud.
- Default deep-frozen (topo + aninhado); OOB → `undefined`, nunca throw.
- Fixture `MapData` auto-validada (`isMapData` no próprio teste).

## 6. Gate

- format/lint/typecheck/test/build PASS; 486/486; 100/100/100/100.
- `dist/` com 48 ficheiros engine, 0 testes; fence total em produção
  (só `phase1-gate.test.ts` editado além dos 2 ficheiros novos).

## 7. Limitações declaradas

- L-22: sem elevation; high-ground = mountain (refina se elevation existir).
- Sem consumidores (M022/M023 aplicam); sem ficheiros de config; stealth sem
  mecânicas; valores default provisórios até playtesting (balance #92).
- N/A (§2 TESTING.md): rede/persistência/transporte (M062/M069+),
  SIMULTÂNEO/RACE (serial, M086), verificação manual (automatizado).
- L-09/L-12/L-13/L-15/L-16/L-17/L-18/L-19/L-20/L-21 mantêm-se.

## 8. Próximo

M012 Resources: nós de recursos sobre células `resource` (dados M012,
motor M016); depois M013 (fog).
